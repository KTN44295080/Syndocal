HISTORICAL TASK PROMPT - NOT ACTIVE INSTRUCTIONS
Retained for traceability only. This file does not establish current ownership,
completion, permission to edit, or a resume order. Use the current user task and
AGENTS.md; reusing an old assignment requires a fresh scope/ownership check.
The original prompt follows unchanged. This label does not claim it completed.

You own ONLY `crates/engine/src/lib.rs`. Continue the current acknowledged project-load allocator work after v3. Do not edit any other file. No stage/commit/push/reset/checkout.

Confirmed remaining P1: allocator reservation uses `max_id.saturating_add(1)` and public allocation uses `fetch_add(1)`. A project snapshot containing `u64::MAX` in any u64 allocator domain or `u32::MAX` timeline layer is reserved as MAX; the next allocation returns an already-authored MAX and the following allocation wraps to 0. `MAX - 1` is also unsafe with the current raw allocator: reservation becomes MAX, the first allocation returns MAX, and the next wraps to 0. Current project-load validation does not reject exhausted or immediately-exhausting allocator domains.

Fix the boundary without weakening the v3 allocator gate/admission/publication contracts.

Required:
- Add a single exact prevalidation covering EVERY ID source consumed by `sync_allocator_counters_unlocked`: fixtures; effects plus cue effect targets; cues; cue lists; palettes; playback executors; timeline events; authored timeline layers (u32); timeline audio clips; lighting and video automations; video layers; compositions; outputs; node graphs; stage objects. Also consider the derived legacy audio-layer allocation path so it cannot saturate. Reject both `MAX` and `MAX - 1` candidates unless the allocator API itself is made exhaustion-safe.
- Reject a candidate before any runtime mutation if an observed allocator-domain ID cannot leave a valid next ID. Exact error must name the domain and offending maximum.
- Reservation itself must not poison/wrap counters for an invalid MAX candidate before runtime validation. Because `send()` currently reserves before runtime validation, either validate capacity synchronously before reservation/queueing for both legacy and acknowledged load commands, or make reservation checked/skipping only when runtime is guaranteed to reject. Direct public `send(LoadProjectSnapshot...)` must not bypass the invariant.
- Replace raw wrapping `fetch_add` allocation with an exhaustion-safe primitive for both u64 and u32 domains. Preserve public signatures if practical, but it must never return a duplicate or 0 after exhaustion. A fail-closed exact panic at true exhaustion is preferable to duplicate/wrap; a Result-returning API is also acceptable only if all callers are migrated and tested.
- For the compatibility-preserving path, treat `MAX` as an exhausted sentinel: under `allocator_gate`, CAS from `current` to `current + 1`; if `current >= MAX`, panic with the exact allocator domain. Never issue `MAX` or 0. Reject authored snapshot maxima `>= MAX - 1` before reservation. A `MAX - 2` snapshot may reserve to `MAX - 1`, permit exactly one allocation of `MAX - 1`, then the next attempt must fail closed without issuing MAX/0.
- Harden every explicit-ID path in `sync_allocator_counters_for_command`, not only project snapshots. Commands such as cue-list/palette/executor/timeline-layer/audio-clip/stage-object upserts or additions must not poison a counter with MAX-1/MAX before runtime validation. Use the same checked ceiling/reservation helper or reject at the public enqueue boundary.
- After a MAX or MAX-1 candidate is rejected: authoritative shared/persistence snapshot remains A; every allocator remains nonzero and does not return the rejected existing MAX; no counter lowers or reuses an earlier ID. It is acceptable that safe non-MAX rejected candidates reserve/skips ranges monotonically.
- Preserve all v3 behavior: shared allocator gate, candidate reservation before publication, linearizable cancellation/admission, definitive wait after admission, publication-before-ack, FIFO barrier, no EngineSnapshot runtime rollback.

Add deterministic table-driven tests for every allocator domain, including u32 timeline layer and derived legacy audio-layer boundary. Test both acknowledged API and direct `send`/legacy path as applicable. Include `MAX`, `MAX - 1`, and the nearest accepted boundary. Drive the accepted boundary through all remaining allocations and assert the next exhaustion attempt fails closed without returning a duplicate or 0. Do not call a case safe merely because only its first subsequent allocation does not wrap.

Run focused boundary tests, `project_snapshot`, allocator tests, full `cargo test -p engine --locked`, fmt/check/diff-check. Report exact counts and any unavoidable theoretical exhaustion boundary. Native/release out of scope.
