# Windows native final validation — 2026-09-09

This checkpoint validates the exact executable regenerated by the configured
Windows warning gate after the checker-only commits. Product source behavior
was unchanged by those commits. No physical output was enabled.

## Warning gate and artifact

- Command: `pnpm.cmd --dir app run check:warnings -- --configuration windows-native-release`
- Result: PASS — warning ratchet `ok`; marker coverage `2/2`; baseline/current
  warnings `0/0`; first-party `0/0`; third-party `0/0`; identity removals `0`.
- Executable: `target/release/syndocal.exe`
- Version: `1.2.0-alpha.69`
- Size: `64,700,928` bytes
- SHA-256: `C22A780108ACB79D5DB0940234045F977B927D70A78D439888431995D1E2295F`

The first invocation through Windows PowerShell 5.1 was rejected before launch
because its UTF-8 handling corrupted this Japanese workspace path. It was not
counted as native evidence. The same prepared probe was then run with the
available PowerShell 7 runtime, which preserves the path.

## Probe

- Prepared probe adaptation: `target/qa/native-final-validation-20260909-01/native-final-validation.ps1`
- Evidence: `target/qa/native-final-validation-20260909-01/native-final-validation.json`
- Port: `51492`
- Process: `22440`
- Exactly one visible `Syndocal` window, title `Syndocal`, responsive and
  maximized.
- Ownership: `Standby / lighting=false / video=false`.
- `get_snapshot`: fixture/cue/video output arrays present; `video.outputs=0`.
- `get_media_asset_thumbnail` missing asset `999999`: expected not-found error,
  with valid `asset` ticket (`schemaVersion=1`, 32-character lowercase ID).
- `get_video_layer_thumbnail` missing layer `999999`: expected `MissingLayer`
  error, with valid `layer` ticket (`schemaVersion=1`, 32-character lowercase ID).
- `physicalOutputOperations=0`.
- Cleanup: exact executable process count `0`, port `51492` listeners `0`,
  `ownedApplicationExited=true`, `debugListenerRemaining=0`.

This proves the bounded Windows native launch/state/thumbnail-IPC layer only.
It does not prove the real-file missing → Retry → recovery flow, physical
ASIO/NDI/DMX output, hardware, macOS, signing, publication, venue acceptance,
or product-wide completion.
