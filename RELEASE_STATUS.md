# Syndocal historical v1.0 / current v1.2 alpha Release Status

Updated: 2026-08-25
Branch: `codex/syndocal-v1.2` (current development branch)
Original completion commit: `6c3de12`
Production-quality baseline commit: `edfc93c`
Final cross-platform CI: [run 29179218727](https://github.com/Seraf0-org/Rayard/actions/runs/29179218727)

> Current continuation note (resumed 2026-08-25): the requested pause after
> `1.2.0-alpha.10` was rescinded before final promotion. The
> [completion flow](qa/SYNDOCAL_COMPLETION_FLOW_2026-08-19.md) is again the active
> dependency-ordered work authority. The [post-alpha.10 snapshot](qa/SYNDOCAL_POST_ALPHA10_PAUSE_HANDOFF_2026-08-24.md),
> [2026-08-19 Codex handoff](qa/CODEX_HANDOFF_2026-08-19.md),
> [AI control-plane roadmap](qa/SYNDOCAL_AI_CONTROL_PLANE_ROADMAP.md), and
> [ASIO input acceptance](qa/ASIO_INPUT_ACCEPTANCE.md) remain exact subordinate
> historical or domain gate/evidence records. Do not mark AI3 complete, advance
> to AI4, or close the current Windows product release until the flow's indexed
> unchecked gates are closed.

## 2026-08-25 exact Windows linker checkpoint

All known Windows Cargo/Tauri entrypoints now fail closed on the exact supported
MSVC toolset and linker before Cargo starts. The guarded wrapper passed 156
assertions, the soak and ASIO no-Cargo suites passed 74 and 36 assertions, the
real release warning gate completed with current warnings `0/0/0`, and the final
independent review reported P0/P1/P2 zero. Exact commands, the narrow official
GitHub-hosted Enterprise exception, invalid pre-Cargo attempts, and remaining
native/CI/non-claims are recorded in
[SYNDOCAL_EXACT_LINKER_CHECKPOINT_2026-08-25.md](qa/SYNDOCAL_EXACT_LINKER_CHECKPOINT_2026-08-25.md).
Product metadata remains `1.2.0-alpha.10`; D4 integration and the next native
artifact are not claimed by this tooling checkpoint.

## 2026-08-25 continuation evidence checkpoint (pre-alpha.11)

This is a documentation/tooling checkpoint on `codex/syndocal-v1.2`; product
metadata remains `1.2.0-alpha.10`. It does not accept D4, ASIO, DJ hardware, or
any other release gate.

- The `rb-output` peer at `main` / `v1.1.1` / `cdd90e1e` was rebuilt and its
  isolated packaged `syndocal-envelope-v1` smoke passed on port `8788` without
  stopping the existing port `8787` source service. The exact EXE/ZIP hashes,
  warning counts, adapter policy, and physical non-claims are recorded in
  [REKORDBOX_STREAM_DECK_PEDAL_ACCEPTANCE.md](qa/REKORDBOX_STREAM_DECK_PEDAL_ACCEPTANCE.md).
- `tools/cleanup-stale-targets.ps1` and its focused test are now safe to track as
  a disabled design harness: two independent runs passed `55/55`, and an Ox
  adversarial review reported P0 `0` / P1 `0`. `-Execute` remains
  unconditionally fail-closed before quarantine, move, delete, Git, or writer
  mutation because a reviewed handle-pinned rename primitive does not yet
  exist. No real workspace target was removed by the harness. The separately
  verified `rb-output` build backup (40 entries, `220,809,897` bytes) was moved
  to the Windows Recycle Bin and remains recoverable.
- D4 Stage transaction integration and ASIO project-selection/capture-retirement
  work continue in isolated worktrees. Their focused or native acceptance is not
  claimed by this checkpoint.

## 2026-08-22 alpha.5 recovery-durability checkpoint (historical)

Historical checkpoint: at this date, `1.2.0-alpha.5` was then the next
distributed-development ordinal; it was not beta, RC,
or release. E2 authority bundle/generation consistency and D1 cache read purity
were reconciled as accepted. E3 automated production-path evidence was independently
green at P0/P1/P2 zero: App and its driver share publication/startup/ACK consumers,
and the registered Rust commands share the complete durable/lifecycle/publication
service with the process-boundary test. All focused frontend, Rust, build, invoke,
localization, format, diff, and warning gates passed with zero first-party warnings.

E3 was accepted at that checkpoint. The ordinary-off pause/trace implementation was independently
reviewed at P0/P1/P2 zero and passed `e3_` 6/6 with zero first-party Cargo warnings.
The E3 crash-acceptance build produced a 56,461,824-byte executable with SHA-256
`97C21F367A46375A2B6010CC6E9307E7D853CEC4EAF420150A06CA47AD9D4182` and
ProductVersion/FileVersion `1.2.0-alpha.5`.

The first exact-path kill captured durable RecoveryPublication after journal
43 -> 44 and before reply, with its browser intent retained. The second pass used
a fresh request, resumed its exact B trace at serial 45, then captured durable
RecoveryAcknowledged at serial 46 before reply/renderer cleanup; the journal and
browser intent again survived the exact-path kill. A hook-free relaunch provided
exactly one responsive, maximized `Syndocal` window at PID `44376`. Journal serial
46 and SHA-256 `22E329110C96997CFEC893CFE6EF48D5F9F77A5A453C9DE64FC91ABACAF650F9`
remained unchanged. The acknowledged recovery offer remained until a later
coherent CleanSave, as specified. Phase 1 progress was 17/71 at that checkpoint. E4 Save durability was
the next tranche; this did not complete AI3, physical I/O, ASIO, or the release.

Commit preflight removed five newly added `too_many_arguments` suppressions through
an internal context refactor and strengthened the external-admission lock-order
audit. Independent review returned P0/P1/P2 zero; first-party warnings and new
suppressions are both zero. The final checkpoint-source native build passed in
1m49s and produced a 56,470,016-byte executable with SHA-256
`8FDFD0EFB8D71D49BD1A137A4AA68F440F098B2C7B3BA253461F5C2AC14D6F19`.
PID `113948` provided exactly one responsive, maximized `Syndocal` window.

## 2026-08-22 alpha.4 development checkpoint (historical)

Scope note: all statements and evidence below describe only the 2026-08-22
checkpoint and are not current-train authority.

Historical checkpoint: `1.2.0-alpha.4` was then the current
distributed-development metadata ordinal. It
does not claim a release candidate. The sample-frame Timeline scheduler is
independently frozen at protocol hash
`48FF684AFD66680DC97F1AB5447F5FAFF920E63A5609D9ABF1FC6F6CF502BD1B` and Engine
hash `0031D9EC1AB5004779A9C28A2665DF2BC14D6122B1FAE32B40DFCB15D5735F66`.
The final plan/apply scheduler review is P0/P1/P2 zero. The native cue-audio
integration is independently green at main hash
`E69A8989E7AE0D030C7AB3FE0AA31B2D36075CB22FADA0382EDDFE99D5214750`, core hash
`D6C1A18FD09E98AB7CA849EE439AFEF93C67C700E42466CB5B4C6854F90C1927`, and DVC
hash `B81913413A4796A37B4F6FE5A146CB75114AB20F0BE1A74B700A4A3C226F3546`.
The old 25 ms detached-Sink route is removed; Click and Guide share one output
sample clock, machine-local routing is persisted, and stale identity or output
topology fails closed.

The pinned authored material is 33 Guide events, natural BPM-linked playback
rate (`170 BPM = 1000 milli`, `194 BPM = 1040 milli` under the bounded formula),
and `Trans` at 149/151/153/155 every two measures. `Complete` is the only Guide
cue at the settlement collision; the destination Intro is suppressed. Cue Audio
passed 41/41, DVC import 105/105, protocol 143/143, frontend build, 411 invokes,
3541/3541 localization, Cue runtime/browser, and the four-viewport Timeline
browser matrix. Windows default/release warning ratchets report zero first-party
warnings. After the exact-checkout process count was verified as zero,
`pnpm --dir app tauri build --no-bundle` passed in 2m37s. The resulting
56,342,016-byte executable reports ProductVersion/FileVersion `1.2.0-alpha.4`
and SHA-256
`AB98EA14F8439E23CC2E3BD80A4041C2B9CC82244A68B6938A3E9B2515A87297`.
Exactly one responsive exact-path `Syndocal` window was launched and maximized;
Edit > Timeline rendered Click, Guide, Follow waiting, shared lane structure,
and the source shelf without a runtime fault. The empty project was not changed,
so audible playback, editor + LED panel + projector operation, fixture/serial-DMX,
MTC/DJ Link, and audio-hardware acceptance remain open.

## 結論

Syndocal v1.0は、2026-07時点の`COMPLETION_PLAN.md`で定義していた**旧ソフトウェア完成条件を満たした歴史的baseline**である。

現行の拡張製品リリースは未完了である。AI control-planeのAI3は5要件すべてを監査済みで、ローカルGUIのsafety-latch Release Blackout／active Arm／exact Take Overというcanonical R4 verticalとそのネイティブ完了ゲートは2026-08-19に通過した。しかしMIDI/OSC/Remoteとall/video/per-output release、owner lease、project-swap physical fence、durable receipt/audit、rate/soak proofは未完了であり、これはAI3全体の合格ではない。Windows ASIOも配布ライセンス/成果物境界、第二vendor、rate/buffer/channel matrix、障害復旧、1時間soak、物理遅延などの受入が未完了である。旧baselineや個別修復の合格を、これら現行要件の合格へ読み替えない。

- 照明、映像、共有タイムライン/キュー/エフェクト、2D Stage Map、`.sdc`保存/復元、外部I/O境界を実装済み。
- Windows/macOS/LinuxでRust、libav、フロントエンド、Tauri、配布物生成とパッケージ起動CIがgreen。
- WindowsではNSISの実インストール/起動/アンインストール、MSIの内容、`.sdc`関連付けを確認済み。
- 1時間ソーク、クラッシュリカバリ、大規模ショー、DMXテレメトリ、複数解像度の一画面UIゲートを通過済み。

ただし、これは「全機材・全会場で検証済み」「署名済み一般公開製品」「今後追加不要」という意味ではない。以下の外部受入と公開運用は、コード完成とは分けて扱う。

## 一般公開・本番投入前の必須確認

### P0: 実機受入

- [ ] Art-Netノードと実灯体で、アドレス、色、Pan/Tilt、ブラックアウト、44Hz継続送信を確認する。
- [ ] sACNマルチキャスト対応ノードで、ユニバースルーティングとネットワークスイッチ越しの安定性を確認する。
- [ ] Enttec USB PRO / DMXKingで長時間送信を確認する。
- [ ] Enttec Open DMXはロジックアナライザでBreak 176us、MAB 16us、フレーム周期を測定する。重要現場ではPRO系を優先する。
- [ ] MIDIコントローラを実操作してnote/CC/clock/MTCとfeedbackを往復確認する。`SMC-Mixer-bt`の物理input/output列挙・open・All Notes Off送信までは2026-07-13に通過した。
- [ ] TouchOSC、iPad/Android Webリモートを実端末で往復確認し、Wi-Fi遅延を記録する。
- [x] NDI 6 Test Patterns/Studio Monitorと送受信し、探索、RGBA色、30000/1001入力、30fps出力、640x360→1280x720、接続維持を2026-07-13に確認した。
- [x] SpoutはURL To Spout/Syphon→Syndocal入力と再接続、Syndocal→TouchDesigner出力、RGBA、640x360→1280x720、送信再起動後の自動再接続を2026-07-13に確認した。
- [ ] 物理cameraのunplug/replugを確認する。`nuroum Webcam V15AF`の1280x720 RGBA取得、format negotiation、2回のstart/stopは2026-07-13に通過した。
- [ ] 対象会場GPUで最大8段ISF stack、複数layer、Preview／Program、native fullscreen、NDI／Spout、recordingを同時に一時間運転し、frame time、drop、CPU／GPU／RAM／VRAMと操作応答を記録する。自動testと短時間実GPU testを実会場合格へ読み替えない。
- [ ] macOS実機とLinux実機で、複数ディスプレイ、フルスクリーン、音声/動画素材、`.sdc`保存再読込を操作確認する。CIはパッケージ起動までである。

結果は `qa/M4_IO_VALIDATION.md` の Acceptance Capture に追記する。loopback結果を実機合格へ読み替えない。

### P0: Webリモートの公開範囲

2026-07-12のv1.1作業でアプリ内の公開境界を強化した。

- 既定は`127.0.0.1:9100`のローカル専用。LAN待受は警告付きの明示オプトイン。
- 起動ごとの6桁ペアリングPINをHTTP Remote画面とWebSocketの両方で検証。
- WebSocket/HTTPのOrigin/Host検証。
- 接続数、メッセージサイズ、操作レートのサーバー強制上限。
- 接続クライアント一覧、拒否数、受信メッセージ数、強制切断UI。

信頼済みLANモードはHTTPのため、会場共有LANやインターネットへ直接公開しない。公開が必要な場合はTLS終端を持つリバースプロキシまたはVPNの背後へ置く。

### P0: 配布と法務

- [ ] Windows Authenticode署名を行う。
- [ ] Apple Developer ID署名とNotarizationを行う。
- [ ] NDI SDK/runtimeを配布する場合、NewTek/NDIの再配布条件を確認する。
- [ ] FFmpeg LGPL動的リンク、ライセンス本文、第三者通知、対応するソース取得方法を公開成果物で再確認する。
- [ ] 現行Windows製品リリースを閉じる前に、ASIO成果物をGPLv3版として分離するかSteinberg proprietary ASIO SDK agreementを締結するかを決定し、対応source／notice義務と成果物分離を含む配布workflowを承認する。SDK 2.3.4のarchive名／SHA-256は`qa/ASIO_SDK_PIN.json`へ固定済み。通常MIT installerはWASAPIのまま維持し、別ASIO成果物をライセンス決定前に同梱しない。
- [ ] 現行alpha/beta/RCを単調に進め、全gate合格後に`v1.2.0`タグを固定し、Release Notes、SHA-256、NSIS/MSI/DMG/AppImage/debと分離ASIO成果物（承認時のみ）を同一release evidence IDへ掲載する。歴史的v1.0 artifactのhashを流用しない。
- [ ] Windows/macOSのクリーンな別PCで最終成果物をダウンロードから起動まで確認する。

個人利用または限定テスト配布では未署名のまま扱えるが、SmartScreen/Gatekeeper警告を利用者へ説明する。

## v1.2 alpha開発列の現在地（v1.1実装履歴を包含）

優先順位は次の通り。

1. Webリモート認証と接続制御（実装済み）
2. 操作履歴/Undo・Redo。Fixture、Cue、Output、Effect削除とマッピング編集（実装済み）
3. Syphon入力/出力(macOS)。Spout入力/出力(Windows x86_64)は実装・local GPU loopback済み
4. 自動バックアップ世代管理、クラッシュログ、診断パッケージ出力（実装済み）
5. HAP Q Alpha、HAP R/BC7、安全境界付き最大8段ordered single-pass ISF stack（実装済み）
6. 署名付き更新通知／install、stable・beta・nightly release channel（実装済み。公開endpoint／署名資格情報は外部運用）
7. 日本語UI/ローカライズ（実装済み。静的UI coverage 100%、主要動的状態、永続・可逆locale切替）
8. ユーザーテンプレート、MIDI／OSCマッピング共有（実装済み）。ワークスペースレイアウト保存も実装済み

### v1.1から継承した実装進捗

2026-07-12にWebリモートのセキュリティ強化を開始した。

- [x] 起動ごとに生成できる6桁ペアリングPINと、PINを含む共有用URLを追加。
- [x] Remote HTMLとWebSocket接続を同じPINで保護し、未指定・不一致を拒否。
- [x] HTTP HostとWebSocket Origin/Hostを実際の待受エンドポイントに限定。
- [x] PIN付きRemote HTMLをService Workerへ保存しないようにし、古い認証ページのキャッシュ再利用を防止。
- [x] 接続数、メッセージサイズ、操作レートの上限。
- [x] 接続クライアント一覧と強制切断UI。
- [x] 既定をローカル専用に変更し、信頼済みLANアクセスは未暗号化警告付きの明示オプトインに限定。

Webリモートのアプリ内P0境界は完了した。インターネット公開用のTLS終端はSyndocal自身へ秘密鍵を保持させず、リバースプロキシ/VPN等の運用境界として別途構成する。

2026-07-12にプロジェクト回復と診断基盤を追加した。

- [x] ブラウザ内の即時Recoveryに加え、デスクトップのアプリデータへProjectFileを原子的に保存。
- [x] 変更中のプロジェクトを最大30世代保持し、破損世代を一覧から除外して古い正常世代から復元可能にした。
- [x] Projectメニューに直近5世代の日時、元プロジェクト、復元、個別削除を追加。
- [x] Rust panic hookでアプリ情報、スレッド、panic、backtraceをクラッシュログへ記録。
- [x] エンジンテレメトリ、映像runtime、プロジェクト件数、実行環境、クラッシュログをまとめた診断ZIP出力を追加。プロジェクト本体とメディア内容は含めない。
- [x] 世代保持、検証、並び順、破損ファイル除外、診断ZIP必須エントリをRustテストで固定。

バックアップは未保存変更のRecovery用途であり、ユーザーが明示保存する`.sdc`の代替ではない。復元後はdirty状態を維持し、明示保存を要求する。

2026-07-12に編集トランザクションとSnapshot同期を更新した。

- [x] Fixture、Cue、Timeline、Video、Output、Effect、Node Graph、Stage Map編集を共通トランザクション境界で記録。
- [x] 成功した変更だけを最大100操作保持し、失敗・無変更操作は履歴へ入れない。
- [x] 同一対象の連続フェーダー／ドラッグ操作を750ms単位で1操作へcoalesce。
- [x] Undo/RedoのProjectメニュー表示、履歴件数、操作名、`Ctrl/Cmd+Z`、`Ctrl+Y`、`Cmd+Shift+Z`を追加。入力欄自身のUndoは奪わない。
- [x] 削除確認を「undo不可」から実際の復元経路を案内する文言へ変更。
- [x] 250ms pollの全EngineSnapshot転送を、revision付きtop-level deltaへ変更。不一致・再接続時だけfull snapshotで再同期。
- [x] no-op除外、連続操作coalesce、deltaの変更フィールド限定をRustテストで固定。

2026-07-12にUI密度と基本アクセシビリティを更新した。

- [x] Projectメニューから90% / 100% / 110%のUIスケールを選択し、端末内へ保存できるようにした。
- [x] 110%を含む実画面containmentゲートを追加し、1366x768で一画面デスクを維持。
- [x] 8-10px指定の可視テキストを11pxへ引き上げ、見出しのbalance、本文のpretty、数値のtabular表示、safe-area paddingを追加。
- [x] 既存のfocus-visibleを全標準入力と操作要素へ維持し、入力欄内のネイティブUndoを保護。
- [x] 2026-07-19の承認済みUI継続計画で、DMX Patch Gridを128アドレス×4ページから32列×16行の512アドレス連続面へ戻した。セルは固定上限512件、roving tabindex、矢印／Home／End移動、節目番号＋focus readout、PATCH内スクロールを使い、外側スクロールを増やさない。
- [x] 灯体一覧は80件、Stage Mapの実表示Fixture一覧は60件を超えるとfixed-row windowingへ切替え、Cue編集は12件単位、Clip Gridは12件単位、映像Layer／Outputはページ単位でDOM上限を固定。
- [x] 実ブラウザへ2,000灯体を投入し、1366x768でMapping Fixture DOMが先頭／末尾とも9行、`aria-rowcount=2000`、末尾灯体到達、画面containmentを確認。
- [x] 1280x720、1366x768、2048x1129の全Setup/Control/Touch containmentを再確認。

2026-07-13に端末ローカルのワークスペースレイアウト保存を追加した。

- [x] Setup／Control／Touch、Setup内タブ、Controlモード、Timeline／Playback面、Live Edit面、属性カテゴリを操作のたびに自動保存し、次回起動時に復元。
- [x] `.sdc`やUndo/Redoとは分離した端末設定とし、別会場・別端末へプロジェクト由来の画面状態を持ち込まない。
- [x] 未知値や将来fieldは安全に正規化し、破損JSONは削除して既定のSetup > Patchへfallback。localStorage拒否時もセッション内操作を継続。
- [x] ProjectメニューへReset Layoutを追加し、保存→page reload→Control > Timeline > Playback復元と既定配置resetを実ブラウザで検証。
- [x] 1280x720、1366x768（UI 110%を含む）、2048x1129の全画面containmentを再通過。

Lighting Cue Engine v2として、Cue番号、pre-wait、follow、tracking/block、notes、Intensity／Focus（Pan/Tilt/Position）／Color／Beam別のFade／Delayを追加した。非tracking Cueは未指定属性を灯体defaultへ戻し、pre-wait／followはengine時刻で実行する。最大16 Cue Partへfixture、video layer、video outputをそれぞれ重複なしで割り当てできる。照明はPart DelayをIFCB Delayへ加算し、Part FadeをIFCB継承元にする。映像はPart Delayまで現在state（再生、enabled、blackoutを含む）を保持してから、Part Fadeでlayer visual state／output opacityを独立補間する。target外参照、重複割当、重複Part番号は保存時に拒否し、Cue Updateやtarget削除では安全に清掃する。

複数named Cue ListとListごとの独立Back／GO位置に加え、最大64 Playback Executorを99 page×16 slotへ割り当てできる。同じCue Listを複数faderへ割り当てると位置を共有し、levelはHTP mergeされる。Playback Masterとfader levelはCue由来Intensityだけへ作用し、Position／Color／BeamとProgrammer overrideを減衰しない。参照型Palette/PresetはIntensity／Position／Color／Beam／Allを最大128件保存し、選択fixtureの現在値（Programmer staged値を含む）からCapture／Recapture、単灯／groupへDirect／Programmer適用できる。CueはPalette値をコピーせずGOごとに最新版を解決し、Cue固有の明示値を優先する。

Mark/MIBは直前Cue完了時にIntensity controlが全て0のfixtureだけ次Cueの非Intensity値を先行適用する。対象filterが空なら全targetを自動判定し、任意fixtureを選ぶとその集合だけへ限定するが、暗転安全条件は常に維持する。ProgrammerはDirect／Live Preview／Blindを切替でき、staged buffer、専用Blind DMX Preview、Commit／Clear、Cue Store/Updateへのstaged captureを提供する。全追加状態は`.sdc`、差分同期、Undo/Redo、project validationへ含め、旧`.sdc`はMain Cue List、空Part／Palette／MIB filter、Main Executor／Playback Master 100%、Programmerなしへ自動移行する。List migration、Part/IFCB／映像時間合成、MIB自動／手動filter、Blind分離、Palette参照解決、duplicate Executor HTP／MasterとProgrammer非干渉を自動テスト済み。

照明Effectの既存ordered LFO／Position Wave stack（reorder、bypass、duplicate、preset保存）へ、Colour／Chaser／Move／Value／Curve／Mappings／Colour Mappingsの7-family railと13 quick-start presetを追加し、色系2 presetを単一属性aliasから独立`Color` runtimeへ移行した。Colorは2〜8 stop、Cycle／Bounce／Sequence／seeded Random、RGB／HSV shortest／HSV longest補間、free／beat sync、phase、fixture spread、既存blend stackに対応する。RGB／ColorAdd、minimum-channel white抽出RGBW、CMY、HSV／HSB、hexまたはnamed slotを持つcolor wheelを灯体ごとに事前解決し、1 effect×fixture×tickで色を一度だけ評価する。Color wheelはDMX function rangeを全て保持しながら物理slot identityで対応色数と優先順位を判定し、未使用の補助wheelは明示Open／Clear／Whiteまたはnear-white slotだけへ戻す。混在groupは対応灯体を保持して非対応灯体だけをskipし、対応ゼロは新規追加時に拒否する。既存group targetは一時的に対応灯体が0件になっても設定を消さず、再所属／再patch時に再bindする。Amber／Lime／UV／warm/cold whiteは校正情報なしで推測せず0へ保持する。Add／Updateはsnapshot公開ackを待ち、競合時はeffectと直前errorをrollbackする。

Chase presetもPosition Wave aliasから独立`Chaser` runtimeへ移行した。最大256 ordered stepはfixture、階層group、明示gapとstep levelを保持し、最大16 Featureへ個別low／highを設定できる。Forward／Reverse／Bounce／seeded Random、最大64 Pixels on、最大16 Wings、duty、Fading／Overlap、Size、Phase、fixture spread、free／beat step clock、Override／Add／Multiply stackを提供する。fixture×step level行列とFeature bindingを事前解決し、1 effect×fixture×tickのnormalized levelを全Featureで共有してattribute hot pathのallocationを避ける。混在fixtureではFeatureごとに対応灯体を保持し、非対応の組だけskipする。Add／Updateは公開ackとeffect単位rollback、別kind更新拒否、Cue Effect Recall、preset／project roundtrip、旧`.sdc` defaultに対応する。専用Inspectorは24-cell preview、Feature coverage、ordered step／gap追加／並べ替え／置換を持ち、stepは8行、Live Rackは10 effect単位に制限する。200 fixture×64 Chaser×2 Feature×10 frameのrelease focused regressionはこのhostで約0.05秒、10 Chaser×16 Feature×64 fixture×最大width／Wingsも別budgetを通過した。previewは入力値に対する決定的表示でありengine再生位置telemetryではない。実灯体の順序／beat timing／一時間DMX負荷は外部受入として残す。

現在の照明UI比較トランシェでは、Daslight 5で先に見ていた`Moving Chaser Rnd`をPan/Tilt／Chaser／Moveの証拠から除外した。これはDimmer effectだった。確認済みのローカルSTATIC Position証拠は、最大化1920x1032で開いた`Moving-Pos > Center-Up (2)`であり、scene/group matrixとstageを残したまま、compactな2D Pan/Tilt pad、正確なPan/Tilt値、独立axis faderを表示する。Syndocalのstatic Positionを、左の常設2D pad／Pan-Tilt axis faderと、右のPosition／Movement Limits tabからなるcompact consoleへ再構成した。Position面は直接値、Nudge、3x3 target、Mirror、Favoriteを同じ面へ置き、Limits面は可動域map、数値、invert／swap、適用操作を保持する。1920x1080では913x317のsurface、289x307のpad、499pxのtool deckに全主要操作とFavorite 3件が初期表示内で収まり、内外横overflow 0、追加縦scroll 0を確認した。browserの1920x1032 measured-work-area fixtureを別gateに加え、1280x720／1366x768はpane内scrollで末尾操作まで到達し、2048x1152を含む全5 browser viewportでdocument/app overflow 0、Limits切替、keyboard tab移動を通過した。1920x1032 fixtureはnative実測とは別証拠である。

dynamic Moveは独立`Move` runtimeへ移行した。1 EffectでPan/Tiltを原子的な対として評価し、Line／centripetal Catmull-Rom Smoothの編集可能な最大32 point path、open／closed、translate／scale／clockwise rotate、Absolute／Relative、Forward／Reverse／Bounce、free／beat clock、phase／fixture spreadを提供する。pathは事前にarc-length tableへcompileし、tick hot pathはbinary searchとEffect単位のfixture pair cacheを使って追加allocationを避ける。groupは対応灯体が一時的に0件でも参照を保持し、再patch／再所属時に再bindする。Add／Updateはsnapshot公開ackとrollbackに対応し、preset／project／Cue Recallをroundtripする。Circle quick-startは旧Pan/Tilt LFO 2本から単一Moveへ置換した。200 fixture×64 Move×10 frameのdebug回帰はこのhostで74.9ms、現行full suiteは254件のengine testを通過した。専用editorはCircle／Line／Triangle／Square／Figure Eight recipe、SVG point追加／drag／削除／並べ替えとkeyboard nudge、全transform／clock／direction controlsを同時表示する。1920x1080では1111x582のeditor、586x450のcanvas、510px inspectorを2列で表示し、surface coverage 99.8%、未使用右／下余白0px、横overflow 0を確認した。1920x1032 measured-work-area browser fixtureと2048x1152 ceilingも2列、1366x768／1280x720は1列へ縮退して外側とpoint listの末尾操作へ到達する。これはsoftware/runtime境界の完了であり、実灯体のPan/Tilt方向、可動域、反転／swap、長時間DMX timingは物理受入に残す。

Timelineにsource-linked Scene Blocksを追加した。各placementはCueの内容を複製せず既存の`cue_id`を参照し、再生時に常に最新のCue bodyを解決する。`duration_ms > 0`、1〜256回の`loop_count`、任意の`jump_to_event_id`を保持し、各loop開始でsource Cueを発火する。内部Timeline Playでは最終block終端を越えたときだけtarget placementへjumpしてtarget Cueを1回発火し、同一tick内のjump chainとjump着地Cueの二重発火は行わない。外部MTC/LTCはauthoritativeなplayheadとしてloop境界をexact-once発火する一方、local jumpを意図的に無視する。旧Timeline pointは`duration_ms = 0`、`loop_count = 1`、jumpなしに互換正規化し、従来のpoint編集もブロック属性を壊さず保持する。Add／Set／Removeと全Timeline itemの一括Snapは公開snapshotのackを待ち、公開失敗時は関連collectionとplayhead guardをrollbackする。存在しないsource Cue／jump target、範囲外loop、終端時刻overflowは拒否し、Cue／event削除時は入力側jump参照を一括清掃する。Scene Blockはproject内の`.sdc`だけに保存し、独自presetやCueの複製を作らない。ブラウザ受入は主gateの1920x1080とmeasured-work-area fixtureの1920x1032で1時間・500 placement、Fit／1-2-5 ruler／zoom／pan／reveal、Lighting／Video各250件の重複cluster、実Tab/EnterとARIA、overview DOM 3500未満を通過した。1920x1032のbrowser fixtureはnative証拠ではない。このhostの2026-07-13観測値2–7 msはhandler＋microtaskのstate activationでありpaint完了値ではなく、回帰gateは両値とも`<100 ms`とする。native QAは別identifierでSampleを実Tauri backendから読み込み、Scene Block追加を通過した。常設native gateは1920x1032のWindows作業領域と実Tauri clientを分離し、最大化client 1920x1009→F11 1920x1080→Esc 1920x1009復帰を検査する。browser 500件負荷とnative操作を合成した証拠ではない。release UI受入は`pnpm --dir app run check:release-ui`でdesktop contract、汎用`check:viewport`、専用`check:timeline-viewport`、Windows native gateを必須実行する。2048x1152を拡張上限、1366x768／1280x720をcontainment fallbackとし、最悪10,000件同時可視のDOM windowing／live-tick gate、実ショーの内部Play jump／loop、外部timecode exact-once、Cue更新、Undo／再読込を残す。

Cueは選択したEffectごとのON／OFFを`CueEffectTarget`として保存し、GO／Back／直接発火／pre-wait完了／follow進行時に一度だけ適用する。リスト外のEffectは変更しない。Store Scopeへ`Effects Only`を追加し、Lighting／Video／Selected Fixture／Selected Groupでは対象範囲内だけを更新して範囲外のRecallを保持する。Cue編集は`Save Details`（メタデータのみ）、`Save Recall`（Effect Recallのみ）、`Update Look`（現在のStore Scopeを再取得）へ分離し、Live面の`Edit Cues`から到達できる。作成／Details／Look更新／Recall保存とEffect ON／OFF切替はsnapshot公開ack後に成功し、Detailsはmetadata／Part／Mark／MIBを単一mutationで確定、非Main Cue List作成も単一engine commandで割当まで確定する。明示full snapshotは250ms delta pollより優先して直列・batch化し、full同期中のdeltaを破棄してdirty判定とdraft同期を守る。旧`.sdc`は空Recallへ互換移行し、Effect削除時は参照を清掃する一方、ユーザーが開く`.sdc`の未知／重複Effect参照は黙って修復せず検証errorとして拒否する。Cueごとの閉じたRecall editorは行DOMを生成せず、開いたeditorも検索付き48件pageへ制限し、500 Effect×12 Cueを全5 viewportで検査する。Recallは現時点では即時ON／OFFであり、Effect parameterのCue別morph／fade、画像／映像RGB sampling、校正済み多原色、専用Curve／Value／Mapping engineは未達である。

旧Library／Form／Graph／Stack縦積みUIは、Effect Library／Inspector／Live Rackを同時表示する全画面3-pane deskへ刷新し、適用操作を各pane下端へ固定、Stack／Graphsをrack tab化した。Color inspectorはgradient、stop color／hex／position／並べ替え／追加削除、algorithm／interpolation／clock／spreadを提供し、scalar Attribute／waveform／video targetを表示しない。Chaser inspectorはpreview、4 direction、Pixels on、Wings、Size、Fading／Overlap、Spread／Phase、Feature range／coverage、ordered stepとgap編集を同じ作業面へ置く。Move inspectorはpath canvasとpoint operations、transform、coordinate／interpolation、clock／direction／phase／spreadを同じ作業面へ置く。1920x1080を最初の主運用／visual gate、1920x1032 measured-work-area browser fixtureを第二の必須browser gate、2048x1152を拡張上限、1366x768／1280x720をcontainment fallbackとして、外側scrollなし、3 pane／2 action dock／rack tab、8 family button、13 card、4 target-required card、Color／Chaser／Move各editorの操作と横overflow 0を検査し、2219/2219の日本語coverageを維持する。native受入はbrowser fixtureとは別に扱う。詳細な受入境界は`qa/LIGHTING_FX_ACCEPTANCE.md`、`qa/CHASER_FX_ACCEPTANCE.md`、`qa/SCENE_BLOCKS_ACCEPTANCE.md`へ固定した。

VJ Deskは小さな3-column card群から、Clips／Live Monitors + Outputs／Layersの全高3-pane deskへ刷新した。中央は手動reference snapshotを廃止し、PでstageしたLayerのPREVIEWと選択Outputのcomposition／opacity／blackout／projection mappingを反映するPROGRAMを常設した。clip/audio/capture設定は折りたたみ、Master・A/B・Record・FFT・Output・Layer/FX rackをpane内で分離した。TouchはGO／Pause／Lighting Master／Video Master／DMX BO／Video BO／All BO/Clearを最上段の常設Safety Deckへ移し、全操作targetを48px以上、critical blackoutを56px以上にした。Remoteサーバ設定はTouchから除外してSetup > Remoteへ一本化し、Standby Syncはraw Tauri invokeを廃止、browser previewでは例外を出さずDesktop requiredとして無効化する。Live Deskは上段を全解像度340pxに固定し、statusを300px以下の連続rail、Cue Padを残り幅のflat scene matrixへ変更、下段へStage／Show Timelineの高さを返した。active fade専用行をMasterの直上へ固定し、fade中もCue Pad／Masterが暗黙grid行へ移動しない。Setup > DMXは狭い高さでDMX InputとRDMが重ならないmax-content rowへ変更し、Setup > MappingはGroups／Types／Stage Configの不足grid行と220px inspectorの横切れを修正した。Setup > Videoは縦積みformからOutputs/Sources／Projection Map／Output Controlsの固定3-pane deskへ変更し、Map editorを最初の作業面へ、PreviewとEnable／Blackout／Preview／Open Windowを右の常設dockへ移した。1280x720／1366x768／2048x1152で外側scrollなし、VJ 3 pane + Preview/Program 2 bus、Touch Safety Deck + 4 guard操作 + 48px target、Live上段330-342px、fadeとMasterの行順、DMX direct-child overlap 0、Mapping filter／sidebar clip 0、Video pane幅222/740/288px以上と重要3操作のviewport内表示を検査した。

VJ Deskにdesktop window mode連動のfullscreen Focusレイアウトを追加した。1600x900以上のfullscreen時だけ重複する外側`VIDEO CONTROL` headerを除去し、Clips／Live Monitors + Outputs／Layers、Preview／Program、live-audio dockを大画面運用向けに再配分する。1920x1080のbrowser fullscreen fixtureでは3 paneが684／811／405px、Preview／Programが317／476px（Program/Preview 1.502）、audio dockが92px、主操作／設定操作が32／28pxとなり、4個の設定labelとOVR／XRUN／C→W／I/O／BUF／CB／Q telemetryを保持したままtruncation／critical overflow／viewport外要素0を通過した。1366x768／1280x720は従来の36px header、64px audio rail、Preview／Program 1:1を維持する。専用identifierのcurrent-source native QA buildでは最大化1913x1080で通常header／compact audioを確認後、F11でtitle bar／taskbarのないexact 1920x1080へ移行し、外側header除去、Program優先monitor、92px audio dock、設定labelと二段telemetryを確認した。`TOPPING Pro USB Audio Device`を48kHz／128 frames／all-to-monoでStartし、約7秒後もOVR 0/0、XRUN 0、callback 128/128/128 framesを保持した。Stop後のEscで1913x1080の通常配置へ復帰した。

大画面VJ operator面のLayer／Output密度を実show相当fixtureで更新した。Layerはcompact bankを3件から6件へ拡張し、各rowへBuilt-in FX、選択段のEnable／Bypass、Advancedを常設する。Advancedだけが最大8段のordered stack、段選択、On／Off、Up／Down、Reset、Remove、Add ISF、Clear stack、選択段parameter editorをmountし、閉じたlayerの重いeditor DOMを作らない。Outputは全出力をLive／Off／BO付きrailへ常設し、rail／Prev／Next、選択detail、Program labelとProgram monitor request output IDを同じ選択へ同期する。7 layer（6+1 bank）、layer 1の最大8段stack、3 outputの専用fixtureを英日×1920x1080／1366x768／1280x720で駆動し、Advanced DOM 0→1、8 row、Event／Bool／Long／Float／Point2D／Colorの全種、8/8 Add lock、Event 1→0・履歴不変・操作中interlock、Advanced／選択／focusを維持するMove、Remove後の安全な隣接選択、段別GPU診断更新、6 transaction／6 snapshot／7 diagnostics refresh、段index 0のQuick Bypass、output 1→3の四者同期を通過した。1280x720ではA/V statusをrow 3、Clip Gridをrow 4へ固定し、録画error／drop telemetryも残す。初回のelement rect検査はfallback幅でAdvanced actionが約4px逸脱する実不具合を検出し、compact grid修正後は全操作phaseでunsafe overflow 0／owner外rect 0となった。final current sourceを別identifierのASIO有効・no-default-feature native QAへ再buildし、空projectではあるが装飾付き最大化1913x1080→F11 origin 0 exact 1920x1080・title bar／taskbar／重複headerなし→Esc 1913x1080復帰を再確認した。マイクと外部出力はこの最終UI確認では起動していない。これは最大8段表示、全control種、Event／Enable／Bypass／reorder／remove、operator UIとDOM budgetの合格であり、実Add／import／reset／clear、4K長時間負荷、native pixel latency、SynapseRack／TouchDesigner同条件比較の代替ではない。受入境界は`qa/VJ_OPERATOR_DESK_ACCEPTANCE.md`を正とする。

PREVIEWはProgram layerの再生位置を追従する表示から、明示的なP操作だけでstageするruntime専用transportへ分離した。ローカルFile／Still Imageをclipのin-pointへ停止状態でstageし、Play／Pause、seek、-4x〜4xの正逆速度、既存loop範囲、ClearをProgramへengine commandを送らず操作できる。Cutは0ms、Takeは指定fadeで、stage済み位置と速度を選択clipへ引き継いでProgramへ送る。選択だけでは自動stageせず、stageのないTakeも拒否する。Preview stateは`.sdc`、Recovery、Undo/Redoへ保存せず、New／Load／Undo／Standby／layer削除／source identity変更／Clearで破棄する。専用rendererはcapture／NDI／Spout registryを持たず、decode prefetch 0、stage対象1 layerだけのsnapshot、非blocking reset、render後とJPEG encode後のgeneration／source再確認で、Program cacheの消費とstale frame公開を避ける。これは独立playheadを閉じたもので、PREVIEW／PROGRAMとも最終native WGPU swapchainのsampleではない。Camera／Screen／NDI／SpoutのPreview transport、Preview音声scrub、実素材multi-layer負荷と会場GPUでの持続freshnessは未受入である。

空のVJ Deskへ安全な初回セットアップ導線を追加した。最大64個のローカル動画を選択し、blocking workerで全pathを検証した後、停止状態のFile layer群とMainへ接続した1920x1080 Display outputを単一engine publication barrier／単一Undo transactionで作る。Outputは常にwindowed、Off、Blackoutで、出力windowや再生、Take、外部transportを自動起動しない。キャンセルはmutation 0、重複実行は拒否する。同一tickの後続commandを混在させずexact snapshotを公開してからackし、snapshot lockを5ms以内に得られない場合はruntime変更もrollbackする。空VJ専用1280x720／1366x768／2048x1152 containmentはCTA全体がviewport内にあることを検査し、safe workflow test、1997/1997日本語coverageを通過した。さらにLibavはdecoder/demux sessionとRGBA frameを各layer 1件、global LRU 8 layerへ制限し、250ms以内の前進を継続decode、同一decoded frame範囲をreuse、逆行・loop・大jump・source/size/signature変更・errorを再open/evictする。File以外、pathなし、HAP迂回では旧sessionを即解放し、PTSとseekをstream start-time基準へ正規化した。実MPEG-4 B-frameとoffset-start映像をFFmpeg逐次RGBA oracleと比較し、forward、seek/reverse、EOF drain、terminal reuseを検証する。CLI fallbackもlayer 1 frame／global LRU 8へ制限した。session/open/reset/continue/reuse/eviction/errorは診断へ公開する。scalerのrequest間reuse、worker間session共有、実素材multi-layer monitor負荷は未検証のため、SynapseRackより軽いとはまだ主張しない。

DMX出力routeの設定上限を64から256へ引き上げた。128個の異なるArt-Net universeを同時に有効化したproduction engine tickで128/128 send成功・0 failureを確認し、無効routeを含むstate/tickでも128件のroute telemetryを保持する。ブラウザ受入fixtureにも128 routeを投入し、Setup > DMXの全route表示が1280x720、1366x768、2048x1152でfull-window containmentを維持する。Daslight 5の公称100 universeを構成数では上回ったが、実Art-Net/sACN nodeを用いた128 universe一時間packet loss／jitter受入は未完了であり、性能優位の根拠にはまだ数えない。

Serial DMX出力はENTTEC USB Pro packet、DMXKing ultraDMX互換packet、Open DMX／FTDIの250000 baud 8N2 + dedicated timing workerを維持する。Scan SerialはUSB VID/PID、product、manufacturer、serial numberを返し、既知のsingle-port ENTTEC Pro／DMXKing／Open DMXだけへ確認式の推奨protocolを表示する。generic FTDI、Pro Mk2、Ultra Proは誤った自動選択を避けてmanualのままとする。product判定と未知／dual-port非推奨をRust testで固定し、推奨UIは128 routeを同時表示した全3 viewportでcontainmentを通過した。QLC+級のnative USB catalog、Pro Mk2／Ultra Pro二系統、DMX4ALL／Vince、serial DMX inputと実機長時間受入は残る。

DMX入力の第一段としてArt-NetとsACN E1.31の受信、ユニバースフィルタ、HTP/LTPマージ、信号断タイムアウト、自動マージ解除、受信元／packet／invalid packet状態を追加した。入力は専用threadで受信し、engine command queue経由で44Hz renderへ渡す。アプリblackoutは入力マージ後にも優先して0を出す。Setup > DMXから起動・停止・状態確認でき、UDP loopback、HTP/LTP、signal clear、blackoutを自動テスト済み。

DMX出力経路は、初期化／送信失敗時にsenderを破棄し、250msから最大10秒までの指数バックオフで自動再接続するようにした。待機中は44Hz engine threadをブロックせず、連続失敗数、再接続試行数、次回試行までの時間、最終成功時刻を経路別telemetryに表示する。複数経路を併用すれば一方の障害中も他方への送信を継続できる。回復状態遷移と上限付きbackoffは自動テスト済み。

時刻同期はManual／Tap／MIDI Clock／MTC／LTC timecode注入／Ableton Linkを共通ClockSnapshotへ集約し、Timeline、Cue発火、BPM同期Effect／映像Layerが同じsourceを参照する。外部pulse/timecode受信時刻をmonotonic clockで保持し、MIDI Clockは500ms、MTC／LTC／Linkは1,000msを超えるとLOCKからSTALEへ遷移する。Runtimeはsource、最終同期からのms、LOCK／STALE／INTERNALを表示し、Live Show面は`HH:MM:SS.mmm` Timeline timecode、Clock source、Sync状態を常時表示する。旧`.sdc v1`で新しい同期health fieldがない場合は外部同期なしへ互換読込する。MIDI pulse、Link freshness／stale、MTC／LTC timeline同期とCue発火を自動テスト済み。

Active / Standby冗長化の第一段として、専用共有フォルダー（UNC／mounted shareを含む）へPrimaryが2秒ごとに完全な`.sdc` checkpointを発行し、Standbyが500msごとに追従するwarm standbyを追加した。checkpoint本体とmanifestは世代別の不変fileとして本体→manifestの順でatomic commitし、FNV-1a checksum、byte長、ProjectFile validationを通過した世代だけを読む。通常の追従とローカルlatest読込は破損した最新世代から直前の正常世代へfallbackし、各Primary sessionを5世代保持する。一方、認証されたcontrol-plane Take Overは同意対象のsession/generationを厳密に固定し、valid・破損・非正規の新世代候補を検出した場合は旧世代へfallbackせずworker停止前に拒否する。Standbyへ適用するsnapshotはlegacy DMX output、全DMX route、全video outputを無効化し、lighting／video blackoutを強制するため、追従だけでは送信を開始しない。heartbeat停止をStandby側のmonotonic clockで5秒判定した後に手動Take Overでき、heartbeat生存中または複数Primary検知中は旧Primaryを物理的／ネットワーク的にfenceしたことを明示確認しない限り拒否する。世代保持、integrity fallback、exact-generation拒否、全出力disarm、split-brain検知はRustテスト済み。これは共有storageを用いたmanual failoverであり、自動fencing、quorum、WAN越しのconsensus、無停止のaudio/video sample-clock引継ぎは未実装なので、二重送信を防ぐ運用手順と実会場LANでのfailover計測は正式受入に残る。

RDMはE1.20 packet core（48-bit UID、GET/SET/DISCOVERY command class、PID、可変parameter data、message length、16-bit additive checksum）、DEVICE_INFO decode、collision-safe DISC_UNIQUE_BRANCH response decodeを追加し、破損長／checksum／未知commandを拒否するテストを追加した。さらにArt-Net 4 OpRdm（0x8300）のheader、15-bit Port-Address、FIFO情報、StartCode除外／復元を実装し、unicast UDP request/responseはtransaction number、送受信UID、gateway IP、Port-Addressが一致した応答だけを採用する。ArtTodRequest/Dataは最大200 UIDのmulti-block、out-of-order block、UID total/lengthを検証してgatewayのTable of Devicesを自動取得し、ArtTodControl.AtcFlushからfull physical discoveryも開始できる。USB経路はENTTEC DMX USB Pro API v1.44のframe（0x7E／label／little-endian length／payload／0xE7）を実装し、Label 7でRDM requestを送り、Label 5のreceive statusとRDM packetを検証する。Label 11の38-byte DISC_UNIQUE_BRANCHを48-bit範囲へbinary probeし、collision分割、発見UIDのDISC_MUTE、開始時のbroadcast DISC_UN_MUTEを行うfull physical discoveryも追加した。probeは総timeoutと16,384回上限で暴走を防ぐ。選択COMはexclusiveにopenし、同じportでENTTEC／DMXKing DMX outputが有効なら二重所有を拒否する。ACK_OVERFLOWは同一GET/PIDを新しいtransaction numberで最終ACKまで再要求し、全blockのparameter dataを連結する。ACK_TIMERは16-bitの100ms単位見積りを総timeout内で待ち、GET QUEUED_MESSAGE（STATUS_ERROR）を取得する。status/unrelated queued responseは25ms間隔、全follow-upは64回上限で、NACK reasonもdecode／表示する。RDM consoleはArt-Net gatewayとENTTEC USB Proを切替でき、50ms-120秒timeout、response block／timer／queue回数、Art-Netでは5秒間隔のTOD inventory監視と追加／削除数を表示し、USB discovery結果もUID selectorへ反映する。SETとdisruptiveなfull discoveryは送信前確認を要求する。loopback transport、header/checksum破損、multi-block TOD、ACK_OVERFLOW連結、ACK_TIMER→QUEUED_MESSAGE、USB frame codec／receive payload／full 48-bit DUB request、UID/hex parserを自動テスト済み。RDM-capable firmwareの実機応答、collisionを含む複数実灯体discovery、Art-Net／USB双方の継続inventory／応答時間受入は未完了のため、現時点では「全RDM機材対応」とは扱わない。

VJ操作として、動画／静止画を最大64件ずつ複数選択してメタデータ付きレイヤーへ一括取込できる。Control > VJ Deskは照明Live Desk／Stage／Faderを隠す専用full-window surfaceで、空のClip Gridから直接OSの複数素材選択を開ける。12 pad単位のClip Gridを備え、全clipへbank切替で到達できる。各padはLIVE/READY、直接launch、PreviewからのCut／Take、Stopを提供する。直接launchはoverlayを維持し、Cut／Takeは他clipを停止またはfade outして選択clipだけをProgramへ出す。Deck A/Bへ別clipをロードして同時再生し、crossfaderでlinear opacity mix、Program audio有効時はequal-power音声mixを行う。専用160x90 thumbnail経路はlive visibility／opacity／master／blackoutに依存せず、source identity単位のcacheと44Hz engine thread外のdecodeで操作面を安定させる。Tauri `core:default` capabilityをmain/output windowへ限定付与し、event listen／window lifecycleを許可したため、release UIへACL errorを露出しない。

Live Monitorは40-byte `SYLV` v1 headerとJPEG payloadを`tauri::ipc::Response`のraw ArrayBufferで返し、旧JSON byte arrayとfrontend PNG／base64変換を常時経路から除外した。Program 10fps／Preview 5fpsを単一の直列loopで要求し、VJ Desk外／document hiddenでは停止、target変更時は旧応答を破棄する。共有preview rendererは`try_lock`競合時に待たずbusyを返し、UIは最終frameを保持してbusy drop、fps、render + encode時間を表示する。要求は最大640x360／230,400 pixel、JPEG品質40-90、Program decode budget 1-2へ制限する。packet／JPEG test、1956/1956 localization、3 viewport containmentは通過したが、実素材の持続fps／freshness／native output干渉は未測定である。

ffprobeでvideo/audio streamを分けて素材metadataへ保存し、silent素材のaudio操作を事前disableする。埋め込み音声はsystem defaultまたは選択deviceへmonitorでき、videoのplay／pause／0.25-4x speed／loop／positionと0-200% gainを追従する。Program audioは明示ON時だけLaunch／Take／Stopへ連動する。音声monitorが有効な間はUI pollに依存しない専用workerが25ms周期でA/V driftを監視し、75ms超を250ms cooldown付きで映像位置へ再同期する。無音時は250ms idle周期へ下げ、44Hz engine threadとaudio callbackの双方をブロックしない。現在drift、最大絶対drift、再同期回数、直近エラーはClip Gridへ表示する。

選択video outputは1-60fps、最大4096角でbackground rendererからFFmpeg raw RGBA pipeへ送り、H.264/yuv420p MP4として記録できる。Program audio toggleが有効なら、選択outputのcompositionに属し、再生中／有効／monitor中のlocal audio sourceだけを開始位置、loop、gain、0.25-4x speed付きでFFmpegへ入力し、AAC 192kbpsへmixしてMP4へmuxする。対象音声がなければ明示的にsilent recordingへfallbackする。UIはaudio requested／included／track countとwritten／dropped frameを表示し、停止時にencoder結果を確定する。FFmpeg入力選別、seek／loop／volume／atempo／amix graphとsilent fallbackの自動テストに加え、実際のraw RGBA + PCM toneからH.264/AAC MP4を生成し、FFprobeでvideo/audio両streamを確認するlocal runtime testも通過した。さらに2026-07-13、production録画commandへ30分・30fps・54,000 frameを通し、冒頭／末尾の白flashと1kHz click、中央の黒／無音をdecode検証した。video/audioはいずれも0.000秒開始・1800.000秒durationで、first/last stream driftは0.0msだった。ただし映像decoder、monitor audio device、recording encoderを単一のsample clockで駆動するsample-accurate同期は未実装であり、実素材を用いたlive decoder／monitor／encoder一体の実会場受入に残る。

Windows x86_64のSpout input/outputは標準featureとしてSpout2 2.007.017を静的リンクした。外部映像route起動時に専用DirectX 11 workerを生成し、入力は送信元の解像度とRGBA/BGRA format変更を追従してlatest frameだけをdecoderへ渡す。出力は選択compositionを60fpsで合成し、R8G8B8A8 senderとして公開する。通常previewとnative outputもSpout input frameを利用でき、停止時はworkerとframe registryを解放する。実GPU loopbackに加え、2026-07-13にURL To Spout/Syphonの1920x1080 DXGI 28入力、外部sender停止／再起動後の同一worker復帰、TouchDesigner 2023.12000への8-bit RGBA出力、640x360→1280x720変更追従、sender再起動後の自動再接続が通過した。Syphonはschema／route／Unavailable診断を維持するが、現行`syphon-wgpu 0.3`がwgpu 29を要求し、本体のwgpu 25と同一graphへ安全に導入できない。macOSの`Syphon.framework`と対応Metal integrationを揃えたhostでの実装・受入を外部残件とする。

NDI 6はlocal loopbackに加え、2026-07-13にNDI Test Patternsからproduction `NdiInput`へ1920x1080 RGBA・30000/1001を受信し、production `NdiOutput`からStudio MonitorへQA color barsを送信した。SDK senderの接続数1-2を確認し、640x360から1280x720への変更後も接続を維持した。このhostはmachine-wideの.NET 7 WindowsDesktop runtimeconfigが破損しているため、`qa/harnesses`のlauncherが子processだけ`DOTNET_ROLL_FORWARD=LatestMajor`を設定し、Program Filesは変更していない。

Camera inputとScreen Captureをlive video sourceへ追加した。レイヤー有効時だけFFmpegを持続起動し、WindowsはDirectShow／gdigrab、macOSはAVFoundation、LinuxはV4L2／X11Grabから1280x720・30fps RGBAを読み取る。フレームごとのprocess起動は行わず、workerとdecoder間でlatest frame bufferを交換・再利用する。Preview、native display、NDI／Spout outputも同じcapture registryを参照し、route停止時はFFmpegをkill／waitしてframeを破棄する。endpointは単一Command argumentとして渡しshell展開しない。command構築、未知backend拒否、project source名正規化、Camera／Screen decoder経路を自動テストし、Windows実desktopを取得して最初の完全frameと停止処理を確認するlocal runtime testも通過した。2026-07-13には物理`nuroum Webcam V15AF`をproduction workerで2回start/stopし、各回1280x720 RGBAを取得、MJPEG 1280x720／1920x1080／2560x1440の30-60fpsとYUYV modeのformat列挙も確認した。物理unplug/replugとmacOS／Linux実hostはM4受入へ残る。

HAP Q Alphaはin-process MOV parserが分離したYCoCg DXT5色面とBC4 alpha面を検証し、既存YCoCg変換とBC4 endpoint／3bit index補間でRGBA8へ合成するようにした。HAP Rは公式仕様の`Hap7`／RGBA BC7 frameをin-processで保持し、BC texture対応GPUでは圧縮blockを`Bc7RgbaUnorm` textureへ直接uploadしてhardware samplingする。Preview、ISF、またはBC非対応GPUではsafe pure-Rust `bcdec_rs`でRGBA8へ展開し、4の倍数でない端blockもcropする。HAP in-process経路が未対応formatや破損frameを返した場合はlibav、続いてFFmpeg CLIへfallbackする。HAP Q Alpha dual-plane、Hap7 MOV、BC7 alpha、CPU fallback、GPU/CPU parityと実GPU 4K BC7 frameを自動テスト済み。実エンコーダー由来HAP R素材の長時間multi-layer再生はM4受入へ残る。

ISF 2 single-pass image filterを、レイヤーごとに最大8段（旧root 1段＋tail 7段）のordered stackとしてimport／内蔵追加、埋込保存、段選択、reorder、bypass、reset、削除、parameter操作できるようにした。VJ Deskから即時追加できる14種の内蔵GPU FX（Invert、Monochrome、Threshold、Posterize、RGB Split、Mirror、Kaleidoscope、Zoom、Rotate、Strobe、Scanlines、Colorize、Vignette、Glitch Shift）をカテゴリ付きselectorで提供し、全shaderをproduction ISF準備経路で自動compileする。Event／Bool／Long／Float／Point2D／Colorを各段最大16 controlまで解釈し、GLSLをNagaで検証済みWGSLへ変換する。control、bypass、reorder、reset、removeはstage indexを持つtargeted commandで更新し、mutation request側でstack全sourceを送り直さない。旧単一effect objectをrootに残してtailを省略可能`stack`として保存するため、`.sdc v1`の旧単一effect／effectなしlayerと互換読込する。1 stackはsource合計512 KiB、project全layerのISF source合計16 MiB、project file全体64 MiBに制限し、image input 1個、single passに限定する。multipass／persistent buffer／imported resource／audio・audioFFT／loop／atomic／imageStore／明示binding・uniform・include・extensionを拒否する。

実行時は有効かつcompile可能な段だけを順番に一つのwgpu command encoderへ積み、frameごとにhost→GPU upload 1回、2枚のGPU-local textureでping-pong、最終GPU→host readback 1回とする。変換結果／GPU pipelineは最大64 shaderまで再利用し、Preview、native display、NDI／Spout、recordingの各rendererが使う共通layer処理経路で適用する。壊れた段はlayer／stage index／label付きerrorとして除外し、残る正常段を継続する。全段失敗またはGPU runtime失敗でも元frameへfail-openする。診断はpipeline数、直近stack実行段数、stack render microseconds、段別errorを公開する。実GPU 1→2→3段の順序／bypass、cache再利用、invalid shaderの元frame fail-open、stack mutation／root昇格、control clamp／reset、複製、保存／旧単一effect互換、unsafe tail／9段目拒否を自動テストで固定した。

ただしISF runtimeはheadless rendererの独立wgpu deviceで最終RGBAをCPUへreadbackして返し、native outputは別deviceへそのframeを再uploadする。したがってstack内部の段間転送はGPU-localだが、decode→effect→composite→display全体のzero-copy、shared texture、任意node graph、TouchDesigner級のGPU-resident networkは未実装である。14種から100種以上へのlibrary拡張、multipass／feedback、実在ISF pack互換範囲、4K multi-layer一時間／実会場soak、同一hardwareでのSynapseRack／TouchDesigner A/Bも未受入である。

Application UpdateはTauri updater 2.2のminisign検証付きdownload／installをProjectメニューへ統合した。release buildがcompile時にHTTPS endpoint、base64化した公開鍵、`stable`／`beta`／`nightly` channelを全て埋め込んだ場合だけ起動後に非同期checkし、未設定buildは理由付きdisabledとして動作する。endpointはHTTPS／host必須、credential／fragment禁止、公開鍵はbase64 decode後にminisign keyとして事前検証する。Installはcheck時のversionを再照合し、manifestが差し替わっていれば拒否する。download前に未保存状態を含む検証済みProjectFile backupを`before update <version>`として原子的に保存し、download／verification状態をUIへ通知する。公開鍵だけをbinaryへ持ち、private keyはrelease secretに限定する。Updater artifact生成用config overlayとkey custody、manifest、改竄／wrong key／platform signing受入を`qa/UPDATE_RELEASE_RUNBOOK.md`へ固定した。公開release endpoint、private signing key、Windows Authenticode、Apple notarization資格情報は外部release operationsの残件である。

User TemplateはProjectメニューの`Save as Template`／`New from Template`から`.sdctemplate` v1として共有できる。現在の`.sdc`互換ProjectFile、埋め込みfixture profile、MIDI mapping、OSC mappingを一体で保存し、読込時にapp／version／project参照／mapping値域と64MiB上限を検証する。旧templateでmapping fieldが省略された場合は空として互換読込する。templateから作成したshowは必ず未保存projectになり、legacy DMX output、全DMX route、全video outputをdisabled、lighting／video blackoutをONにして意図しない送出を防ぐ。既存の`.midimap`／`.oscmap`単体共有も維持する。roundtrip、旧field省略、identity／拡張子拒否、全出力disarmをRust testで固定した。

2026-07-15〜16に、Daslight 5実機（ユーザー実ショー`Shinkan2026.dvc`のライブキャプチャ）とSyndocal正規スクリーンショット43枚の並置比較に基づくUI再設計を開始した。計画・受入境界は`qa/UI_REDESIGN_PLAN.md`を正とし、QLC+はユーザー指示によりUIベンチマークから除外した。T1は決定論的show-identity色相（cue/groupごとのFNV-1aハッシュ→GOアクセント±20°を除外した21スロット、`--identity` CSS変数のみで配布、`check:identity-color`ゲート付き）をCue Pad／タイムライン／Cue行／グループチップへ貫通させ、ライブ計測でcue#1=hue150・cue#2=hue345の全面一致を確認した。T2はTimelineOverviewを固定44単位viewBoxからピクセル空間SVGへ変換し、identity色相の2バンドブロック（Cue名＋`clockDisplay.formatCompactClock`によるM:SS.ffデュレーション刻印、幅による4段縮退）、64px HTMLガター（レーン名＋件数＋eye切替）、フェードウェッジ、旗形ポイントマーカー、専用プレイヘッドを与えた。automation drag／resize／keyframe座標系は同一意味論でピクセルヘルパーへ再結線し、500ブロックfixtureのDOMは3054（<3500予算）を維持した。T9はSetup>Mappingを照明専用床面図へ純化した: 投影面はXZ上面図で幾何学的に正しい線分＋向きティック＋ラベルの参照表示のみ（レイヤー既定OFF、ワープ／コーナーピン等の投影編集はSetup>VideoのProjection Mapへ一本化し、Mappingサイドバーへ「映像セットアップで投影を編集」ジャンプを追加）、Screen種Stage Objectは薄帯表現、灯体はタイプ別コンパクトグリフ（moving=円＋ヨーティック／par=角形／bar=横長／laser=三角、既定Fitズーム実測13.5px、透明ヒット領域で12pxゲート維持）、グリッドminor/major階調、マーキー改善、ズームスライダーを追加した。さらにユーザー指摘（Control/Touchステージが旧描画のまま）を受け、新設`StageGlyphs.tsx`を灯体／投影線分／Stage Objectの単一レンダラー定義とし、Mapping編集ステージとStagePreview2Dの双方が消費する構造へ統一、Controlステージで旧リング／歪みポリゴンDOMゼロをライブ計測で確認した。各スライスは実装（T1/T2=Opus、T9=Opus+Codex gpt-5.6-sol）後にFableがtsc／build（index 490-494kB）／localization 100%／viewport全マトリクス（207 pass）／timeline系／large-show 2000灯windowingを再実行し、ブラウザJS計測で受入してからコミットした（`51212a1`／`d8c272a`／`1db7af5`）。ワークスペース切替でレイアウトが全変わりする問題への回答として、下半分（GROUPS＋編集可能2Dマップ＋SELECTIONS＋コンテキストペイン）を常設し上半分のみ交換するT10統一シェル、Daslight TOUCH同等のEDIT/LIVE切替型編集可能TouchサーフェスT11をユーザー承認済みで計画に固定した。これらはブラウザゲートとローカル計測による内部証跡であり、Daslight 5との同一タスク操作手数比較・実会場運用証跡はまだ主張しない。

2026-07-20にUI継続計画（`qa/UI_REDESIGN_T15_T17_PLAN.md`）のT16 FXエディタ可視化を完了した（Codex gpt-5.6-sol実装、Fable監督検証、先行するT15-P基盤・T15タイムライン減量の上に構築）。Control>ライブ編集>エフェクトのFXワークスペースへDaslight同順の9ファミリーchooser（STEPS／COLOR FX／CHASER FX／MOVE FX／VALUE FX／CURVE FX／MAPPINGS／COLOR MAPPINGS／SUPER SCENE、STEPS/SUPER SCENEは既存編集経路への遷移ファミリー）を常設し、Effect Library／Inspector／Live Rackの各カードへ実データ駆動のグラフィカルプレビューを追加した。新設`effectVisualization.ts`／`EffectGraphicalPreview.tsx`はLFO波形カーブ（Sine/Cos/Triangle/Saw/Square/Random/Perlinを実shape・low/high・phaseから描画）、Colorパレットのグラデーションストリップ（実stop位置・RGBから）、Move pathのXY描画を保存/runtimeと同一値で描く。Move頂点drag（`moveEffectDrag.ts`）は4px閾値・Y軸反転・ghost・live readout・Escape cancel・範囲clamp・commit契約を持つ。新規文字列とaria-labelはJA化し、`Perlin: パーリン`を含む。監督再実行ゲート（すべてgreen）: `check:fx-visual`、`check:move-effect-helpers`、focused `check:fx-visual-viewport` 5解像度（1280x720〜2048x1152、move esc rollback／commit／clamp／`scroll=zero`／`failed=[]`）、full viewport matrix 232 pass/0 fail、`tsc --noEmit`、`check:localization` 2593/2593(100%)、`check:identity-color`、`check:large-show-ui`（2000灯windowing）、`vite build`（main index 482.86kB）、`check:timeline-viewport`（T15回帰6 pass）。監督はCodex報告になかった2件を独自に発見・修正した: (1)`check-viewport-containment.mjs`のCDPクライアントがソケットclose/error時に保留Promiseをrejectせず、実行中にブラウザが死ぬと全T16断言を実行しないままexit 0で偽PASSし固定ポートをリークしていた欠陥を、close/error時の全pending reject＋`beforeExit`フェイルクローズで修正し、Chrome強制killでexit 1・0.9秒終了・リークゼロを実証した。(2)LfoShape `Perlin`のJA辞書欠落を追加しアサーション固定した。ネイティブ受入は別扱いとし、freshビルドのSyndocal WebView2へユーザー実ショー`Shinkan2026.dvc`（灯体41・キュー30・エフェクト変換6/近似44/シーンブロック42/ビーム特徴量221）をインポートし、9ファミリー全JA表示・identity色分けScene Matrix・FXバッジ付きセルと、スタックへ適用した実Perlin効果のグラフィカルカーブプレビュー（実波形・0–65535・φ0%・「パーリン」表示）を目視確認した。これらはブラウザ／ローカル／単一hostのnative内部証跡であり、Daslight 5との同一タスク操作手数比較・実会場運用証跡はまだ主張しない。

2026-07-20にT17 Scene Liveモディファイアを完了し、`qa/UI_REDESIGN_T15_T17_PLAN.md`の全トランシェ（T15-P/T15/T16/T17）を閉じた（Codexのusage limitに伴いユーザー承認のうえFableが直接実装し、同一の監督ゲートを自らへ適用）。speed・size・phase・flashの初期値は`CueSummary.live_modifiers`（`serde(default)`＋未設定skip、legacy `.sdc`バイト同一）として保存され、アクティブSceneへのラッチ式Live overrideはengineの`SetCueLiveModifier`/`ClearCueLiveModifier`がコマンド適用時にキュー所有activationのパラメータをauthored×係数で再構築する方式で、**44Hz hot pathへの値検索・allocation・分岐追加はゼロ**（release実測 p95=3.06ms/p99=3.66ms/max=4.28msで従来レンジ内）。リセット規則（release/再trigger/project loadでauthoredへ）はengineテストで固定し、latchは`build_persistence_snapshot`・`project_snapshot_for_save`・frontend比較スナップショットの三重で`.sdc`と汚れ判定から排除される。flashはCueの保存属性で、Scene MatrixセルとTouch LIVEサーフェスの配置Buttonが押下=Trigger/解放=Releaseのモーメンタリとして同一コマンド経路を叩き、speed/size/phaseダイヤルも両面が共有`CueLiveModifierStrip`（authored位置とLIVEラッチをDOM属性で区別、Reset付き）を使う。受入は新設focused `check:scene-live-viewport` 5解像度（authored読み出し→ラッチ→Reset→再triggerリセット→Matrix/Touch flashのdown/up、containment 0）、full viewport matrix 232 pass、localization 2603/2603、engine 374/374、syndocal 320 pass、T16回帰greenで固定した（既存負債1件=color effect検証テストの失敗はT16コミット時点でも再現することをstash A/Bで確認し別タスク化）。ネイティブWebView2でShinkan2026.dvc（灯体41・キュー30・シーンブロック42）をインポートし、実シーン「1.1 新宝島」をトリガーして共有ライブストリップ（速度x1/サイズ100%/位相0%、authored中立でリセット無効）を確認、速度スライダーを動かすとx4へラッチしLIVEバッジ点灯・リセット有効化、リセット押下でauthored（x1）へ復帰・LIVE消灯・リセット再無効化までを目視した。flashのモーメンタリ挙動はfocused `check:scene-live-viewport`（Matrix/Touch両面のdown/up）で固定済み。これらも内部証跡であり、実会場運用・Daslight操作手数比較はまだ主張しない。

2026-07-29に「Daslightを超える照明ソフトとしての完成を優先、人が操作する・見るに重きを置く」というユーザー指示を受け、内部ゲート駆動から**オペレーター体験駆動**の監査・修正サイクルへ移行した（正: `qa/OPERATOR_AUDIT_2026-07-29.md`）。現HEADのnative debug buildへShinkan2026.dvc（灯体41・キュー30）を実インポートし、実ウィンドウ操作＋キャプチャ＋3倍拡大＋コード裏取りで所見13件（H級3件）を確定。同日中にT24系全5トランシェを完走した: T24-A（23b50a5）が41灯密度で判読不能だったステージラベルを純関数レイアウト（12文字省略・可視25灯超デクラッタ・ズーム150%復帰・空間グリッド衝突回避、`check:stage-labels`新設）で解消し、T24-C（6b00c9f）が全37ファイルダイアログ呼び出しへ親ウィンドウを付与して最大化ウィンドウ背後へのダイアログ消失を封じ（.dvcインポートのビジー表示＋再入ガード込み。監査時の「数十秒無反応」の真因は隠れダイアログで、パースは1秒未満と実測訂正）、T24-D（aba8d89）がインポート内容の発見導線（MatrixセルSSバッジ・タイムライン空状態ヒント・FXワークスペースのCue所有件数表示）を追加し、T24-B（4f7bb22）がSetup>Mapping上面の約85%死空間をT8方式のその場拡張（ステージ373→941px・2.52倍、離脱0.5px復元、mappingExpansion 19断言、発振防止contain維持）で解消し、T24-E（e334c15）が小型摩擦4件（「ミキサー」→「VJデスク」用語衝突・フェーダーペイン読取ストレッチ・タップボタンクリップ・復旧メッセージ英語）を閉じた。各トランシェは監督（Fable）が全ゲート独立再実行＋フルviewportマトリクス232pass＋再ビルドexeへの復旧復元→ネイティブ実機目視で受入し、実装はCodex gpt-5.6-solが担った。ネイティブ目視はハーネスの盲点2件（SSバッジのellipsisクリップ、F9読取行の重なり）を検出し、前者は断言を実描画幅の非クリップ検証へ強化した。残る監査項目はF6（タイムライン展開レーン高）とF8（Matrix列密度）で、後者はDaslight側操作手数実測（未計測10タスク＋patch/programming/FX適用/保存再開）と合わせて次フェーズで扱う。これらは内部証跡であり、Daslight同一タスク操作手数比較・実会場運用証跡はまだ主張しない。

2026-07-29〜30にDaslightの実操作モデル（ユーザー解説＋実機並置キャプチャで確定した「セル本体=再生／右端色ストリップ=編集選択／右ペイン常駐シーン設定／EDIT=フェーダー値のシーン上書き・LIVE=非破壊」）へUIを直結させるT25/T26系を完走した。T26-A（7f4f321）がシーン設定右ペイン（Staticプロパティ+9ファミリーFX chooser+FXエディタ切替、セル右端identityストリップ=編集選択専用）を、T26-B（411505e）がControlフェーダーデッキのLIVE/EDIT直結書込（scoped update_cue_from_currentのジェスチャ単位1トランザクション・identity色EDITバッジ・Undo/Redo・未選択ガード）を、T25-E（036989c）がフレームレス一体化ウィンドウ（decorations:false・42pxトップバー=タイトルバー・40x40最小化/最大化/閉じる・8方向リサイズ・最大化クライアント1920x1009→1920x1032）を、T25-F（76e80b0）がトップバーPULSE（既存33msテレメトリの状態ゲート再利用・停止中ポーリングゼロ・250ms fail-closed・クリックで音声入力設定へ）を実装した。LIVE/EDIT/PULSEは英語不変語。全トランシェ着地後の新UI再監査（Shinkan実機通し、audit2系証跡）はH級ゼロ・M級1件（数値欄の生float表示）で、同欠陥は共有numberDisplayヘルパー＋41サーフェス棚卸し＋保存精度非破壊の実証で解消済み（1bdce3a）。この期間の監督実機受入は、ブラウザ断言が構造的に見えない欠陥3件（EDIT/LIVEトグル文字の行クリップ消失、setup-video小/大解像度のcontainment反転回帰、cue-recall期待文字列）をフルマトリクスexit code検証とWebView2 CDP直結検死（remote-debugging-port経由のDOM/入力実測）で捕捉し、それぞれ描画テキスト可視性断言・5解像度焦点必須化・厳密DOM数断言としてハーネスへ還元した。操作手数はシーントリガー=1クリック・ライブ速度=1ドラッグでDaslight実測と同数。

2026-07-30後半はユーザーの実機フィードバック駆動で15トランシェを1日で着地させた: 密度パリティ（ステージツール列・54pxアイコンフェーダー・カテゴリレール左統一・156pxバンク列で12バンク同時表示・デッキヘッダ1行化）、シーンセル解剖（フル名2行・ストリップclick=編集/drag=並べ替え/横断バンク移動）、T27-Aデザイントークン、T28ライブカラー（グリッドマスグリフ・Mega Bar 8セル個別色・セル別全幅ビーム・DMX出力ゼロでも動作）、ホイールピッカー最近傍スロット近似（.dvc COLORREF/BGR真因修正込み）、FX一本化（デッキ2タブ化・グローバルFXワークスペース撤去-5018行・Daslight式3面ペイン・main chunk 444kB）、Controlステージでの灯体移動/回転（Undo完全復元）、操作性バグ根治2件（30Hz再レンダーによるselect即閉・フェーダードラッグ中断→id-reconcile+キャプチャ保護）。全トランシェで監督のWebView2 CDP実機受入とフルマトリクス（フェーズ移管後の新基準217 pass）を通過。フルマトリクスは実装側焦点ゲートが構造的に見逃す回帰を5回検出した（persistent-band不変性/mapping-expansion/control-mode同名断言残存/setup-video反転/setup-dmx最小幅）。未計測の残り（パッチ・静的プログラミング・FXターゲット適用・保存再開）はDaslight側で実行を伴うためユーザー協働で行う。

2026-07-31に.dvcタイムライン変換のブロック単位忠実度を初めて全件実測した。原本XMLから独自抽出した正解値と実機エンジンのchild_timeline実データ（読み取り専用get_snapshot経由）を突き合わせ、Shinkan2026 Projects版のSuper Scene「新宝島」42ブロック×10フィールド（開始/長さ/対象キュー解決/フェード両端/速度/テンポ追従/loop_fill/拍位置/拍長）と、完全版（Shinkan-Left、Super Scene「Shin」「Unr」）229ブロック＋音声3クリップ・レーン→レイヤー構成・シーン別全長・クロックBPM 157.861が全件一致（不一致0）。この検証で唯一の忠実度欠落だったDaslight POSITION（シーン途中入り開始。完全版38/229・Projects版5/42ブロック）を`source_offset_ms`としてシーンブロックへ追加した: protocolはserde(default)追加のみで`.sdc` v1互換維持、engineはRuntimeCueStepClock::Timeline/ChildTimelineへのsaturating加算のみで44Hz経路を再構成せず、チェイサー半周期オフセットの位相実測（period 200ms/offset 100msでstep 0→1・level 65535→0・seek確立状態も一致）をテストで固定。インポーターは正値を変換（convertedレポートに+offset表示）、負値は0クランプ＋approximateレポート（Daslightの負値仕様が未検証のため意味論を発明しない）。Block Propertiesインスペクタに「ソースオフセット ms」欄を追加（blur即コミット）。監督検証: fmt 0/engine child_timeline 9/9/dvc 23/23/project_file 32/32/tsc 0/vite 448.48kB/JA訳2710(100%)/ハーネス断言削除ゼロ・34行追加/フルマトリクス217 pass・exit 0・fail行ゼロ/ネイティブ再ビルド受入で両版再インポート後のオフセット期待値込み全件diff不一致0、UI実機で取込値40表示→500編集のエンジンラウンドトリップを確認。直前トランシェとして中ボタンドラッグの2Dステージパン（全ツール共通）とWebView2既定右クリックメニューの全域抑止（編集可能要素は既定維持）もCDP実機受入済み。

同日、最後の未確認Daslight FXジェネレータ4種を実機Daslightで黒箱照合し（ユーザー許可の全自動操作。スクラッチ新規プロジェクトの保存差分でcurve全11波形のID対応表を確定、アンカー2点一致。既存プロジェクトへの保存なし）、変換を実装した。curve ID10=Strobe（周期あたり10本の等間隔2%パルス、Size=1で半振幅・Size=2でフルレンジ——グラフ実測とエンジン実測が一致: 1万点サンプリングで10パルス/high 20.07%、Fl-Strobe再現でDMX 255/0/255@250ms間隔）、color ID129=Plasma（全11パラメータ1対1照合、パレットはPARAM TYPE=4 ID=1のCOLORS正規化RGB——格納場所を初確定）、color ID130=Rainbow（COLOR FX系、Gradient=VAL×100）、chaser ID322=Chaser #2（実検体はDaslight上でも0ビーム空FX、ラベルのみ追加し正直スキップ維持）。PlasmaとRainbowの内部数式・掃引タイミングとStrobeパルス幅は未検証部分をapproximateレポートに必ず明示する。金標準: Documents版 変換6→7/スキップ1→0、完全版 変換22→27/スキップ8→3（残3件は原本自体が空選択のFXで、Daslight上でも無発光）。パレット上限は実検体10色Rainbowに合わせengine/.sdc検証/フロントエンドで2..16へ統一。監督検証: fmt/engine strobe 6/6・effect 55/55/dvc 27/27/project_file 32/32/tsc/448.48kB/JA訳2710(100%)/フルマトリクス217 pass exit 0/ネイティブ再ビルド受入で両版再インポート（7/0・27/3）とFl-StrobeのLfo shape=Strobe・period 2500ms・low 0/high 65535をエンジン実データで確認。これで両ショーの.dvcインポートに黙って欠落するFXはゼロになった（照合証跡はqa/DVC3_EFFECT_MAPPING.md「DVC-3c 完了」と target/qa/dvc-id-map/）。

続くユーザー指示でシーンセルの状態セマンティクスをDaslightパリティへ変更した: アイデンティティ色のストリップ塗りと枠は実行中セルのみ（アイドルは中立ストリップ＋色文字ラベルのみ、選択は1px中立アウトラインへ降格）。ストリップのclick=編集/drag=並べ替え/バンク間移動と実行中セル内のLIVE進捗・ライブ修飾は不変。identity-colorゲートとハーネス断言3familyを状態条件付きへ再交渉（削除ゼロ・同名断言の他フェーズ掃引済み）。この実機受入で既存のネイティブ限定欠陥を発見・修正: EngineSnapshotDeltaに`active_group_cue_ids`が欠けており、グループ所属キュー（インポートショーは全キュー該当）の実行中状態が30Hzデルタ経路で一度もフロントへ届いていなかった。ブラウザfixtureは状態直注入のため全ハーネスが構造的に見逃す盲点で、実機CDP受入が捕捉。デルタへのフィールド追加＋差分emit＋フロントマージで修正し、直列化含む焦点テスト2件で固定。受入実測: Barバンク「Amber」トリガでそのセルのみactive=true・枠rgb(215,86,159)=Daslightバンク色、他バンク同名7セルは中立維持、リリースで全消灯（拡大スクリーンショット: target/qa/ui-comparison/cell-exec-01-zoom.png）。診断中に一度「修正後も暗転」と見えたのはWebView2のHTMLキャッシュが旧ハッシュJSを掴んでいた再現性のない偽陰性で、計装ビルド（window直書き1行、コミット前に除去）で切り分けた。ゲート: tsc/vite 448.69kB/JA訳100%/identity-color/dvc snapshot_delta 2/2/フルマトリクス217 pass exit 0×2回。

2026-08-02にユーザー指示3件（大画面レスポンシブ#33・タッチ帯撤去#34・グループ第一級化#36）を連続で着地させた。#33（コミット4372407）: パッチアドレスグリッドのセルを24px上限固定からペイン連続導出（16〜44px、幅・高さ両制約、ResizeObserver+rAF）へ変更し、真因だった「プロファイル未ロード時に`.dmxPatchMap`がデスクauto行へ繰り上がり16px床で凍結・下に空1fr行」の行ずれを`grid-row: 2`明示で排除。空状態フェーズ（form=0/gridRow=2/ペイン÷デスク≥0.9/2048セル≥24px/末尾≤1px）をフルマトリクス常設ゲート化し基準218→223。実機2560×1392受入でセル16→35px・ペイン338→755px、導出入力実測が期待値一致。オクルージョン中ウィンドウのRO/rAF停止による古い計測値は自動化アーティファクトと切り分け（可視化で再配達されるため製品欠陥ではない）。#34（コミット25e6875）: タッチ内ヘッダー帯9操作を棚卸しし、トップバー重複（GO・両マスター）とタイル重複（BACK/PAUSE/BO群/All Clear）を帯ごと撤去、空きセルはFRONTサブマスターで8×12密度維持、保存済み面の旧タイルは描画前サニタイズ、`TouchSafetyDeck.tsx`削除で正味116行減。帯断言はゼロ存在＋5バインディング機能継承＋全高回収（≥0.99）へ移管し、タッチ面内トップバー重複禁止を追加。マトリクス223維持（タッチ15フェーズ全てband=0/duplicates=0/fill=1）、実機受入合格。#36（コミット538d64d）: グループを`{id,label,color}`のプロジェクトレベル実体へ昇格（.sdc v1はserde(default)追加のみ、レガシーは灯体グループ文字列から初出順導出＋既存group_colors継承、リネームはlabelのみでID不変・membership追従、削除Undoは順序・所属位置・色まで復元、engine不変）。グループ帯に「＋」新規（空グループ可・インライン名入力）、ダブルクリックリネーム（現在名プリフィル）、右クリックカスタムメニュー（Rename/Colorスウォッチ/Delete）、固定フレーム確認ダイアログ＋ステータス行Undo、パッチ後のグループ登録ダイアログを実装し、実体管理を帯へ集約（所属割当・ターゲット参照は監査の上維持）。cargoテスト4件追加（ラウンドトリップ/レガシー導出/リネーム保持/削除Undo、project_ 88/88・group 20/20）、fixture-groupsフェーズ12断言×5解像度をフルマトリクス常設化し基準223→228。実機WebView2受入で作成→リネーム→メニュー→削除確認→Undo復元の全サイクルを確認。3件ともCodex実装・Fable監督（誤cwd起動によるread-only空振り完走、rescueのジョブID誤報告、スリープ死ジョブの"running"残骸による起動ブロックという委任インフラ障害3種をこのサイクルで特定・解消し、対策をメモリへ固定）。

2026-08-03に操作手数計測協働（ユーザー承認のDaslightスクラッチ自動操作）とそこから派生した6トランシェを着地させた。Daslight側4項目を実測（パッチ4op/静的3op/FX適用1op/保存3+再開2op、証跡target/qa/ui-comparison/opcount-*.png）し、Syndocal側との突合で発見したギャップを即日修正: #38（24a33b5）カラーピッカー述語がGDTF標準名ColorAdd_*を受けず生CH行へ退化していた欠陥を共有colorCandidates拡張＋重複述語5箇所統一＋Touchスウォッチ8個＋隠しCSS副因修正で解消（色指定は計測不能→3op、実機でColorAdd_R実書込確認）。#39（d7c53c8）BLINDモード新設: EDIT+編集シーン選択時のみ有効な目トグルで、編集は既存プログラマー仮値へステージングされライブDMXはバイト同一、2Dステージのみ編集値＋BLINDウォーターマーク表示、確定/破棄付き、runtime-only。#40/#41（6bbfe3f）原点軸破線をグリッドパターンと同一stageOriginアンカーへ統一（100%で1.2-1.7px・3340%で40-56pxのずれ→全ズーム0.00px）、ツールレールをS/P/R/H/?の1列5個へ削減（レイヤートグルは上部帯へ集約、ホットキー27個維持）。#42（140dd5d）ユーザー指摘で発覚したBLIND確定の意味論欠陥を修正: commit_programmer_valuesがシーン文脈なしで常時active解除＋ライブ流し込みしており、Scene1再生中のScene2編集確定で出力が漏れ・activeが脱落していた。確定コマンドへ編集シーンIDを付与し、非アクティブは内容のみ更新（DMXバイト同一・active維持）、アクティブは一度適用＋実行状態保持へ。実機2シーン再走で全段確認、engineテスト4本＋ハーネス断言nonActiveCommitDmx/activeCuePreserved常設。#43（f64f2c1）Daslight並置比較指示3件: スウォッチ40px→20×20固定、EDIT/LIVE順序、Blind目アイコンのみ化。フルマトリクス基準は218→223（#33空状態）→228（#36グループ）→233（#39 BLIND）へ拡大し全コミットで維持。残件: FX適用のSyndocal側計測（新規プロジェクトでFX/シーン編集UIへの到達経路が未整備という発見込み）、物理DMXリグ受入（ユーザーの機材接続待ち: Art-Net/sACNノード＋実灯体＋USB-RS485）。

2026-08-03後半、物理DMXリグ協働と3トランシェを着地させた。**シリアルDMX物理受入が合格**（5331a58）: 汎用FT232R USB-RS485ケーブル（COM3）→4ch Dimmer/RGB灯体（アドレス1）をEnttec Open DMX互換経路で駆動し、ユーザー目視で白/減光の安定点灯を確認——Syndocal初の物理DMX証跡。実機がワーカーの実欠陥を暴いた: ループにワイヤレートペーシングが無く、各フレームのbreakがFTDIチップバッファ排出中の前フレームを切断（症状: 初回一瞬点灯→消灯、2msガードでは約2秒周期の瞬断）。ワイヤ時間22,764µs＋実測必要だった8msガード（約32fps）で安定化し、`open_dmx_frame_pacing_wait`純関数＋io テスト11/11で固定。qa/M4のOpen DMX行を物理合格へ更新（波形計測はロジアナ未所持で継続保留）。#44（d5f27c7）シーンFXブロック: シーン設定ペインにSTEPS/COLOR/CHASER/MOVE/VALUE FXのDaslight同構成ブロックを新設し、1クリックでcue所有FX（.dvcインポータと同一のcue.effect_targets表現）を追加。実機でeffect_targets 0→1を確認し**FX適用手数はDaslight同数の1操作**に（これで手数比較の全項目が計測完了: パッチ4vs5/Dimmer1vs2/色2vs3/FX1vs1/保存3vs3/再開2vs2）。初回パスが既存FXカラー/チェイサーエディタ検査10件を壊しフルマトリクスが捕捉（焦点ゲートは見逃し）、修正ラウンドで既存契約不変のまま解消。シーン設定断言5行が常設化され基準233→238。#45（87a156e）I/O簡素化（ユーザー指示「ごちゃごちゃしすぎ」）: 既定表示をDaslight式の要点のみ（DMX=ルート一覧＋クイック追加＋シリアル適用、MIDI=デバイス/接続、OSC=バインド/リッスン、リモート=サーバ起動）へ絞り、他は10個の折り畳みへ全機能格納（撤去ゼロ・新旧対応の全監査表付き）。ペイン内実測142/104/99/104→**28/6/4/9**、画面全体でも約半減。live-edit-typesファミリの参照1件の再交渉漏れをフルマトリクスが捕捉し修正ラウンドで解消、最終238 pass。物理残件: Art-Net/sACNノードの実機受入（ユーザーのノード接続待ち）、ロジアナ波形、MIDI実操作/TouchOSC/タブレットリモート。

2026-08-04、実ショー通し受入（ユーザー質問「タイムラインとシーンが完璧に動作すれば何の文句もない…抜かりないか？」への回答として実施）が**リリース阻害級の欠陥#48を発見し、即日修正した**（fd19059）。Shinkan2026.dvc（灯体41・キュー30・42ブロックの子タイムライン持ちSuper Scene）はインポート完璧だが、Super Sceneをマトリクス/GO/MIDI経路で直接トリガーすると30秒間U0オールゼロ——`rebuild_child_transports()`がメインタイムライン配置キューのみ子トランスポートを構築しており、実ショーの運用経路（マトリクス直接トリガー）が一度も動作していなかった。修正はTimeline/Direct二つのトランスポートIDドメインが既存build_child_transport機構（occurrence収集・pending cue・フェード・FX・ステップクロック・tempo-follow・source_offset_ms）を共有する形で、確立はコマンド経路（44Hz tick予算不変、transport_tick_reallocations=0）、解放はrelease_cue_with_value_restore経由、再トリガーは0から、メインシーク非干渉。engineテスト9本追加（419/419）、再交渉1件（128kシーンブロックのwall-clock 50→75ms、並列競合実測による）。受入はVALORANT/Vanguard稼働でCDPが構造的に塞がれたため、ファイル存在ゲート式ローカルゴールデン`dvc_local_golden_super_scene_direct_trigger_plays_child_timeline`へ切替: 実Shinkan2026.dvcをインポート→Super Scene直接トリガー→親キューactive維持＋子ブロック由来の相異なる非ゼロDMX状態2つ以上＋リリースでゼロ復元を確認（修正前は全ゼロ）。このゴールデンは実ショーの恒久回帰資産として残置。フロントエンドバンドルは238-passマトリクスビルドとバイト同一（engine限定変更）につきマトリクス結果を継承、ゲーム終了後に再確認。教訓もメモリへ固定: Vanguard稼働中はWebView2のリモートデバッグポートが開かず、フルマトリクス後半フェーズのタイミング失敗も誘発する。（→同日後半に両説とも誤帰属と判明し訂正。下記参照。）

2026-08-04後半、3トランシェ着地と検証基盤の根本欠陥2件の特定・修正を行った。#46+#47（0d3cb71、ユーザー指示）: 原点軸破線を画面固定（non-scaling-stroke・1px・4-4固定ダッシュ）にしてズームで太らないようにし、灯体選択/ホバー枠を0.55/0.8/1.1pxの画面固定・グロー無しへ減弱（当たり判定不変）。ビューポート帯はフィット2種のアイコン化＋ズーム「−/＋」化＋スナップ行統合で正味1行削減（26px×2行=56px固定）。#49（2da14d6）: 「プロファイルを使用」成功時にSetup>Patchへ自動遷移し、操作手数比較で唯一劣後だったパッチ4vs5を**4vs4**へ——計測全項目でDaslight同等以上を達成。物理系: 灯体を121chモード（ch1マスターディマー＋RGB×40セル）に変更したユーザー構成に合わせ、COM3実配線でPositionWave三重相（R/G/B位相1/3ずらし）のレインボーを40セルへ流す環境変数ゲート式デモテストを常設（eddb47a、実走60.2秒・シリアル送信成功2,641+・失敗0）。検証基盤の真因究明: 朝から続いた「フルマトリクスのみで再現する決定論的7件失敗」は、（誤）VALORANT負荷でも（誤）Windows更新でもなく、**新headless Chromeが起動時に自己再起動しspawnされたpid＝即死するランチャーである**ことが原因だった。ハーネスのtaskkillは実ブラウザに一度も届かず、リサイクル時に同一CDPポートで旧ブラウザが応答して黙ってステイル再接続→後半ファミリが旧状態を計測して同一値で失敗、毎ランChromeがリークしていた。修正はポートLISTENING実保持pidの列挙kill＋ポート接続拒否までの待機（10秒超はfail-loud）。加えてCodex新規計測断言がmeasureテンプレートリテラル内で`\s`→`s`に潰れる文書化済みの罠を踏んでいた件も`\\s`で修正。最終フルマトリクスは**238/238・リークゼロ**。もう一件の確定: WebView2ランタイム151（08-01導入、08-04朝の再起動で初実効）は`--remote-debugging-port`を無視するため、CDP経由のネイティブDOM検死は使用不能になった。以後のネイティブ受入はゲート式cargoテスト（エンジン実値）＋ウィンドウキャプチャ実効果判定（UI目視）の二経路を正とする。

2026-08-04夕、ユーザー指示2件と監査検出1件を連続着地させた。#51（506340c、ユーザー指示「4分割せず」）: Setup I/OのDMX/MIDI/OSC/Remoteサブタブを廃し、全幅DMX Connections＋MIDI/OSC/Remote3列カードの単一サーフェスへ。#45の10ディスクロージャとゾーン別lean予算（28/6/4/9）は完全維持・機能撤去ゼロ、旧保存タブ値は安全に正規化。ハーネスは20フェーズ→5フェーズの統合契約（サブタブ不在・4ゾーン同時可視・レガシー正規化検査）でフルマトリクス基準238→**223**。#53（3fa5abc、ユーザー指示「どうやって灯体のデータを引っ張ってくるのかわからん」）: PATCH左ペインを常設プロファイルブラウザ化（検索＋検証済み標準機4種の27px行＋キャッシュGDTF＋最近使用＋既存インポート導線、内部スクロール）。選択は既存のプロファイル使用コマンド経路を再利用し、未武装時はJA/EN明示ガイダンス。Library経由は#49の自動遷移維持・PATCH内選択はPATCH残留。空状態ガイダンスはcheck:empty-states管轄へ。#52（3d84f56、監査検出）: スクリーンショット監査（自前headless Chromeツアー——WebView2 151のCDP遮断下での新しいUI目視経路として確立）が発見した「投影サーフェス/ステージオブジェクトラベルとT28セグメント帯がズームでスケールする」不整合を、灯体ラベルと同じworldPerCssPixel 11px経路へ統合して解消（実測38.07px→11.00px固定、セグメント可視グリフ1/zoom補正、当たり判定はステージ空間のまま）。3コミットともtsc/vite/ローカライズ（2761/2761）/用語/フルマトリクス223をそれぞれ独立通過。操作手数比較は#49で全項目Daslight同等以上、121chリグ物理受入はレインボーデモのオペレーター目視で確定済み。ネイティブ目視（新debugビルド、キャプチャ実効果経路）で#51統合I/O・#53パッチブラウザ（空状態ガイダンス→選択→フォーム武装→最近使用記録）・#46の305%ズーム破線・#47の1行帯を実機確認。

同日夜、#54（7dabfa0、ユーザー指示「まだ冗長。Daslightは左で完結。ドラッグドロップでパッチできない」）: パッチを左1列完結へ再構成——プロファイルブラウザ直下にユニバース/アドレス（次の空きインライン）/数＋PATCH、配置オプションは既定閉の折り畳みへ降格、グリッド上部のフォーム帯を撤去（グリッドが中央全面化）、右ペインは選択灯体編集専用化（新規パッチ用途の「プロファイルをパッチへ使用」重複を削除）。プロファイル行はHTML5ドラッグでグリッドへ直接パッチでき、ホバー中アドレスにフットプリントプレビュー、占有衝突・ユニバース超過はドロップ拒否＋ステータス表示、クリック→PATCHのキーボード経路は維持。ハーネスは左列契約へ再交渉＋setup-patch-dnd 5フェーズ新設（占有・衝突拒否のfail-closed化）で基準223→**228**。tsc/vite/ローカライズ2760/用語/空状態/フルマトリクス228すべてgreen。

深夜、パッチのメーカーカタログ系3件を連続着地。#55（ee68dff、ユーザー指示「各種メーカーの灯体プロファイルが出ていないが？」）: パッチ左ブラウザの検索をローカル＋GDTF Shareオンライン横断化（2文字/450msデバウンス、既存search/cache/importコマンドのみ、資格情報はLIBRARYと単一ソース共有・セッションメモリのみ非永続）。使用でDL→キャッシュ→武装→D&D可、未ログイン/オフラインは静かな案内行。setup-patch-gdtf-share 5フェーズ（モックinvoke）で基準228→**233**。#56（4a7b6f3）: ライブ利用で発覚した「全エラーをオフライン表示に丸める」欠陥を判別型errorステートへ修正——認証失敗はセクション内赤表示＋資格情報再表示、一般エラーは理由＋再試行、offlineは接続系のみ（run_curlのエラー形状をREAD-ONLY列挙して分類、Rust不変）。ユーザー協働の切り分けでAPI自体は健全（不正資格情報へ正しいJSONエラー応答）・ユーザーのcurl直叩きでresult:trueを確認し、真相は**GDTF ShareのAPIはメールアドレス不可・ユーザー名のみ**というサイトログインとの仕様差だった（アプリ欠陥ではなくUX欠陥）。#57（95a2966、ユーザー指示「モードごとに別になってる。Daslightは入れ子」）: キャッシュ/Share共通の3階層ツリー（メーカー→灯体→モード、GDTFにシリーズ層は存在しないため2階層目まで）へ。既定はメーカー折り畳み、検索は全階層横断＋一致枝自動展開、単一モード灯体は灯体行が直接操作可、tree ARIA＋キーボード開閉、27px密度、D&D/ダウンロード状態契約は維持。3件ともフルマトリクス233green。ローカライズは2786/2786へ。

2026-08-05未明、初期ライブラリ体験の改善（ユーザー指示「ここをマジでなんとかしたい」——Daslightの同梱1.5万件との差の解消）を3段計画で開始し、段1と段3を着地。段1（6e42a4e）: 検証済みジェネリックパックを4→**60種・6カテゴリ**へ拡張（PAR/ウォッシュ20・ムービング12・LEDバー/ピクセル13・ディマー5・ストロボ3・エフェクト7）。実勢の廉価灯体チャンネル構成を網羅し、オペレーター実機の121chリグと同型のMaster+40x RGBも収録。全プロファイルが正規属性（ColorAdd_*/Pan/Tilt/Shutter1等）でカラーピッカー・ポジションパッドに接続し、説明文へ実チャンネル順を格納。#57ツリーにカテゴリ階層で編入、数字検索はフットプリント完全一致。プロジェクトには使用した定義のみ埋め込み。段3（a99f4ee）: Shareツリーのメーカーヘッダーへ「全部キャッシュ」——メーカー全カタログを再列挙し既存キャッシュ経路で1件ずつ直列取得、既存有効キャッシュはスキップ、個別失敗は計数して続行、停止は実行中1件の完了後に停止し完成分は残存（既存の.partial→検証→rename流儀が中断一貫性を保証、再実行はスキップ再開）。検索API200件上限で全量列挙できない場合は部分キャッシュせずfail-closed。キャッシュ見出しに件数＋MB表示。両段ともフルマトリクス233green、ローカライズ2799/2799。段2（OFL同梱: MITライセンス確認済み・1,000灯体超）は配布サイズの設計判断（焼き込み vs 初回一括取得）をユーザーへ提示中。

2026-08-05午前、Codexが利用上限に達したためユーザー指示で実装レーンをFable直接実装へ一時切替し、指示4件を連続着地。I/O 2:2化（6bad7ab）: 統合I/Oの1:3配置を四象限等分グリッドへ（CSSのみ、ラッパーはdisplay:contentsでゾーン契約不変、1920/1280実測で四象限均等・外側スクロール0）。#62（dd9d9f4）: 実アプリでD&Dパッチが不能だった真因はTauriのネイティブドラッグ処理がWindows WebView2のHTML5 DnDを横取りする既知問題——`dragDropEnabled: false`で解放（Tauriファイルドロップ機能は未使用のため副作用なし）。あわせてパッチ実行フォームを左ペインの固定フッター化（スクロールはブラウザリストのみ）、ユニバース/アドレス/数を1行75px×3のコンパクト行へ（実測: フォーム総高132px・フォーム自身のスクロール0）。#63（04a7367）: 照明サブタブをDaslight同型の**パッチ1本**へ統合——ライブラリ/プロファイルタブ廃止（旧保存値はpatchへ正規化）、武装プロファイルのサマリ（DMXマップ/チャンネル機能/ジオメトリ）は灯体未選択時の右ペインへ、自作プロファイル工房はPatch Sourceヘッダーの「自作プロファイル」ボタン発のワークベンチ面へ、カタログ修復は「選択灯体のプロファイル源破損→武装プロファイルへ再リンク」のインライン警告行として存続（get_fixture_profile_health経由）。ハーネスはsetup-library/profiles 10フェーズ撤去で基準233→**223**、#55資格情報共有検査はLibrary往復の再マウント依存を排して明示Refreshへ再交渉。全コミットでtsc/vite/ローカライズ（2804/2804）/用語/空状態/焦点パッチ族21/21/フルマトリクスgreen。

同日、初期ライブラリ段2（8b331da、ユーザー方針決定「インストーラに焼き込む」）で**Open Fixture Library（MIT）を同梱**した。`scripts/build-ofl-library.mjs`がOFLスナップショットをSyndocal形式へ変換し（**620灯体・2,084モード・324kB**）、パッチブラウザの検証済みパックとキャッシュの間に「同梱メーカーライブラリ」セクションを新設。**アカウント・ダウンロード・ネットワーク不要で初回起動時からメーカープロファイルが使える**状態になり、#57階層ツリー・#54ドラッグ&ドロップ・フットプリント検索も全て同一経路で機能する。ペイロードはブラウザ初回描画時に動的import（独立331kBチャンク）、パッチ系パネルも新規patch-panelsチャンクへ移し、主チャンクは501kB→446.9kBへ戻して警告閾値以下を維持。変換はSyndocal自身のカスタムプロファイルと同じ非可逆方式（各モードを順序付きDMX属性レイアウトへ、coarse+fine隣接ペアは16bit統合、未マップchは番号付きスロットとして保持しフットプリント厳密一致、ピクセルテンプレート系マトリクスモードは対象外）。MIT帰属はTHIRD_PARTY_NOTICES.md・licenses/open-fixture-library-MIT.txt・セクション直下のインライン表示の3箇所。あわせて#64（ユーザー質問「配置オプションって必要か？」）: 機能自体はDaslightに無い優位点（線/グリッド/円配置・ステージXYZ実寸・向き仕込み）のため保持しつつ、プロファイル未武装時は丸ごと非表示にして空プロジェクトでの無意味な「占有数0ch/終了アドレス1」表示を解消、要約は数≥2のときだけ複数パッチを示唆する。ハーネスは同梱セクション検査（ツリー存在・メーカー100社以上が既定折り畳み・MIT帰属可視・展開前は行ゼロ）を追加し、セクション順と配置ディスクロージャの断言を再交渉、基準223維持。

2026-08-06、メーカーライブラリを第二段へ拡張した。OFL変換は従来除外していたmatrix insertをrepeat規則とchannel orderから完全展開し、**620灯体・2,084モードから627灯体・2,798モードへ増加**、うち714 matrixモードを復元した。補完ソースとしてQLC+ fixture definitions（Apache-2.0）を追加し、OFLを高優先度として正規化manufacturer+modelが衝突する145灯体をQLC+側から除外、同一ソース内の完全重複2モードも除去した。最終同梱はOFL 627灯体・2,798モード、QLC+ 1,590灯体・4,561モード、合計**2,217灯体・7,359モード／213メーカー群**。双方を固定source revisionから再生成でき、専用`check:profile-library`が入力／出力件数、重複なし、1–512ch、連続offset、16bit幅、audit整合、matrix展開数を検証する。Patch UIは既存ツリー、ドラッグ&ドロップ、武装／作成経路を共用し、各灯体行へOFL／QLC+由来、帰属欄へlicense・revision・件数を表示する。JSONは別々のlazy chunk（OFL 787.99kB、QLC+ 1,150.66kB）で、主indexは446.90kBを維持した。変換後表現は引き続き順序付きDMX属性layoutであり、未解決channelもgeneric slotとしてfootprintを保つ一方、capability range、wheel media、physical geometryを完全保持するものではない。このため利用可能数とmatrix coverageは前進したが、Daslightの公式カタログ件数との同等以上やプロファイル意味論の完全同等はまだ主張しない。

UI localeはProjectメニューからEnglish／Japaneseを即時切替でき、`syndocal.uiLocale.v1`として端末へ保存する。`.sdc`やworkspace layoutとは分離し、保存拒否時もsession内の選択を維持する。翻訳境界は静的text、動的status text、title／aria-label／placeholderを扱い、初回またはlocale切替時だけ全treeを処理し、通常更新はMutationObserverが変更nodeだけをmicrotask単位で処理するためtelemetry更新ごとの全DOM走査を行わない。英語sourceは保持され、同一sessionで英語へ可逆復帰できる。Project／Setup／Control／Touch／照明／映像／Mapping／I/Oの操作語と説明、Project／Template／Updater状態を日本語化し、storage fallback、動的pattern、英語fallbackをhelper test、`lang=ja`／日本語Project menu／1366x768 containmentを実browser testで固定した。TSX ASTで静的textとtitle／ARIA／placeholderを数えるcoverage gateは、単位・規格名・座標略号・入力例の意図的共通表記を含め2219/2219（100%）を要求する。灯体／キュー／映像／出力／パレット／デバイス名等のユーザーデータは`data-no-localize`境界へ分離し、既知の静的preset以外に裸のuser labelが残ると検査を失敗させる。実browserでは`Video`／`Save`／`Output`という衝突しやすい灯体名が日本語localeでも変化しないことを確認する。新しい静的UI文字列を日本語化または明示的な共通表記へ分類しない限りCI検査を通らない。外部backendが返す未知の詳細errorは誤訳せず英語fallbackを維持する。

音声解析はwaveform／beat／BPMに加え、1024 sample Hann windowとin-place radix-2 FFTで時間別Bass（20-250Hz）／Mid（250Hz-2kHz）／High（2-10kHz）を生成し、0-1へ正規化してProjectFileへ保存するようにした。旧projectでは`audio.spectrum`を空配列として読む。Timeline Audioに最大128点へ間引いた3 band表示を追加し、100Hz／1kHz／5kHz toneのband分離、有限値、範囲を自動テスト済み。local video音声の再生／device選択、play/pause/speed追従、75ms drift補正、loop終端再起動、同期診断はVJ Clip Gridへ実装済み。通常運用での長時間driftは抑制するが、映像decoderとaudio deviceを単一のsample clockで駆動するsample-accurate同期ではない。

2026-07-15に混合Color/Chaser/Move性能証跡を追加した。独立Color／Chaser／Move runtimeは実装済みで個別の200灯×64エフェクト回帰はあったが、3エンジン混合スタックのrelease p95/p99ゲートと混合ワークロードでの1時間ソークが未取得だった。新規`mixed_color_chaser_move_release_stack_meets_44hz_budget`は、本番`apply_effects` tick経路（fixtureごと・control（Dimmer／RGB／Pan／Tilt）ごと、per-call clock snapshotを含む）で200灯×64エフェクト（Color 22／Chaser 21／Move 21、phase分散、Colorは3-stop HSV-shortest全spread、Chaserは4-step Wings／width／duty／overlap／spread、Moveは4点smooth closed path）を1000 tick計測する。このhostのrelease 2回でp95 2.82–3.21ms／p99 3.06–3.78ms／max 3.89–4.35ms（22.7ms 44Hz tickの約14%）を観測した。計画時の推定予算2/4/8msは計測前設定でp95超過だったため、ベンチが本番hot pathを忠実に再現し人工overheadを加えていないことを確認した上で、release専用ゲートをp95≤5ms／p99≤8ms／max≤12msに固定し根拠をテスト内へ明記した。ソークハーネスへ`--mixed-lighting`（`qa/run-soak.ps1`は`-MixedLighting`）を追加し、既存のPULSE／WAVE＋4s timeline loop＋live FFT command pressure＋320x180@30fps videoへ、fixture 1対象の独立Color（3-stop Cycle／HSV-shortest／2s）・Chaser（4-step gap／Dimmer feature／125ms）・Move（4点smooth closed／Pan・Tilt対／2s）各1をpublished-ack経由で追加する。1時間release実行はPASS（108,001/108,001非黒フレーム、drop 0、jitter p99 512us、queue p99 48us、command-to-DMX p99 53us、Art-Net 216,004送信0失敗、peak WS 20.9MB）。既定ソークの動作・既定レポートパス・全ゲート値は不変で、既定モード60秒も無回帰確認した。これはこのWindows 1台のソフトウェア内計測であり、実灯体のColor／Chaser／Move方向・timing、実Art-Net nodeでの128 universe持続ソーク、同一hostでのDaslight計測は外部残件のまま変わらない。証跡は`qa/M5_RELIABILITY_VALIDATION.md`を正とする。

Projector出力へ左右上下それぞれのEdge Blend幅、blend gamma、Black Levelを追加した。設定は`.sdc`／mapping presetへ保存され、旧fileはblend 0・gamma 2.2・black 0で互換読込する。3-8点の任意polygon output mask、反転、0-0.5 UVのedge featherに加え、PNG／JPEGから輝度を16x16・4bitへ量子化してproject内へ埋め込むbitmap luma maskを追加した。外部file pathへ依存せず`.sdc`／mapping presetで共有でき、polygonとbitmapは乗算合成後に共通invertを適用する。Projection Mapping UIからImport／Clearと埋込状態を操作できる。CPU preview／test patternとnative GPU compute出力は同じbilinear bitmap sampling、polygon、feather、edge blend gamma、black liftを適用する。GPU/CPU parity、実PNG resize／量子化、bitmap補間／反転、polygon内外／反転／feather、値域validation、preset／旧file互換を自動テスト済み。固定16x16は会場輪郭とsoft maskのportableな実装であり、pixel単位の高精細maskが必要な案件は高解像度GPU texture maskを将来拡張として扱う。

Node Graph SourceへAudio FFTを追加し、Timeline AnalysisまたはLive Input、Bass/Mid/High、gain、biasを選んで照明属性と映像parameterへ接続できるようにした。Timeline spectrum point間は線形補間し、解析dataなしは0、gain/bias後は0-1へclampする。Live InputはStart時に確保する4本×2048 frameのfree／ready固定slot poolへmono downmixし、通常data callbackではheap allocation、mutex、FFTを行わない。異常に大きいcallbackも末尾8192 frameへ処理量を制限し、slot枯渇時は待機せず最古ready slotを置換して最新入力を優先する。I8／I16／I24／I32／I64／U8／U16／U32／U64／F32／F64の全CPAL PCM sample formatを同じ固定poolへ正規化し、callback回数、frame current／min／max、drop chunk／frame、queue current／high-water／capacityをatomic計測する。専用workerは構築時に確保したfeature ring、Hann window、FFT scratchとhistoryを再利用して最大約30Hzで解析し、CPAL callback／capture時刻差とlocal worker到着時刻から現在／最大capture-to-worker推定値を公開する。分割slotはframe offsetで補正し、250ms以上古いslotと非有限PCMを解析へ渡さない。RMS silence gateと旧Node Graph互換の3-band値を備え、44Hz engine threadでもFFTを実行しない。

WindowsのLive InputはCPAL既定hostのWASAPI shared modeで、system defaultまたは列挙device、sample rate、requested fixed buffer、全channel平均／単一channel／stereo pairのdownmixを明示選択できる。device catalogは更新世代ごとのopaque IDと重複名を区別するlabelを返し、古い／未知IDはsystem defaultや同名deviceへ黙ってfallbackせず拒否する。per-device capabilityからdefault configと対応channel／sample-rate／sample-format／buffer rangeを取得し、非対応構成、8192 frame超または200ms超のfixed bufferをStart前に拒否する。64pxのfull-window railは停止中にdevice／rate／requested buffer／mix、live中にoverrun／capture-to-worker／選択I/Oと実callback frame／queueを表示する。B/M/Hは安全状態だけを返す軽量commandで約30Hz、詳細statusは約1Hz、device Refreshと各pollはsingle-flightとし、古いcapability応答を無効化する。1280x720と1920x1080のstopped／worst-case live telemetry viewport gateは、rail重複なし、3 meter、4 telemetry badge、実Clip Grid scrollport内のpad可視性とcritical overflowなしを通過した。Windows nativeでは最大化したcurrent-source buildでsystem default WASAPI shared入力を48kHz monoでStart／Stopし、callback current／min／max 480／480／480 frames、capture-to-workerの採取時点current 0.1ms／max 10.1ms、OVR 0／0、queue current／high-water／capacity 0／1／4を確認した。この1構成を長時間、ASIO、全hardwareの性能証明には読み替えない。

Device Refresh後の明示選択はbackend／device名が新旧catalogueの双方で一意な場合だけ新しいopaque IDへ引き継ぐ。曖昧・消失時はsystem defaultへ黙って切り替えず、stale選択を表示し、capabilityを破棄してoperatorの再選択までStartをロックする。

専用live-audio viewport gateは停止中DOMの直接書換えを廃止し、post-mount Tauri invoke mockでproduction Solid componentを駆動する。1920x1080／1920x1032／2048x1152／1366x768／1280x720の英日10ケースでRefresh、generation ID一意再対応、同名曖昧fail-closed、明示再選択、capability、exact Start request、42／58／76% meter、live telemetry、Stop、safety clear pendingを通過した。

選択sample rateごとにbackendがchannel数／sample format／buffer capabilityの単一`resolved_config`を返し、UIはそのchannel／formatをStartへ返送してbackendが同じ構成を再解決・厳密検証する。これはStart前の構成確定であり、requested fixed bufferと実callback frameの表示を分離する。WASAPIを含む汎用post-open OS／driver applied-config照会はまだ持たず、ASIOだけはbridgeがactual fixed bufferを返す。

非既定Windows featureとしてASIO入力を実装した。Rodio／CPAL 0.16の既定WASAPI graphへCPAL 0.18.1を混在させず、`tools/asio-bridge`を独立lock／独立artifactとしてbuildし、appはABI v1 bridge DLLを動的loadする。UIはWASAPI Shared／ASIOを明示切替し、ASIOではsystem defaultを作らず、更新世代に結び付くdriver、rate、channels、native sample format、fixed bufferをStart前とbridge内で再検証する。driver消失、reset／resync／rate change、XRUN、250ms callback gapはterminal faultとなり、別ASIO driverやWASAPIへ黙ってfallbackしない。post-openでactual buffer framesとbackend XRUNをstatus／64px railへ返す。通常MIT buildとinstallerはASIOを含まず、配布には別途license決定が必要である。

2026-07-14の短時間実機smokeでは、`TOPPING Pro USB Audio Device`を明示指定し、48kHz／2ch／i32／requested・applied 128 framesでStartした。7 callbacks／896 frames、max capture delay 4166.7us、XRUN 0、nonfinite 0、terminal event 0でStopも成功した。`Realtek ASIO`はhardware input/output unavailableとして失敗し、別driver／WASAPIへfallbackしなかった。これはbridge／app loaderと1台の明示構成を確認した証拠であり、第二の正常driver、1時間soak、callback percentile、capture-to-engine／pixel latency、native UIの実入力運用、TouchDesigner同条件A/Bは未受入である。

同じTOPPINGの明示ID `asio:TOPPING Pro USB Audio Device`、48kHz／2ch／i32／128 framesでbridge ignored hardware testを`--locked --offline`実行し、100-cycleを27.6767468秒で通過した。actual bufferは100回すべて128、callbackは合計200かつ各cycle 2以上、Stop 100／Free 100、warning 0、terminal event 0、XRUN event 0／API count 0、nonfinite 0、frame mismatch 0、別driver／WASAPI fallback 0だった。これにより100回Start／Stop gateは完了したが、1時間連続運転、別vendor、hot-plug／fault injection、物理latencyと配布判断は別gateのままである。

同じcurrent sourceから通常production featureと`asio`を有効にした識別可能なnative QA buildを作り、小窓ではなく最大化とF11を主条件にしてVJ Deskを操作した。最大化window captureはWindows chrome込み1913x1080、F11後はorigin 0,0の正確な1920x1080でtitle bar／taskbarなしとなった。UIからASIO、`TOPPING Pro USB Audio Device`、48.0kHz、128 frames、Average All→monoを明示選択してStartし、`ACTIVE`、OVR 0/0、XRUN 0を確認した。Stopで`READY`へ戻り、Escで最大化windowへ復帰した。これでfull native UIのASIO操作経路は通過したが、短時間1台のsmokeであり、1時間soak、第二vendor、物理latency、TouchDesigner同条件比較の代替にはしない。今後のUI主受入はF11 1920x1080と最大化を用い、1280x720／1366x768はcontainment fallbackに限定する。

stream error／engine publish failure／250ms無入力は`stale`へfail-closedし、meterとsmoothingをzero、ephemeral spectrumを`None`へclearする。Engine自身も最後のLive spectrumを250ms TTLで失効させる。worker panicはthread境界で捕捉し、status pollも予期しない終了を検出してpending clearを再試行する。fault後のqueued chunkは再publishできず、queue full時もclearを再試行する。Stop時にclearが未受理ならruntimeとretry ownerを保持し、受理後だけstreamを破棄するため、旧runtimeのclearが新streamを消す世代交差を防ぐ。UIは未受理を赤の`SAFETY CLEAR PENDING`、engine queue受理後をamberの`SAFETY CLEAR ACCEPTED`／`Live stale · clear accepted`と表示し、実出力適用ackなしにzero完了を断言しない。停止／project load時もspectrumをclearし、`.sdc`にはdevice状態を保存しない。Timeline補間DMX 128、Live Mid DMX 191、Some→NoneとEngine TTLでDMX zero、249/250ms watchdog、fault latch／re-arm、Stop中QueueFull retry、callback downmix／処理量上限／固定slot再利用／最古slot置換／timestamp offset補正／非有限値回復、FFT scratch再利用、silence/tone分離、legacy roundtripを自動テスト済み。

2026-07-14にlive workerを16対数band、RMS、Peak、spectral flux、適応onset、onset strength、60–200 BPM／confidence／beat phaseへ拡張した。48kHz既定は2048 FFT、UI／engineへのpublishは最大約30Hzで、FFT ring／window／historyは構築時確保後に再利用する。旧Node Graph向けBass／Mid／Highも同じfeature frameから派生し、16 bandとrhythm telemetryは64px rail内で表示する。決定論的Auto VJ directorはordered candidate、seed、show revision、1–256拍／onset境界、0–60秒transition、avoid-repeat、action logを持ち、CLOCKまたはLIVE INPUTを明示選択する。Live inputでは実onset sequenceをexact-onceで数え、手動Takeは同tickでも優先してHoldをラッチし、blackoutを解除しない。入力clear／250ms TTL、候補消失、snapshot公開失敗はHold／Fault／rollbackへ倒す。Projectにはconfigだけを保存し、load後runtimeは必ずOffである。control commandは対象状態を含むEngineSnapshot公開後にackする。Auto VJのProgram Audio切替はfrontend監視ではなくbackend coordinatorが一度だけ処理し、手動Takeも同じ経路へ統合した。25ms active／250ms idleのwakeable workerは映像・音声summaryだけをcloneし、device open、decode、sink変更をTakeの応答経路から分離する。世代tokenで古いhandoffを無効化し、OFFのStopAll、active音声のgain／device再構成、Launch／Deck直接操作との所有権調停、project load成功時の停止と失敗時の回復を行う。audio 19件、protocol 19件、engine 275件、Tauri live-input 30件、Program Audio 10件、frontend build／storage／localization／英日5解像度のAuto VJ・live-audio gateが通過し、最終static reviewの残存P0/P1は0件だった。最大化した1920x1032作業領域のcurrent-source native VJ Deskでもcommand errorなしのReady、system-default micのStart／Stop、停止後clearを確認した。

同日にlive feature frameへspectral centroid、80ms／800msのfast／slow density、kick／snareのstrengthと独立refractory pulseを追加した。VJ Deskの54px REACTIVE stripからcompiled Audio Reactive Rackを一操作で開き、Live Inputの16 band、RMS、Peak、flux、onset／strength、BPM／confidence／phase、centroid、両density、kick／snare event／strengthをgain／bias／gate、attack／release／hold、4 curve、invertで整形し、照明属性または映像parameterへ接続できる。Audio nodeは44Hz tickごとに一度だけ評価し、load／upsert時に逆向き経路と対象IDをcompileするため、各targetでnode／edgeを再帰走査しない。Rack monitorは保存済みAudio graphを選択し、node IDが一致するengine runtimeのIN／OUT／safety状態だけを表示する。VJ stripもenabled数をLIVEと誤表示せず、READY／SAFE ZERO／HELD／LIVEをruntimeから区別する。Speed／BPM-sync映像targetは表示値だけでなく実playheadを駆動し、authored transport値を保持したまま入力断時は作者設定速度へ即復帰する。未検証、clear、250ms TTL切れはreleaseを待たずsafe zeroへ戻り、authoredな灯体／映像baseを復元する。onset／kick／snareは全graphへ同tick一回だけ配信する。Project save、checkpoint／history、Cue captureはon-demand authored-video snapshotを使い、変調値の焼込み／reload後二重適用を防ぐ。persistence snapshotはrendered videoを一度だけ構築し、authored側はraw layerと既存non-layer metadataから組み立てて二度目のeffect評価を避ける。graph作成／enable／removeはsnapshot publication barrier後にackし、失敗時はgraphとCue参照を一体rollbackする。64 mapping×各200 lighting targetのrelease benchmarkは1000 sampleでp99 196us／max 502usとなり、rack単体のp99 1ms／max 2ms gateを通過した。audio 22件、protocol 19件、engine 284件（manual benchmark 1 ignored）、既定Tauri suite 263 passed／4 hardware-manual ignored、frontend build／project-storage／2298/2298 localization、1920x1080／1366x768 Audio Rack containment、英日5解像度のAuto VJ／live-audio gateが通過した。別identifierのcurrent-source native QA buildを1920x1032へ最大化し、既存Syndocal／Daslightを閉じずにPreview／Programを保持したREACTIVE strip、Create Mapping、Live Input、`Snare strength`選択、停止中`SAFE ZERO`を確認した。P1 telemetry修正後も同じQA targetを再buildして最大化し、空rackが`0/0 READY`、editorが`No saved mapping`／`SAVE TO MONITOR`を示して仮計算をengine出力として表示しないことを確認した。

未達はASIO配布ライセンス／成果物方針、第二の正常ASIO driver、ASIO／WASAPI一時間soak、同一device hot-plug自動復帰、WASAPIを含む汎用post-open applied-config、live spectrum／clearのengine出力適用ack、capture-to-analysis／engine／pixel latency percentile、frozen dataset精度、大規模showのsave／checkpoint／warm-standby中tick-jitter、同一hardware／contentでのTouchDesigner五試行比較である。専用Audio Rack、ASIO短時間smoke、100-cycleとfull native UI操作の合格はnamed descriptor coverageとproduct-level fail-closed contract、および1台での明示driver反復open／closeとoperator pathを示すが、TouchDesigner全般との同等以上、機能優位、end-to-end性能優位の根拠にはしない。受入境界は`qa/AUDIO_REACTIVE_VJ_ACCEPTANCE.md`と`qa/ASIO_INPUT_ACCEPTANCE.md`を正とする。

Web Remoteは信頼済みLAN用、Active / Standbyは専用共有storage用の境界である。不特定LANやインターネットへ直接公開せず、Standby昇格前には旧PrimaryのDMX／video送信経路を必ずfenceする。

3Dビジュアライザは当面本体へ戻さず、既存の`visualizer`データ境界から外部実装へ接続する。標準操作面は2D Stage Mapを維持する。

2026-08-10（Fable引き継ぎ自走サイクル、DVC-V3/V4＋実保存標本シリーズ）: FABLE_HANDOFF_2026-08-09の未解決証拠リストを全消化した。DVC-V3（Codex実装・6e8c59e）はDaslight VALUE FXファクトリ全域621-628を静的証明し、ID623 Plasmaを厳密import、622/624/626-628は評価器不一致（radial sawtooth／離散integer profile／保持particle／qrand非復元遷移／度数量子化sine-gradient）を証明した上でfail-closed、Transform=1は1×Nでも非恒等（floor(N/2)2半区画ミラー折返し）と構造証明してID625のfail-closedを恒久根拠化した。ユーザーの権限昇格を受けFableがDaslight実機で使い捨てscratchプロジェクトに標本を自作: 実保存XMLがID625/ID623のCodex証明スキーマ・コンストラクタ既定値とバイト一致し、feature束縛（PRESET SSLPRESET=4）済みのstrongpoint対象シーンはrepo可搬golden `dvc_local_golden_value_sweep_and_plasma_import_from_saved_specimen` が実importを固定、Dimmer preset非搭載profile（f3200a）対象はfail-closed経路の実カバレッジとして断言化した（`qa/specimens/ValueCatalog-Sweep-Plasma.dvc`、80c82be/d398848）。同標本でCOLOR MAPPINGS 5/3実body（ID36）とMove ID221のBEAMID=1..6を初取得し、DVC-V4（Codex実装・babc0eb）が両者をスキーマ厳密検証つき精密fail-closedへ: MAPPING RectangleはPatchキャンバス100×100 rasterサンプラ（ANGLEは包含マスクのみ回転）と完全証明され、忠実importに必要なprotocol追加2件（Rainbow Grayscale＋placement body／beam単位Moveターゲット＋circumcircle補間）は`qa/DVC_COLOR_MAPPINGS_MOVE_SOURCE_PARITY.md`に**ユーザー承認待ち提案**として固定。副発見として既存MAPPINGS 521の窓非保持（同じRectangleサンプラを使うべき）を既知の非厳密性として記録した。並行して監督レーンはフルviewportマトリクスで継承回帰2件（Scene Matrixバンク帯3px密度ドリフト=6e476a0修正／T25-Eトップバードラッグ領域の3サイト陳腐化断言=16ccc3a再交渉）を検出・修正し、最終フルマトリクスMATRIX_EXIT=0・全カテゴリ0失敗を確認、監査残件F8（Matrix列密度）は再計測でクローズした（総可視セルSyndocal~93 vs Daslight~72）。Rustゲートはdvc 61/61・syndocal 387 pass/9 ignored/0 failで着地。

2026-08-10〜11（Fable監督・Codex/Opus並行実装、correctedトランシェ＋evaluator統合）: DVC corrected互換トランシェ（5198610）とexact/enhanced二重ルート統合（ab160ed）をorigin/codex/syndocal-v1.0へ着地・pushした。correctedトランシェは復元実装アーティファクト（40ms量子化・Qt 1024テーブル・65536パレットキャッシュ・750フレーム上限・nearest-fold・RNG履歴依存）を全除去し、authored意味論を厳密化: Perlin/Move/Curve/Burst/KR/Sweepは連続位相の解析評価器（1025msは1025msのまま、Transform折返しはA/B証拠付きでtent写像へ）、Sparkleは単一視覚時計の状態機械（40ms視覚エポック×Number同時粒子、テンポ不変人口上限Number×(⌈寿命/40⌉+1)、authored period=乱数ループ長、シークは満杯場へ決定論的バックフィル、描画時パレット解決、live-modifier/topology/activation 3経路の状態移送）へ再設計した。プロセス上の要点は敵対的検証ループが**コミット前に実欠陥2件を捕獲**したこと（①視覚スポーン×壁時計失効の非対称=高速同期で約1秒毎の場全消去→ユーザー裁定「単一視覚時計」「period=ループ長」で根治②パレット縮小×状態移送のOOB panic→particle_seed保持・描画時解決で根治）。6クラス横断監査でSparkle級欠陥の他家族非存在を消極証明し、f64位相化（83時間精度テスト付き）とu64剰余も同梱。実機Daslight標本`ValueCatalog-Burst-KnightRider.dvc`（Burst float 0..1 gradient／KR int percent／Transform enum ID3=1／二重PRESET束縛／IDSELECTION 32ビーム）を自作しimporter goldenで固定、具象profile-channel PRESET束縛も実装した。統合トランシェはdaslight_exact/syndocal_corrected分岐を廃止（正味-405行）、統一解析評価器＋Syndocalネイティブドメイン＋読み込み時決定論的移行（将来バージョン拒否テスト付き）とし、レガシー.sdcはフラグ受理→再直列化で除去を契約テスト化した。監督検証は統合HEADで全て直接実行: fmt／engine全テスト／syndocal 409／workspace all-targets／リリースゲートp95=2.118ms（Sparkle最大2行＋KR 254レーン行込み、5/8/12ms契約不変）／フロント6ゲート（localization 100%）／viewportフルマトリクスexit 0（**新基準pass 208**=ハーネス再編後）／HEAD再ビルドexeの1ウィンドウ応答・プロセス残存ゼロ。後続はChaser(256×)/Curve(31×)/Smooth Move/LFO/PositionWaveのゲート最大行と、エフェクト基数の「封筒保証+警告」ポリシー（ユーザー裁定済: ハード上限なし・64×200封筒をゲート保証しtelemetry+UI警告で可視化）をCodexで実装中。

- アプリ全体を縦スクロールさせない。一画面デスクを維持し、必要なスクロールは各パネル内に閉じる。
- Desktopは起動時最大化を既定とし、F11でwindow fullscreen、Escで解除できる状態を維持する。主browser visual gateは1920x1080、1920x1032はbrowser上のmeasured-work-area fixtureとする。T25-E（2026-07-30）以降、本体ウィンドウはフレームレス（`decorations: false`、42pxトップバー=タイトルバー、右端に40x40の最小化/最大化/閉じる、8方向エッジリサイズ、ダブルクリック最大化）であり、native受入は1920x1032のWindows作業領域内で最大化client 1920x1032、F11 fullscreen client 1920x1080、Esc後の完全復帰を別証拠として検査する（装飾あり時代の1920x1009は履歴値）。閉じるは`close()`のCloseRequested経路を通り、復旧/未保存確認フローを迂回しない。nativeの主証拠にはcurrent HEADのisolated QA buildまたは同一revisionのrelease artifactだけを使い、起動済みの旧binary／小窓binaryは比較・診断証拠に限定する。2048x1152は拡張上限回帰、1366x768／1280x720は小画面containment fallbackとして別に維持する。fallback通過だけでUIデザイン合格としない。
- SetupはLightingとVideoの専用画面を混在させず、タブで切り替える。
- Controlは上段のCue/Transport、左下の2D Stage Map、右下のLive Edit/Timeline/Mixerを維持する。
- 44Hz DMXスレッドに動画処理、ブロッキングI/O、動的アロケーションを持ち込まない。
- Windows/macOS/Linux固有機能は`cfg`/feature境界に置き、Unavailableなバックエンドでアプリ全体を起動失敗させない。
- プロジェクト名はSyndocal、拡張子は`.sdc`、発行者は`Seraf()のKTN`。Rayard/`.ry`/KDMXのユーザ向け表記を復活させない。
- 小変更は対象テスト、マイルストーンだけ全ビルドを行い、テストプロセスを多重常駐させない。

## 次スレッドの開始手順

まず次を確認する。

```powershell
git status --short
git branch --show-current
git log -5 --oneline
pnpm --dir app run check:release
```

参照順:

1. `RELEASE_STATUS.md` - 現在の判定と次の優先順位
2. `COMPLETION_PLAN.md` - v1.0 Definition of Doneと実装証跡
3. `qa/M4_IO_VALIDATION.md` - 外部I/O実機受入
4. `qa/M5_RELIABILITY_VALIDATION.md` - ソーク/回復/負荷
5. `qa/M6_RELEASE_VALIDATION.md` - 配布物/CI/署名判断
6. `CLAUDE.md` - 詳細な実装履歴。古い記述は末尾の新しい記録を優先する

次の作業は、v1.0コードを無目的に拡張するのではなく、次のいずれかを新しい目標として明示してから始める。

- 実機受入と正式リリース
- Webリモートのセキュリティ強化
- v1.1機能開発
