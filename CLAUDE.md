# Rayard Claude Code Handoff

This workspace is a large, intentionally dirty prototype branch for Rayard, a Rust/Tauri/SolidJS DMX lighting + VJ control app.

## Product Decisions To Preserve

- Product name: Rayard.
- Developer / publisher: Seraf()のKTN.
- Project files use `.ry`. Keep legacy project-name and extension aliases out of user-facing files.
- The user prefers lightweight checks for small changes. Use full Tauri builds only at major milestones.
- 3D visualization is deferred/external for now. Keep the app focused on the Setup 2D mapping visualizer unless the user asks otherwise.
- The Setup UI should keep a DasLight-like operational feel: dense, dark, tabbed, grid-based, and tool-oriented.
- README updates can lag implementation unless the user asks for docs. This file is the current handoff artifact.

## Current Workspace State

The worktree is expected to be dirty. Do not revert broad changes just because they are uncommitted.

Current handoff checkpoint, 2026-06-15:

- Handoff checkpoint: 100%. This is a strong prototype handoff point, not final product completion.
- Product completion is not 100% yet: native wgpu video output, in-process FFmpeg/HAP workers, hardware-timed Open DMX refinement, and real NDI/Spout/Syphon bindings remain future work.
- Full workspace tests passed: `cargo test --workspace`.
- Frontend production build passed: `pnpm --dir app build`.
- Tauri debug build passed: `pnpm --dir app tauri build --debug`.
- Tauri release build passed: `pnpm --dir app tauri build`.
- Debug executable was produced at `target/debug/rayard.exe`.
- Release executable was produced at `target/release/rayard.exe`.
- Current post-handoff slices are intentionally narrow: `WorkspaceChrome` is wired into `App.tsx`, Live/Touch stage SVG rendering is shared through `StagePreview2D`, and Setup Mapping controls/layers are shared through `MappingFilterStrips`, `MappingToolRail`, `MappingViewportControls`, `MappingStageConfigPanel`, `MappingStageObjectsLayer`, `MappingVideoSurfacesLayer`, `MappingBeamsLayer`, `MappingGeometryLayer`, and `MappingFixturesLayer`.

`app/src/App.tsx` was previously reverted after a bad PowerShell overwrite. The exact 24,429-line dirty version could not be recovered from editor history, but the current file has been rebuilt to a working recovery point: Rayard branding is restored, Setup/Control/Touch still render, Setup Mapping has a broad 2D map workspace, and `pnpm --dir app build` passes. Treat this as the handoff baseline rather than trying to resurrect the lost dirty file with git checkout/reset.

Important high-level changes already present:

- Tauri/Solid app is branded as Rayard in `app/package.json`, `app/index.html`, and `app/src-tauri/tauri.conf.json`.
- `.ry` project association is configured in `app/src-tauri/tauri.conf.json`.
- Setup has `Setup`, `Control`, and `Touch`-style workspace flows in `app/src/App.tsx`.
- Setup Mapping has a 2D mapping view with group/type strips, stage grid, fixture placement, projector/output surfaces, and selection controls.
- Internal SVG/isometric 3D UI has been removed from `app/src/App.tsx`; keep 3D work outside the main UI unless the user asks to bring it back.
- Engine has GDTF/custom profile patching, 44 Hz DMX tick, Art-Net/sACN/serial route support, cues, timeline, effects, video state, telemetry, and project snapshot support.
- Video is currently a CPU/reference preview and state model path with still-image decode, FFmpeg CLI file-frame extraction, bounded frame queues, decode scheduling diagnostics, and Display output preview windows. Native wgpu video output, in-process FFmpeg/HAP workers, NDI, Spout, and Syphon transports are future-phase work.

## Useful File Map

- `app/src/App.tsx`: main SolidJS UI. It is still very large; the topbar/workspace/setup tab chrome is now extracted into `app/src/components/WorkspaceChrome.tsx`.
- `app/src/styles.css`: main UI styling.
- `app/src/components/`: extracted SolidJS UI panels/editors, including `WorkspaceChrome.tsx`, `StagePreview2D.tsx`, `MappingFilterStrips.tsx`, `MappingToolRail.tsx`, `MappingViewportControls.tsx`, `MappingStageConfigPanel.tsx`, `MappingStageObjectsLayer.tsx`, `MappingVideoSurfacesLayer.tsx`, `MappingBeamsLayer.tsx`, `MappingGeometryLayer.tsx`, `MappingFixturesLayer.tsx`, `DmxOutputConfigPanel.tsx`, `DmxRawMonitor.tsx`, `DmxTestFramePanel.tsx`, `DmxRoutesPanel.tsx`, `EngineTelemetryPanel.tsx`, and `VideoOutputWindow.tsx` for Phase 1 DMX/output diagnostics and dedicated display-output windows.
- `app/src/customFixtureProfile.ts`: custom fixture profile helpers.
- `app/src/numericHelpers.ts`: pure numeric/DMX-value/color/geometry-matrix helpers extracted from `App.tsx` (clamp/percent, hsv<->rgb, geometry matrix math). No SolidJS/state deps; safe to grow with other pure helpers.
- `app/src/channelFunctionHelpers.ts`: pure GDTF channel-function helpers extracted from `App.tsx` (sorted/picked/ranked function value lookups, label/range/detail formatting, plus color/gobo wheel predicates `isColorWheelFunction`/`isGoboWheelFunction`/`goboPatternForFunction`). Depends on `AttributeControl`, `numericHelpers`, and the `GoboSlotPattern` type from `components/WheelSlotPanel`.
- `app/src/svgExportHelpers.ts`: SVG/stage-map export DOM helpers extracted from `App.tsx` (`svgExportComputedStyleProperties`, `standaloneSvgExportSelectorsToRemove`, `inlineComputedSvgStyles`, `downloadTextFile`, `safeExportFileNamePart`). No app-state deps.
- `app/src/favoritesStorage.ts`: localStorage-backed color and position favorites extracted from `App.tsx` (`loadColorFavorites`/`saveColorFavorites`, `defaultPositionFavorites`, `positionFavoriteFromUnknown`, `loadPositionFavorites`/`savePositionFavorites`). Reuses the `PositionFavorite` type already exported by `components/PositionControlPanel` (App.tsx's duplicate local interface was removed). Storage keys are module-private.
- `app/src/controlCategory.ts`: per-category "quick look" presets + value resolution extracted from `App.tsx` — `CategoryQuickLook` type, `quickLooksForCategory`, `opticsRoleForControl`, `quickLookValueForControl`. The `ControlCategory` enum / `controlCategories` table / `controlCategoryForAttribute` classifier are NOT here — they already live in `uiModes.ts` (shared with components); this module imports `ControlCategory` from there.
- `app/src/stageObjects.ts`: stage decoration object helpers extracted from `App.tsx` (`stageObjectKinds`, `stageObjectDefaultColor`, `stageObjectClass`). Depends only on `StageObjectKind`.
- `app/src/uiModes.ts` (pre-existing): single source of truth for `WorkspaceTab`, `SetupSubTab`, `ControlMode`, `ControlCategory` types and the `setupSubTabs`/`controlModes`/`controlCategories` tables + `controlCategoryForAttribute` and tab/mode shortcut helpers. App.tsx and components both import from here (App.tsx's old local duplicates were removed in slice 19).
- `app/src/videoOutputMapping.ts` (pre-existing): projector/output mapping helpers + `defaultVideoOutputMapping`, `NumericVideoOutputMappingField`, `outputAspectRatio`, and (added slice 18) `MappingVideoOutputCornerKey`/`mappingVideoOutputCorners`/`mappingVideoOutputCornerGain`. This is the single source of truth for video-output mapping; `controlMappingActions.ts` re-exports `NumericVideoOutputMappingField` from here.
- `app/src/projectSnapshot.ts`: `projectComparableSnapshot`/`projectSnapshotSignature` extracted from `App.tsx` — strip volatile runtime fields (active cue/fade, timeline/video playback position, clock phase, dmx preview, telemetry) so two `EngineSnapshot`s can be compared for real project changes.
- `app/src/wheelMedia.ts`: GDTF wheel-slot media helpers extracted from `App.tsx` (`WheelMediaBytes`/`WheelMediaPayload` types, `wheelSlotMediaPath`, `wheelMediaCacheKey`, `canLoadWheelMedia`, `wheelMediaPayloadToObjectUrl`). No app-state deps.
- `app/src/hotkeyHelpers.ts`: keyboard-shortcut helpers extracted from `App.tsx` (`isEditableShortcutTarget`, `controlCueHotkeyIndex`, `mappingStageToolFromHotkey`). Depends only on the `MappingStageTool` type.
- `app/src/fixtureLimits.ts`: fixture pan/tilt/dimmer limit + axis-mapping helpers extracted from `App.tsx` (`defaultFixtureLimits` const plus `normalizeLimitRange`, `applyAxisLimit`, `sourceValueForAxisLimit`, `dimmerValueWithinLimits`, `effectivePanTiltValues`, `sourcePanTiltValues`). Depends on `numericHelpers` + `FixtureLimits`/`PatchedFixtureSummary` types.
- `app/src/controlMappingActions.ts`: MIDI/OSC control-mapping action sets, the video-output mapping field catalog (`videoOutputMappingFieldOptions`), and small domain types/predicates extracted from `App.tsx` (`NumericVideoOutputMappingField`, `MappingFixtureFlag`, `FixtureFlagClearKind`, `fixtureFlagClearKinds`, the `is*MappingAction` predicates, `normalizeFixtureFlagClearKind`, `videoOutputMappingFieldOption`). Depends only on `./types`.
- `app/src/videoHelpers.ts`: pure video-source label + media path/time formatting helpers extracted from `App.tsx` (`videoSourceInputLabel`, `videoSourceInputPlaceholder`, `videoSourceCanBrowseFile`, `videoSourceKindLabel`, `mediaLabelFromPath`, `shouldReplaceVideoLayerDraftLabel`, `formatDuration`, `formatVideoTime`, `videoSourceMetadataLabel`). Depends only on `VideoSourceKind` from `./types`. (`beamPoints`, the fixture beam-triangle geometry that sat next to these, was moved into `stageGeometry.ts` instead.)
- `app/src/fixtureVisuals.ts`: fixture visual-kind classification + mapping-stage label/size helpers extracted from `App.tsx` — `MappingFixtureVisualKind` type, `fixtureVisualKind`, `mappingFixtureStageSize`, `fixtureTypeKey`, `fixtureTypeLabel`, `mappingTypeGlyphClass`. Depends only on `PatchedFixtureSummary` from `./types`.
- `app/src/editorDrafts.ts`: editor draft models and pure "from summary" builders extracted from `App.tsx` — `VideoOutputConfigDraft`, `CueMetadataDraft`, `TimelineEventDraft`, `TimelineAutomationDraft`, `TimelineVideoAutomationDraft` interfaces plus `videoOutputConfigDraftFromSummary`, `cueMetadataDraftFromSummary`, `timelineEventDraftFromSummary`, `timelineAutomationDraftFromSummary`, `timelineVideoAutomationDraftFromSummary`. Depends only on `./types`.
- `app/src/mappingViewPresets.ts`: Setup-mapping stage-tool + saved view-preset model and localStorage helpers extracted from `App.tsx` — `MappingStageTool` type, `MappingViewPreset` interface, `normalizeMappingStageTool`, `defaultMappingViewPresets`, `mappingViewPresetFromUnknown`, `loadMappingViewPresets`, `saveMappingViewPresets`. App.tsx imports the two types plus load/save (the default/normalize/from-unknown helpers are only used internally by load now). `mappingStageTools` array and storage key are module-private.
- `app/src/stageGeometry.ts`: stage 2D coordinate helpers extracted from `App.tsx` — `StageWorldBounds`, `stageViewBoxSize`, `stagePadding`, and `stageWorldToSvgPoint`/`svgPointToStageWorld` conversions. (`CueCapturePreviewPanel.tsx` and `StageMap2D.tsx` still keep their own local `StageWorldBounds`; unifying those onto this module is a possible future cleanup.)
- `app/src/uiModes.ts`: workspace/setup/control tab mode definitions.
- `app/src/videoLayerDefaults.ts`: default video layer state fragments.
- `app/src/videoFrameCanvas.ts`: shared RGBA preview-frame validation, canvas drawing, and data-URL conversion helpers.
- `app/src/videoOutputMapping.ts`: projector/output mapping helpers.
- `app/src-tauri/src/main.rs`: Tauri command boundary, project files, validation, telemetry report, GDTF Share, video preview commands.
- `crates/engine/src/lib.rs`: realtime engine, DMX render loop, cues, timeline, effects, telemetry.
- `crates/gdtf/src/lib.rs`: GDTF ZIP/XML parser and media/model loading helpers.
- `crates/io/src/`: Art-Net, sACN, serial DMX, MIDI, OSC, remote WebSocket.
- `crates/video/src/lib.rs`: video state/runtime/reference compositor/preview frame boundaries.
- `crates/visualizer/src/lib.rs`: renderer-facing scene conversion; keep it as a data boundary while 3D UI is deferred.

## Known Verification Commands

Use targeted checks while iterating:

```powershell
cargo test --workspace
cargo test -p rayard phase1_smoke_project_sample_
cargo test -p rayard phase1_smoke_project_sample_loads_into_engine_and_renders_cue
cargo test -p rayard phase1_smoke_project_sample_sends_cue_to_artnet_loopback
cargo test -p rayard phase1_smoke_project_sample_is_valid
cargo test -p rayard project_
cargo test -p rayard sample_effect_presets_are_valid_for_phase1_mini_show
cargo test -p rayard gdtf_profile_patch_drives_engine_dmx_preview
cargo test -p rayard engine_telemetry_budget_report
cargo test -p engine engine_sends_rendered_fixture_state_to_artnet_loopback
cargo test -p engine bpm_clock
cargo test -p gdtf
pnpm --dir app build
pnpm --dir app tauri build --debug
```

Notes:

- `pnpm --dir app build` currently succeeds with no chunk warning. `vite.config.ts` `manualChunks` already splits `control-panels` (~40 kB) and `mapping-editors` (~17 kB) out of the main bundle, so the main `index-*.js` chunk is ~419 kB, under the 500 kB warning threshold. The earlier "chunk warning" note is stale.
- `pnpm --dir app tauri build --debug` currently builds `target/debug/rayard.exe`. Full release packaging should still be saved for major release checkpoints:

```powershell
pnpm --dir app tauri build
```

## Recent Verified Points

- GDTF parse -> patch -> Engine DMX preview is covered by `gdtf_profile_patch_drives_engine_dmx_preview` in `app/src-tauri/src/main.rs`.
- Engine rendered fixture state -> Art-Net UDP loopback is covered by `engine_sends_rendered_fixture_state_to_artnet_loopback` in `crates/engine/src/lib.rs`.
- 2026-06-23 targeted Phase 1 MVP re-check passed: `cargo test -p rayard phase1_smoke_project_sample_`, `cargo test -p rayard gdtf_profile_patch_drives_engine_dmx_preview`, `cargo test -p engine engine_sends_rendered_fixture_state_to_artnet_loopback`, and `pnpm --dir app build` all completed green. This verifies the current dirty tree still covers sample project validity, sample load/render, sample Art-Net loopback, GDTF patch-to-preview, Engine Art-Net loopback, and frontend type/bundle checks.
- Telemetry budget report pass/fail/warn behavior is covered by `engine_telemetry_budget_report*` tests in `app/src-tauri/src/main.rs`.
- BPM clock tap/MIDI/external sync is covered by `cargo test -p engine bpm_clock`.
- Project snapshot loading restores self-contained fixture controls from `snapshot://fixture/{id}` and does not depend on the original external profile path.
- The app topbar has `Load Sample`, backed by the embedded `samples/phase1-mini-show.ry`, for quick Phase 1 manual smoke checks without a file dialog.
- The app topbar also has `Run Smoke`: it loads the embedded mini show, triggers the first cue, switches to Control, selects DMX Raw U0, and reports non-zero values in A1-A8.
- The Output panel telemetry section now exposes Reset, Save Report, and live budget status. It is backed by `reset_engine_telemetry`, `save_engine_telemetry_report`, and `get_engine_telemetry_report`.
- Latest major checkpoint in this handoff, 2026-06-14: `cargo test --workspace`, `pnpm --dir app build`, `pnpm --dir app tauri build --debug`, and `pnpm --dir app tauri build` passed on Windows.
- Position Wave effect editing has quick presets for origin from stage center or selected fixture, X/Z/radial direction buttons, and a mini stage map for setting origin/direction against current 2D fixture and projector positions. Radial uses the engine's zero-direction distance fallback.
- Control now includes a minimal Node Graph builder that reuses the current LFO/Position Wave source and selected lighting/video target, with enable/disable/remove and `.nodegraph` preset save/load commands.
- 2026-06-23 Phase 2 effect-preset entry point: added the `load_sample_effect_preset` Tauri command backed by embedded `samples/front-dimmer-pulse.effect` and `samples/front-dimmer-wave.effect`, plus `Sample Pulse` / `Sample Wave` buttons in the Effects header. The command validates the embedded preset, accepts an optional target override, and adds the preset through the same Engine effect path as file-loaded presets. The Effects header also has `Pulse Target` / `Wave Target` buttons that retarget those embedded presets to the current fixture/group/video target using the existing target-override form. Verification now includes `cargo test -p rayard sample_effect`, covering sample preset validity, embedded sample retargeting (`embedded_sample_effect_presets_can_retarget_current_effect_target`), and actual Engine insertion of a retargeted embedded sample effect (`embedded_sample_effect_preset_adds_retargeted_effect_to_engine`); `pnpm --dir app build` also passed after the UI wiring (green; main index chunk ~471.08 kB).
- 2026-06-23 Phase 2 flash preset slice: added `samples/front-dimmer-flash.effect` as a Square LFO Dimmer flash for the `Front` group (`period_ms: 125`, `clock_sync.beats: 0.25`), wired the embedded sample preset aliases (`flash`, `front-dimmer-flash`, `front_dimmer_flash`), and added `Sample Flash` / `Flash Target` controls beside the existing pulse/wave controls. The Effects header actions now wrap inside the panel so the extra buttons do not push the app frame into global scrolling. Verification: `cargo fmt`, `cargo test -p rayard sample_effect`, and `pnpm --dir app build` passed. README/sample README intentionally not updated in this slice per the user's preference to batch docs later.
- 2026-06-23 Phase 2 chase preset slice: added `samples/front-dimmer-chase.effect` as a Square PositionWave Dimmer chase for the `Front` group (`wavelength: 2.0`, `clock_sync.beats: 2.0`), wired aliases (`chase`, `front-dimmer-chase`, `front_dimmer_chase`), and changed the Effects header sample controls from one button per preset to a compact preset selector plus `Sample` / `Target` buttons. This keeps the DasLight-style dense header scalable as more basic effect presets are added. Verification: `cargo fmt`, `cargo test -p rayard sample_effect`, and `pnpm --dir app build` passed. Docs are still intentionally batched for later.
- 2026-06-23 Phase 2 output-route diagnostic coverage: added `sends_dmx_test_frame_to_enabled_sacn_routes` beside the existing Art-Net route diagnostic test. The new loopback test exercises `send_dmx_route_test_frames` with two enabled localhost sACN routes plus one disabled route, parses the emitted E1.31 frame with `io::sacn::parse_sacn_dmx_packet`, and verifies universe/data routing plus disabled-route silence. Verification: `cargo fmt`, `cargo test -p rayard sends_dmx_test_frame_to_enabled_sacn_routes`, and `cargo test -p rayard sends_dmx_test_frame_to_enabled` passed; `cargo fmt` still prints the known `could not canonicalize path: 'C:\Users\kouty'` warning.
- 2026-06-23 Phase 2 serial-output coverage: added explicit `DmxKingUltraDmx` protocol support as an Enttec USB PRO-compatible serial route. The variant is wired through `protocol`, Engine sender creation, Tauri validation/test-frame sending, App/Output panel type options, and the WebSocket remote route labels. It is intentionally grouped with Enttec PRO for baud handling and duplicate serial-port route detection, not with Enttec Open DMX's FTDI break timing path. Verification: `cargo test -p rayard dmx_output_config_validation_treats_dmxking_as_serial_pro_route`, `cargo test -p engine dmxking_ultradmx_uses_enttec_pro_serial_sender_path`, `cargo test -p io serial`, and `pnpm --dir app build` passed.
- 2026-06-23 DasLight-style app frame fix: `app/src/styles.css` now pins `html`, `body`, `#root`, and `.app` to the viewport with `overflow: hidden`, changes `.app` from the stale five-row grid to `auto minmax(0, 1fr)`, and gives `.layout`, `.layout > *`, and direct `.panel` children fixed-height internal scroll boundaries. This prevents the whole Rayard window from page-scrolling while preserving internal panel scrolling for long lists such as fixture libraries and profile lists. Verification: `pnpm --dir app build` passed after the CSS change.
- The video CPU reference compositor accepts RGBA, BGRA, DXT1, and DXT5 frames; `cargo test -p video` passes after the DXT/BGRA path was added.
- Display output windows now call the CPU preview path with a one-frame decode warmup budget per live frame, while manual debug previews keep the fuller prefetch budget.
- Project validation allows named NDI/Spout/Syphon placeholder routes to load even when the native backend is not linked; backend availability stays in Video Control I/O plans and Sync Routes diagnostics.
- Video Control I/O plan rows now label unavailable external routes as `Blocked`, while Sync Routes reports `Started`, `Kept`, `Stopped`, `Idle`, `Blocked`, or driver failure rows.
- Latest lightweight checkpoint after the output-window decode/refactor and external-I/O validation changes: `cargo test -p rayard video_preview_decode_budget`, `cargo test -p rayard video_preview`, `cargo test -p rayard video_output_decode_preview_summaries_report_schedulable_requests`, `cargo test -p rayard external_video`, `cargo test -p rayard project_file_keeps_external_video_routes_as_runtime_diagnostics`, `pnpm --dir app build`, and focused `git diff --check` over touched Rust/frontend/doc files passed.
- Browser verification at 1280px confirmed Setup, Control, and Touch no longer create horizontal page overflow after the Output/DMX route panel CSS fix.
- 2026-06-14 (Claude takeover, frontend slice 1): Extracted pure numeric/DMX/color/geometry-matrix helpers out of `App.tsx` into new `app/src/numericHelpers.ts` (clamp01, finiteOr, clampRange, clampDmxValue, formatDmxPercent, dmxValueToPercent, percentToDmxValue, formatShortDmxPercent, valueToHexByte, outputAspectRatio, hsvToRgb, rgbToHsv, rgbToHex, defaultColorFavorites, normalizeHexColor, geometryIdentityMatrix, normalizedGeometryMatrix, multiplyGeometryMatrix, cumulativeGeometryMatrix, geometryMatrixTranslation, rotateStageOffsetYaw). `App.tsx` imports them back; net ~120 lines removed. Verified with `pnpm --dir app build` (tsc + vite, green, no chunk warning). Working tree was clean before this slice and is intentionally left uncommitted. Next safe slices: continue moving other pure helpers, then tackle the larger Setup Mapping / Output-Telemetry JSX panels (these need signal/prop threading, so do them as their own verified slices).
- 2026-06-14 (Claude takeover, frontend slice 2): Extracted stage-coordinate helpers into new `app/src/stageGeometry.ts` (`StageWorldBounds`, `stageViewBoxSize`, `stagePadding`, `stageWorldToSvgPoint`, `svgPointToStageWorld`) and GDTF channel-function helpers into new `app/src/channelFunctionHelpers.ts` (`normalizedFunctionText`, `channelFunctionValue`, `sortedChannelFunctions`, `pickFunctionValue`, `indexedFunctionValue`, `rankedFunctionValue`, `channelFunctionLabel`, `channelFunctionRangeLabel`, `channelFunctionDetail`). `App.tsx` imports them back. `App.tsx` now ~19,005 lines (down from ~19,082 after slice 1, ~19,200 originally). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted. Remaining pure-helper candidates in `App.tsx` worth extracting next: small pure label/enum helpers (e.g. `outputProtocolLabel`, `controlCategoryForAttribute`, `fixtureVisualKind`/`fixtureTypeKey`/`fixtureTypeLabel`/`mappingFixtureStageSize`, `videoSourceInputLabel`) — most depend only on `./types` enums and could form a `labelHelpers.ts`. After the obvious pure helpers are exhausted, the next big win is extracting whole JSX panels (Setup 2D Mapping, Output/Telemetry), which require signal/prop threading and should each be their own carefully-verified slice.
- 2026-06-14 (Claude takeover, frontend slice 3): Extracted SVG/stage-map export helpers into new `app/src/svgExportHelpers.ts` and the color/gobo wheel predicates (`isColorWheelFunction`, `isGoboWheelFunction`, `goboPatternForFunction`) into `app/src/channelFunctionHelpers.ts`. `App.tsx` now ~18,885 lines (down from ~19,005 after slice 2; ~19,200 originally — ~315 lines moved out across the three slices). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-14 (Claude takeover, frontend slice 4): Extracted localStorage color/position favorites into new `app/src/favoritesStorage.ts` and removed the duplicate local `PositionFavorite` interface from `App.tsx` (now imports the type from `components/PositionControlPanel`). `App.tsx` now ~18,785 lines (~420 lines moved out across four slices into `numericHelpers.ts`, `channelFunctionHelpers.ts`, `stageGeometry.ts`, `svgExportHelpers.ts`, `favoritesStorage.ts`). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-14 (Claude takeover, frontend slice 5): Extracted the Setup-mapping stage-tool/view-preset model + localStorage helpers into new `app/src/mappingViewPresets.ts` (`MappingStageTool`, `MappingViewPreset`, `normalizeMappingStageTool`, `defaultMappingViewPresets`, `mappingViewPresetFromUnknown`, `loadMappingViewPresets`, `saveMappingViewPresets`). `App.tsx` now ~18,655 lines (~550 lines moved out across five slices into six helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-14 (Claude takeover, frontend slice 6): Extracted the editor draft interfaces + "from summary" builders into new `app/src/editorDrafts.ts` (video-output config, cue metadata, timeline event/automation/video-automation). `App.tsx` now ~18,575 lines (~630 lines moved out across six slices into seven helper modules: `numericHelpers`, `channelFunctionHelpers`, `stageGeometry`, `svgExportHelpers`, `favoritesStorage`, `mappingViewPresets`, `editorDrafts`). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-14 (Claude takeover, frontend slice 8): Extracted the video-source label/media-formatting cluster into new `app/src/videoHelpers.ts` and moved `beamPoints` into `stageGeometry.ts`. This cluster sat immediately above `export default function App()`, so the tail end of the module-level pure helpers is now extracted. `App.tsx` now ~18,461 lines (~740 lines moved out across eight slices into nine helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-14 (Claude takeover, frontend slice 9): Extracted the MIDI/OSC control-mapping action sets + field catalog + classifier predicates/types into new `app/src/controlMappingActions.ts` (moved the `NumericVideoOutputMappingField`/`MappingFixtureFlag`/`FixtureFlagClearKind` types and `fixtureFlagClearKinds` const with them; App.tsx re-imports all). `App.tsx` now ~18,400 lines (~800 lines moved out across nine slices into ten helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted. STATUS: the cleanly-separable top-level helpers are now largely exhausted — most remaining module-level helpers are either tiny scattered label fns coupled to the core `ControlCategory` enum, or live inside the `App()` component body. Further meaningful size reduction now means extracting JSX panels (Setup 2D Mapping, Output/Telemetry), which thread SolidJS signals and should be done as deliberate, individually-reviewed slices rather than rapid autonomous micro-extractions.
- 2026-06-14 (Claude takeover, frontend slice 10, dedup): Removed duplicate untyped `defaultTransform`/`defaultColorAdjust`/`defaultFxAdjust` constants from `App.tsx`; these already existed (typed as `Transform2D`/`VideoColorAdjust`/`VideoFxAdjust`) in `app/src/videoLayerDefaults.ts`, which `App.tsx` was not importing. App.tsx now imports them from there, eliminating drift risk. Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-14 (Claude takeover, frontend slice 11): Extracted the type-free static UI preset arrays (`defaultColorPalette`, `colorQuickLooks`, `panTiltNudgeSteps`, `panTiltTargetPoints`, `mappingSnapPresets`) into new `app/src/uiPresets.ts`. `App.tsx` now ~18,338 lines (~865 lines off the original ~19,200 across eleven slices into eleven new helper modules: `numericHelpers`, `channelFunctionHelpers`, `stageGeometry`, `svgExportHelpers`, `favoritesStorage`, `mappingViewPresets`, `editorDrafts`, `fixtureVisuals`, `videoHelpers`, `controlMappingActions`, `uiPresets`). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted. This concludes the clean module-level pure-helper/data extraction phase — the remaining interleaved enum tables (`setupSubTabs`, `controlCategories`, `mappingVideoOutputCorners`) are tightly bound to core App-local types and give little benefit moved, and everything else is inside `App()`. The next high-value step is JSX-panel extraction (Setup 2D Mapping view, remaining Output/Telemetry orchestration), which threads SolidJS signals as props and should be done as deliberate, individually-reviewed slices.
- 2026-06-15 (Claude takeover, frontend slices 12-13): Two more clean pure-helper groups that turned out to still exist above `App()`: slice 12 extracted keyboard-shortcut helpers into new `app/src/hotkeyHelpers.ts` (`isEditableShortcutTarget`, `controlCueHotkeyIndex`, `mappingStageToolFromHotkey`); slice 13 extracted fixture limit/axis helpers + `defaultFixtureLimits` into new `app/src/fixtureLimits.ts`. `App.tsx` now ~18,271 lines (~930 lines off the original ~19,200 across thirteen slices into thirteen new helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted.
- 2026-06-15 (Claude takeover, frontend slices 14-15): slice 14 extracted project-snapshot comparison helpers into new `app/src/projectSnapshot.ts`; slice 15 extracted GDTF wheel-slot media helpers + types into new `app/src/wheelMedia.ts`. `App.tsx` now ~18,214 lines (~990 lines off the original ~19,200 across fifteen slices into fifteen new helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted. Remaining cohesive top-level group worth one more slice: the `ControlCategory` domain (done in slice 16).
- 2026-06-15 (Claude takeover, frontend slice 16): Extracted the control-category domain into new `app/src/controlCategory.ts` (`ControlCategory`/`CategoryQuickLook` types, `controlCategories` table, `controlCategoryForAttribute`, `quickLooksForCategory`, `opticsRoleForControl`, `quickLookValueForControl`). `App.tsx` now ~18,034 lines (~1,170 lines off the original ~19,200 across sixteen slices into sixteen new helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted. This effectively completes the module-level helper extraction: everything above `export default function App()` is now either a tiny core type alias / a few interleaved enum tables tightly bound to App-local types (`SetupSubTab`, `MappingVideoOutputCornerKey`, etc.), or the `invoke`/`listen` Tauri wrappers. All further size reduction now requires extracting code from INSIDE the `App()` component body — i.e. JSX panels (Setup 2D Mapping view, Control views, Output/Telemetry orchestration) that thread SolidJS signals and must be done as deliberate, individually-reviewed slices.
- 2026-06-15 (Claude takeover, frontend slices 17-19 + dedup pass): slice 17 extracted stage-object helpers into new `app/src/stageObjects.ts`. slice 18 moved the projector corner table/type/gain (`MappingVideoOutputCornerKey`, `mappingVideoOutputCorners`, `mappingVideoOutputCornerGain`) into the existing `videoOutputMapping.ts`, and removed App.tsx's DUPLICATE `defaultVideoOutputMapping` const (canonical copy already in `videoOutputMapping.ts`) + de-duplicated `NumericVideoOutputMappingField` (now defined once in `videoOutputMapping.ts`, re-exported by `controlMappingActions.ts`). slice 19 discovered that `uiModes.ts` already owned `WorkspaceTab`/`SetupSubTab`/`ControlCategory`/`controlCategories`/`controlCategoryForAttribute`/`setupSubTabs` — App.tsx had been carrying stale duplicates and the slice-16 `controlCategory.ts` had wrongly re-defined the `ControlCategory` trio. Fixed: App.tsx now imports those from `uiModes.ts`, and `controlCategory.ts` keeps only the quick-look helpers and imports `ControlCategory` from `uiModes`. Also de-duplicated `outputAspectRatio` (removed the slice-1 copy from `numericHelpers.ts`; App.tsx imports it from `videoOutputMapping.ts`) and `videoOutputAspectModes` (App.tsx local copy removed; imported from `videoOutputMapping.ts`). `App.tsx` now ~17,958 lines (~1,245 lines off the original ~19,200; 18 new helper modules + consolidation onto pre-existing `uiModes.ts`/`videoOutputMapping.ts`/`videoLayerDefaults.ts`). Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~418 kB). Still uncommitted. LESSON FOR FUTURE EXTRACTION: before creating a new helper module, grep the existing `app/src/*.ts` (especially `uiModes.ts`, `videoOutputMapping.ts`, `videoLayerDefaults.ts`) — App.tsx still contains several constants/types that are stale duplicates of already-extracted ones; prefer importing the existing canonical copy over minting a new module.
- 2026-06-15 (Codex resumed `/goal`, JSX slice 20): Resolved the `WorkspaceChrome` fork by choosing the no-regression path: trim the unused `controlModeTabs`/edit-live-mixer props out of `app/src/components/WorkspaceChrome.tsx`, preserve the current topbar behavior including `Load Sample` and `Run Smoke`, and wire `WorkspaceChrome` into `App.tsx` as a pure JSX extraction. Do not reintroduce `ControlMode` tabs until there is a deliberate Control-workspace feature design. Verified with `pnpm --dir app build` (tsc + vite, green).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 21): Extracted the duplicated Live Stage / Touch Stage SVG renderer into `app/src/components/StagePreview2D.tsx`. The component owns only the reusable 2D stage drawing: stage objects, projector/output surfaces, fixture beams, fixture shapes, and selection hit targets. Parent panels still own selection bars, cue pads, touch flash controls, and fixture/projector actions. `App.tsx` is now ~17,223 lines (down from ~19,200). Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~413 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 22): Extracted the Setup Mapping viewport control strip into `app/src/components/MappingViewportControls.tsx`. This component owns only Tool/fixture-output-object readouts, cursor readout, fit/zoom/reset controls, snap controls, and layer visibility toggles; the editable SVG stage and mapping mutations remain in `App.tsx`. `App.tsx` is now ~17,149 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~414 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 23): Extracted the Setup Mapping stage-bounds + stage/view preset panel into `app/src/components/MappingStageConfigPanel.tsx`. The component owns only the bounds inputs, auto-fit/current-fit controls, stage map preset controls, view preset controls, and export buttons; persistence, validation, messages, and SVG/export payload logic remain in `App.tsx`. Removed the old DOM-query helper for view-preset button actions by passing the selected view id directly. `App.tsx` is now ~17,004 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~415 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 24): Extracted the Setup Mapping top filter strips into `app/src/components/MappingFilterStrips.tsx`. It owns the horizontal Groups and Types strips; App.tsx still owns group/type filter state and fixture row derivation. This also moved the last `mappingTypeGlyphClass` App.tsx usage into a component. `App.tsx` is now ~16,969 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~416 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 25): Extracted the Setup Mapping vertical tool rail into `app/src/components/MappingToolRail.tsx`. It owns stage tool buttons and the quick layer toggles, while App.tsx still owns mapping tool/layer state. Added explicit `aria-label`s to the compact letter buttons while preserving the existing DasLight-like dense rail. `App.tsx` is now ~16,913 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~417 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 26): Started the editable SVG stage split by extracting the stage reference-object layer into `app/src/components/MappingStageObjectsLayer.tsx`. It owns drawing and handles for stage objects (drag/rotate/resize hit targets), while App.tsx still owns object state, drag state, and mutation handlers. Existing `StageMap2D.tsx` was inspected but not wired because it diverges from the current App.tsx stage implementation and lacks current stage-object behavior. `App.tsx` is now ~16,849 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~417 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 27): Extracted the editable stage projector/output-surface layer into `app/src/components/MappingVideoSurfacesLayer.tsx`. It owns projector surface drawing, corner-pin handles, rotate handle, scale handle, and pointer hit targets; App.tsx still owns selected output state, mapping lookup/default fallback, warp corner math, and drag/mutation handlers. `App.tsx` is now ~16,772 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~418 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 28): Extracted the editable stage beam and GDTF geometry draw layers into `app/src/components/MappingBeamsLayer.tsx` and `app/src/components/MappingGeometryLayer.tsx`. These are render-only layers: App.tsx still owns fixture render rows, geometry row derivation, and layer visibility state. `App.tsx` is now ~16,737 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~418 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 29): Extracted the editable stage fixture + place-preview layer into `app/src/components/MappingFixturesLayer.tsx`. It owns fixture shapes, selection classes, yaw handle drawing, labels/levels, and the place-preview glyph; App.tsx still owns selected fixture sets, fixture row derivation, pan/rotate mode decisions, and drag/mutation callbacks. `App.tsx` is now ~16,606 lines. Verified with `pnpm --dir app build` (tsc + vite, green; main index chunk ~418 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 30): Extracted the editable stage SVG shell into `app/src/components/MappingEditableStageShell.tsx`. The component owns the SVG frame, grid/axes, cursor guide, snap lines, marquee rectangle, viewBox classes, and pointer/wheel event boundary; App.tsx still owns mapping state, stage coordinate math, drag handlers, export ref, and all child layers. `App.tsx` is now ~16,554 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingEditableStageShell.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~419 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 31): Extracted the Setup Mapping selected-projector correction panel into `app/src/components/MappingProjectorControlsPanel.tsx`. It owns the mini projector preview, output enable/blackout/window/sync buttons, Fit Object action, stage X/Z/Y/rotation controls, aspect mode/ratio controls, lens distortion, keystone, reset/clear-warp, and 16:9/4:3/1:1 presets. App.tsx still owns output state mutations, selected stage object state, and video output window commands. `App.tsx` is now ~16,316 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingProjectorControlsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~419 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 32): Extracted the Setup Mapping stage-object create/list/edit panel into `app/src/components/MappingStageObjectPanel.tsx`. It owns the Stage Objects draft form, object list, selected object transform/kind/label/color editor, delete action button, Pick/Add Inside buttons, and Line/Grid Here placement buttons; App.tsx still owns stage-object persistence, selected object state, fixture selection count, and layout/pick mutations. `App.tsx` is now ~16,143 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingStageObjectPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~420 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 33): Extracted the Setup Mapping fixture selection tools and filtered fixture list into `app/src/components/MappingFixtureSelectionPanel.tsx` (`MappingFixtureSelectionToolsPanel`, `MappingFixtureListPanel`). These components own the Selections count header, search field, Pick Visible/Duplicate/Remove/Clear Pick controls, selected-group assignment controls, filtered fixture list, and empty fixture message; App.tsx still owns fixture filtering, selection semantics, group parsing/application, and mutation handlers. `App.tsx` is now ~16,062 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingFixtureSelectionPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~421 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 34): Extracted the Setup Mapping selection action inspector and selected-fixture transform/GDTF inspector into `app/src/components/MappingSelectionActionsPanel.tsx` and `app/src/components/MappingFixtureInspectorPanel.tsx`. The first component owns selection highlight/solo/park toggles, snap nudging, multi-fixture layout/align/distribute/mirror/rotate/duplicate/remove/control/clear actions; the second owns the selected fixture X/Z/Y/Yaw editor, GDTF geometry summary rows/warnings, single-fixture flags, and Control/Patch/Duplicate/Remove shortcuts. App.tsx still owns all mutation handlers, selected fixture/geometry derivation, and workspace navigation state. `App.tsx` is now ~15,853 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingSelectionActionsPanel.tsx app/src/components/MappingFixtureInspectorPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~422 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 35): Extracted the Setup Mapping projector list/selected-projector wrapper into `app/src/components/MappingProjectorSelectionPanel.tsx`. It owns the Projectors header, output list, active-output styling, and selected-output handoff to `MappingProjectorControlsPanel`; App.tsx still owns selected output state and output mutation/window handlers. `App.tsx` is now ~15,836 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingProjectorSelectionPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~423 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 36): Extracted the Video Setup composition list/form into `app/src/components/VideoCompositionSetupPanel.tsx`. It owns composition cards, layer order controls, per-composition layer picker, remove composition button, composition draft label/layer picker, and Add Composition button; App.tsx still owns video snapshot state and composition mutation handlers. `App.tsx` is now ~15,762 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoCompositionSetupPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~423 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 37): Extracted the Video Setup output creation form into `app/src/components/VideoOutputCreatePanel.tsx`. It owns the Composition Output draft controls (label, kind, width/height, fade ms, Display monitor/fullscreen, transport endpoint, Add Output); App.tsx still owns draft signals and the add-output mutation. During this slice an old form closing tag briefly caused a JSX mismatch, then the Mapping panel and Video Setup closing structure was corrected and rebuilt green. `App.tsx` is now ~15,723 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoOutputCreatePanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~424 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 38): Extracted the per-output config editor into `app/src/components/VideoOutputConfigPanel.tsx`. It owns output label/kind/width/height, Display monitor/fullscreen, non-display endpoint, and Apply Output controls for each output row; App.tsx still owns the config draft map and apply mutation. `App.tsx` is now ~15,647 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoOutputConfigPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~424 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 39): Extracted the per-output Projector Map editor into `app/src/components/VideoOutputMappingPanel.tsx`. It owns mapping preset label/selection/actions, reset/clear warp/aspect presets, `ProjectorMapEditor`, output preview card, offset/scale/rotation/aspect/lens/keystone/corner-pin numeric controls; App.tsx still owns mapping preset persistence, preview state, and mapping mutation. Removed the now-unused App.tsx `ProjectorMapEditor` and aspect-mode imports. `App.tsx` is now ~15,237 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoOutputMappingPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~424 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 40): Extracted per-output enable/blackout/opacity/preview/window/remove actions into `app/src/components/VideoOutputActionsPanel.tsx`. It owns the output row button strip, including Display-only window/test-pattern/sync controls; App.tsx still owns the action handlers. `App.tsx` is now ~15,211 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoOutputActionsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~425 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 41): Extracted the remaining Video Setup output list wrapper into `app/src/components/VideoOutputListPanel.tsx`. It owns the output timeline list, per-output row header, routing select, and composition of `VideoOutputConfigPanel`, `VideoOutputMappingPanel`, and `VideoOutputActionsPanel`; App.tsx now only passes outputs/compositions/mapping presets/preview state and mutation handlers. `App.tsx` is now ~15,178 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoOutputListPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~426 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 42): Extracted the Video Control CPU preview/preview-status diagnostics block into `app/src/components/VideoPreviewDiagnosticsPanel.tsx`. It owns only the preview buttons, preview info text, layer queue diagnostics, and output decode-plan diagnostics; App.tsx still owns diagnostic polling, labels/classes, and video preview state. `App.tsx` is now ~15,609 lines in the current working tree. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoPreviewDiagnosticsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~426 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 43): Extracted the Video Control Render Plans, Backends, and I/O Plans status blocks into `app/src/components/VideoRuntimeStatusPanels.tsx`. The new components own only status display rows, empty states, and refresh/sync buttons; App.tsx still owns render-plan derivation, backend state classes, external transport sync, and polling. `App.tsx` is now ~15,521 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoRuntimeStatusPanels.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~427 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 44): Extracted the Video Control master blackout/opacity controls and output window/control list into `app/src/components/VideoControlOutputsPanel.tsx`. The components own only master controls, per-output status display, window action buttons, and output opacity/action buttons; App.tsx still owns video output mutations, window commands, render/window status derivation, and snapshot state. `App.tsx` is now ~15,437 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoControlOutputsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~428 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 45): Extracted the Video Control CPU preview image and source-add form into `app/src/components/VideoSourceCreatePanel.tsx`. It owns only the preview image fallback, source kind selector, layer label/path inputs, browse button, and Add Video Layer button; App.tsx still owns the draft signals, file picker, source validation, and layer creation. `App.tsx` is now ~15,408 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoSourceCreatePanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~429 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 46): Extracted the Video Control layer list/editor into `app/src/components/VideoLayerListPanel.tsx`. It owns layer naming, move/duplicate/metadata buttons, blend/enabled/solo controls, opacity/speed, transform, color, FX, playback, loop, BPM sync, cue-point buttons, fade-out, and remove buttons; App.tsx still owns all video layer mutations, state patch helpers, cue-point commands, and snapshot refresh. `App.tsx` is now ~14,766 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoLayerListPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~430 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 47): Extracted the Video Control timeline-video-automation create/edit panel into `app/src/components/VideoTimelineAutomationPanel.tsx`. It owns the video automation layer/param/curve/time/value form, the existing automation edit grid, save/remove buttons, and shared video-param option labels; App.tsx still owns selected draft signals, timeline video automation row derivation, draft synchronization, add/save/remove commands, and snapshot refresh. `App.tsx` is now ~14,535 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoTimelineAutomationPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~430 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 48): Extracted the Effects panel video-target settings into `app/src/components/VideoEffectTargetPanel.tsx`. It owns the video layer/param selector, low/high range inputs, and PositionWave video target position controls/source buttons; App.tsx still owns target-mode state, video target derivation, selected output/stage-object position helpers, and effect creation. `App.tsx` is now ~14,442 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/VideoEffectTargetPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~431 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 49): Extracted the Effects panel group target picker into `app/src/components/EffectGroupTargetPanel.tsx`. It owns the target group text input and active group chips; App.tsx still owns group parsing, group toggling, selected fixture group coupling, and effect target validation. `App.tsx` is now ~14,422 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/EffectGroupTargetPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~431 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 50): Extracted the Effects list into `app/src/components/EffectListPanel.tsx`. It owns effect summary text, move/enable/use-output-position/save/remove buttons, and the PositionWave video-target action visibility; App.tsx still owns effect ordering, enable state, preset persistence, removal, and output-position mutation. `App.tsx` is now ~14,393 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/EffectListPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~431 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 51): Extracted the Node Graph saved-graph list into `app/src/components/NodeGraphListPanel.tsx`. It owns graph summary rows, Wave/LFO/node/edge chips, enable/save/remove buttons, and empty-list hiding; App.tsx still owns graph target-label derivation, enable/save/remove commands, and the node graph form/preview. `App.tsx` is now ~14,375 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/NodeGraphListPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~431 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 52): Extracted the Node Graph editor shell into `app/src/components/NodeGraphEditorPanel.tsx`, composing the existing `NodeGraphListPanel`. It owns the Node Graph header, label/transform inputs, transform preview SVG, save/reset buttons, and graph list placement; App.tsx still owns graph construction, transform draft state, target labels, preset loading/saving, and graph enable/remove commands. `App.tsx` is now ~14,296 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/NodeGraphEditorPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~432 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 53): Extracted the Effects form final action controls into `app/src/components/EffectActionControlsPanel.tsx`. It owns light-target low/high inputs, phase, blend mode, and Add Effect button; App.tsx still owns effect target mode, selected fixture/video availability checks, draft state, and effect creation. `App.tsx` is now ~14,260 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/EffectActionControlsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~433 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 54): Extracted the Effects source controls and PositionWave stage picker into `app/src/components/EffectSourceControlsPanel.tsx`. It owns shape/period controls, the wave stage SVG map, origin/direction/video-target handles, wave source shortcut buttons, and wave numeric inputs; App.tsx still owns coordinate derivation, drag handlers, fixture/surface/object projections, effect target mode, and selected output lookup. `App.tsx` is now ~14,081 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/EffectSourceControlsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~435 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 55): Extracted the Output panel lighting runtime controls into `app/src/components/LightingRuntimeControlsPanel.tsx`. It owns Lighting Master, Submasters, DMX/All blackout controls, BPM/beat clock controls, and MIDI Clock input scan/connect/disconnect; App.tsx still owns DMX output config/routes/test frame, MIDI Feedback/Control mapping, OSC mapping, remote control, telemetry, clock mutations, and snapshot refresh. During the slice an obsolete closing `</div>` from the old inline Clock container briefly caused a JSX mismatch; the extra close was removed and rebuilt green. `App.tsx` is now ~14,021 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/LightingRuntimeControlsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~436 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 56): Extracted the Output panel MIDI Feedback and MIDI Control mapping UI into `app/src/components/MidiControlMappingPanel.tsx`. It owns the feedback output select/connect/send/auto toggle, MIDI message/action draft form, conditional cue/effect/node/layer/output/mapping-preset selectors, low/high ranges, mapping command buttons, and saved mapping list. App.tsx still owns the mapping state, learned-input handlers, persistence, connection commands, target-label derivation, OSC mapping, remote control, and telemetry. `App.tsx` is now ~13,781 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MidiControlMappingPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~441 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 57): Extracted the Output panel OSC Input mapping UI into `app/src/components/OscControlMappingPanel.tsx`. It owns OSC bind/port/start/stop controls, address/action/attribute draft fields, conditional cue/effect/node/layer/output/mapping-preset selectors, low/high ranges, learn/add/load/save buttons, and saved mapping list. App.tsx still owns OSC state, learned-input handling, persistence, server commands, target-label derivation, Web Remote, and telemetry. `App.tsx` is now ~13,558 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/OscControlMappingPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~443 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 58): Extracted the Web Remote controls into `app/src/components/RemoteControlPanel.tsx`. It owns remote bind IP/port inputs and Start/Stop buttons; App.tsx still owns the WebSocket server command handlers, remote state, and telemetry placement. This leaves the Output aside mostly as component composition plus the existing `EngineTelemetryPanel` call. `App.tsx` is now ~13,542 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/RemoteControlPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~443 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 59): Extracted the Control/Faders Cue management UI into `app/src/components/CueManagementPanel.tsx`. It owns the cue store form, capture preview placement, Back/GO/Pause controls, fade meter, editable cue list, cue update/copy/remove buttons, and cue timeline placement chips; App.tsx still owns cue capture scope derivation, cue metadata drafts, cue commands, timeline placement mutations, and snapshot state. `App.tsx` was reduced to roughly ~13,408 lines at this point. Verified with `git diff --check -- app/src/App.tsx app/src/components/CueManagementPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 60): Extracted the Timeline transport/audio/snap/cue-event editor into `app/src/components/TimelineCueEventsPanel.tsx`. It owns the timeline header, transport controls, playhead range, `TimelineOverview`, audio analysis display, snap controls, cue-event add form, and cue-event edit list; App.tsx still owns timeline state, audio analysis commands, snap calculations, timeline event drafts, and the remaining lighting automation editor below it. `App.tsx` is now ~13,257 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TimelineCueEventsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~446 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 61): Extracted the Timeline lighting automation editor into `app/src/components/TimelineLightingAutomationPanel.tsx`. It owns the Add Automation form and existing lighting automation edit list; App.tsx still owns snap calculations, selected attribute state, automation draft state, fixture attribute lookup, and engine commands. Verified with `git diff --check -- app/src/App.tsx app/src/components/TimelineLightingAutomationPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~447 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 62): Extracted the Fader selected-fixture/group operation strip into `app/src/components/FaderFixtureControlPanel.tsx`. It owns the group Submaster banner, preset/load/duplicate/remove buttons, fixture or group Highlight/Solo/Park controls, Clear Flags, and selected-fixture Transform editor; App.tsx still owns the mutation commands, selected fixture/group state, and the lower attribute editor/fader grid. `App.tsx` is now ~12,954 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/FaderFixtureControlPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~448 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 63): Extracted the lower Fader raw channel grid into `app/src/components/FaderGridPanel.tsx`. It owns the visible attribute fader loop, fixture/group write routing boundary, DMX resolution/offset labels, and empty states; App.tsx still owns category selection, specialized Dimmer/Position/Color/Wheel/Optics panels, value derivation, and `setAttribute`/`setGroupAttribute` commands. `App.tsx` is now ~12,927 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/FaderGridPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~448.68 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 64): Extracted the Fader attribute editor shell/category rail into `app/src/components/FaderAttributeEditorPanel.tsx`. It owns the `.attributeEditor` frame, category rail buttons, disabled state, and body slot; App.tsx still owns all specialized attribute panels and the active-category/control derivation. `App.tsx` is now ~12,915 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/FaderAttributeEditorPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~448.82 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 65): Extracted the shared attribute category rail into `app/src/components/AttributeCategoryRail.tsx` and reused it from both `FaderAttributeEditorPanel.tsx` and the Touch fixture panel. This removes the duplicated control-category button loop while preserving the Fader `.attributeCategoryRail` and Touch `.touchAttributeCategoryRail` classes. `App.tsx` is now ~12,908 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/AttributeCategoryRail.tsx app/src/components/FaderAttributeEditorPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~448.77 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 66): Extracted the Touch fixture/group picker into `app/src/components/TouchFixturePickerPanel.tsx`. It owns the Touch group chip scroller, filtered fixture button list, selected-fixture classing, and empty state; App.tsx still owns selected group/fixture state, fixture filtering, and action/attribute controls below the picker. `App.tsx` is now ~12,881 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchFixturePickerPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~449.13 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 67): Extracted the Touch fixture/group guard action row into `app/src/components/TouchFixtureActionsPanel.tsx`. It owns selected-fixture Highlight/Solo/Park buttons, selected-group Highlight/Solo/Park buttons, Clear Flags, labels, and disabled states; App.tsx still owns the actual flag mutation commands and the Touch attribute controls below. `App.tsx` is now ~12,848 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchFixtureActionsPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~449.82 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 68): Extracted the Touch Dimmer slider and Out/Half/Full/Flash controls into `app/src/components/TouchDimmerControlPanel.tsx`. It owns the touch dimmer range UI, quick-level buttons, momentary flash button classing, and pointer/key event forwarding; App.tsx still owns dimmer target derivation, value writes, and flash restore state. `App.tsx` is now ~12,826 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchDimmerControlPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~450.28 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 69): Extracted the Touch Pan/Tilt pad into `app/src/components/TouchPanTiltPad.tsx`. It owns the Touch pan/tilt SVG-like pad frame, limit overlay, current target marker, pointer capture, and pointer move forwarding; App.tsx still owns Pan/Tilt source-value math, fixture/group writes, and selected-position control derivation. `App.tsx` is now ~12,809 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchPanTiltPad.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~450.48 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 70): Extracted the Touch color swatch palette into `app/src/components/TouchColorPalettePanel.tsx`. It owns the touch color grid, swatch buttons, swatch background color style, title, and aria labels; App.tsx still owns the palette source and fixture/group color write command. `App.tsx` is now ~12,798 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchColorPalettePanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~450.58 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 71): Extracted the Touch generic attribute grid into `app/src/components/TouchGenericAttributeGrid.tsx`. It owns the fallback touch attribute rows, channel/attribute labels, range sliders, and formatted output values; App.tsx still owns visible-control derivation, current value lookup, and control writes. `App.tsx` is now ~12,785 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchGenericAttributeGrid.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~450.75 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 72): Extracted the Touch Remote panel into `app/src/components/TouchRemotePanel.tsx`. It owns the remote status/header, URL display, bind IP/port controls, Start/Stop buttons, Touch BPM Set/Tap controls, and submaster sliders; App.tsx still owns WebSocket remote commands, BPM mutations, and group submaster writes. `App.tsx` is now ~12,733 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchRemotePanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~451.20 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 73): Extracted the Touch Video deck panel into `app/src/components/TouchVideoPanel.tsx`. It owns the Touch Video section header, empty state, per-layer deck, progress meter, play/pause/cue/rev/fwd controls, opacity/speed/seek sliders, enable/solo/loop toggles, Add Cue, and cue-point jump buttons; App.tsx still owns video layer state persistence and cue-point commands. `App.tsx` is now ~12,555 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchVideoPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~451.42 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 74): Extracted the Touch Fixture section shell into `app/src/components/TouchFixturePanel.tsx`. It composes the Touch fixture picker, guard action row, shared attribute category rail, child attribute controls, and empty state; App.tsx still owns fixture/group selection state, control derivation, dimmer/pan/color/generic value writes, and command handlers. `App.tsx` is now ~12,534 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/TouchFixturePanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~452.53 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 75): Extracted the Fader color/gobo wheel panel composition into `app/src/components/FaderWheelSlotPanels.tsx`. It owns the paired Color/Gobo wheel conditional rendering and forwards the existing wheel media/function callbacks; App.tsx still owns wheel entry derivation, current DMX writes, and channel-function application. `App.tsx` is now ~12,517 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/FaderWheelSlotPanels.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~453.30 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 76): Extracted the Fader auxiliary attribute panel composition into `app/src/components/FaderAuxiliaryAttributePanels.tsx`. It owns the Wheel/Optics/Category Quick/GDTF Function conditional panel stack and forwards existing entry/function callbacks; App.tsx still owns entry derivation, optics math, quick-look application, and value writes. `App.tsx` is now ~12,490 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/FaderAuxiliaryAttributePanels.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~455.33 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 77): Extracted the Fader primary attribute panel composition into `app/src/components/FaderPrimaryAttributePanels.tsx`. It owns the Dimmer/Position/Color conditional panel stack and forwards the existing limits, pan/tilt, color, favorite, and pointer/key handlers; App.tsx still owns selected controls, limits drafts, color/position memo state, and engine write commands. `App.tsx` is now ~12,466 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/FaderPrimaryAttributePanels.tsx app/src/components/FaderAuxiliaryAttributePanels.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~458.75 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 78): Extracted the Setup GDTF load controls and loaded-profile summary into `app/src/components/ProfileLoadPanel.tsx` and `app/src/components/LoadedProfileSummaryPanel.tsx`. They own the GDTF path/GDTF Share controls and the selected profile's mode DMX map, channel-function list, and geometry summary; App.tsx still owns profile import/download commands, selected mode state, and patch form state below the summary. `App.tsx` is now ~12,352 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/ProfileLoadPanel.tsx app/src/components/LoadedProfileSummaryPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~459.65 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 79): Extracted the Setup Custom Profile editor into `app/src/components/CustomProfileEditorPanel.tsx`. It owns the custom profile maker/name/mode/attribute inputs, template buttons, draft row editor, DMX cell map, footprint preview, and create/save/load controls; App.tsx still owns draft parsing/analysis, row mutation helpers, and custom profile create/save/load commands. `App.tsx` is now ~12,182 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/CustomProfileEditorPanel.tsx app/src/components/ProfileLoadPanel.tsx app/src/components/LoadedProfileSummaryPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~460.71 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 80): Extracted the Setup DMX patch grid/list and universe overview into `app/src/components/DmxPatchMapPanel.tsx`. It owns the Grid/List toggle, universe selector, DMX address grid, patched-fixture list, summary row, and compact universe tracks; App.tsx still owns DMX map calculation, next-free selection, address-cell click routing, and fixture selection state. `App.tsx` is now ~12,055 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/DmxPatchMapPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~461.35 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 81): Extracted the Setup patch execution form into `app/src/components/PatchFixtureFormPanel.tsx`. It owns fixture label/universe/address/count/stride/layout/position/rotation/group inputs, footprint conflict summary, Patch Fixture(s), Next Free, and profile warnings; App.tsx still owns selected profile/mode, normalized patch calculations, address validation, and patch command execution. `App.tsx` is now ~11,944 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/PatchFixtureFormPanel.tsx app/src/components/DmxPatchMapPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~462.63 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 82): Extracted the Setup fixture group tabs and filtered fixture list into `app/src/components/SetupFixtureListPanel.tsx`. It keeps the existing order by accepting the DMX patch map as a child between the group overview and fixture list; App.tsx still owns fixture filtering, selected group/fixture state, and the DMX patch-map calculations. Verified with `git diff --check -- app/src/App.tsx app/src/components/SetupFixtureListPanel.tsx app/src/components/SetupFixtureEditorPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 83): Extracted the selected Setup fixture editor into `app/src/components/SetupFixtureEditorPanel.tsx`. It owns Use Profile/Duplicate, patch draft inputs, group assignment, Dimmer/Movement limit editor, and 2D Mapping transform/layout controls; App.tsx still owns draft signals, limit normalization, pointer math, Tauri commands, and fixture layout mutations. `App.tsx` is now ~11,207 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/SetupFixtureListPanel.tsx app/src/components/SetupFixtureEditorPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~464.79 kB).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 84): Extracted the Setup 2D Mapping SVG layer composition into `app/src/components/MappingStageLayersPanel.tsx`. It owns the Stage Objects, Projectors, Beams, Geometry, and Fixtures layer stack inside `MappingEditableStageShell`; App.tsx still owns stage visibility flags, selection state, drag predicates, output mapping lookup, and pointer routing. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingStageLayersPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green).
- 2026-06-15 (Codex resumed `/goal`, JSX slice 85): Extracted the Setup 2D Mapping right selection sidebar into `app/src/components/MappingSelectionSidebarPanel.tsx`. It composes fixture selection tools, stage-object editor, selection actions, fixture inspector/list, and projector selection; App.tsx still owns all mutation commands, draft signals, selected object/output/fixture state, and workspace tab switches. `App.tsx` is now ~11,169 lines. Verified with `git diff --check -- app/src/App.tsx app/src/components/MappingStageLayersPanel.tsx app/src/components/MappingSelectionSidebarPanel.tsx` (only the existing LF->CRLF warning) and `pnpm --dir app build` (tsc + vite, green; main index chunk ~470.38 kB).
- 2026-06-14 (Claude takeover, frontend slice 7): Extracted fixture visual-kind/label helpers into new `app/src/fixtureVisuals.ts` (moved the `MappingFixtureVisualKind` type with them; App.tsx's two interfaces that reference it now import the type). `App.tsx` now ~18,546 lines (~660 lines moved out across seven slices into eight helper modules). Verified with `pnpm --dir app build` (tsc + vite, green). Still uncommitted. NOTE: the remaining easily-separable pure helpers are now mostly small scattered label functions (diminishing returns). The next high-value step is extracting whole JSX panels (Setup 2D Mapping, Output/Telemetry) — a different, higher-risk slice class that threads SolidJS signals as props and should be reviewed deliberately rather than done as rapid autonomous micro-slices.

## Local Smoke Asset

- `samples/phase1-mini-spot.fixture` is a small custom fixture profile for manual Phase 1 checks.
- `samples/phase1-mini-show.ry` is a self-contained project sample with one patched fixture, one cue, Art-Net U0 output, and one 16:9 mapped display output.
- `samples/README.md` documents the manual smoke flow and expected channel footprint.
- Use the topbar `Load Sample` button to load the embedded mini show directly, use `Run Smoke` to load and trigger the first cue in one step, or load `samples/phase1-mini-spot.fixture` from Setup -> Profiles to build the same smoke patch manually at `U0 A1`.
- Expected footprint:
  - Ch 1: `Dimmer` 8-bit
  - Ch 2-4: `ColorRed`, `ColorGreen`, `ColorBlue` 8-bit
  - Ch 5-6: `Pan` 16-bit
  - Ch 7-8: `Tilt` 16-bit
- Good smoke path: patch one fixture, set Dimmer to full, set RGB, move Pan/Tilt, confirm DMX Raw updates on U0, then send an Art-Net test/loopback frame if a receiver is available.
- The project sample also has an Engine smoke test that loads the `.ry`, triggers its cue, and verifies rendered DMX preview values.

## Current Rough Edges

- 2026-06-23 (Codex UI frame fix): Fixed the app-level scroll regression by wrapping `WorkspaceChrome` as one grid row, keeping `.app` at `100dvh`, and removing setup-tab `scrollIntoView()` that could move the document instead of only focusing a panel. Verification: `pnpm --dir app build` passed; Chrome/Playwright layout checks at 2048x1129 and 1366x768 showed `document.scrollHeight === document.clientHeight` and `canWindowScrollY: false` across Setup Patch, Setup Mapping, Control, and Touch. Long panels still use internal `.panel` scrolling. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 fan preset slice): Added `samples/front-pan-fan.effect` as a static PositionWave Fan preset for the `Front` group `Pan` attribute (`Triangle`, `speed: 0.0`, `wavelength: 6.0`, centered 24576..40960). Wired embedded aliases `fan`, `front-pan-fan`, and `front_pan_fan`, added Fan to the Effects sample preset selector, and extended sample preset validation/re-target tests. Verification: `cargo fmt` (only existing `C:\Users\kouty` canonicalize warning), `cargo test -p rayard sample_effect`, and `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 circle bundle slice): Added `samples/front-circle-pan.effect` + `samples/front-circle-tilt.effect` as a paired Circle sample (`Pan` Sine + `Tilt` Cosine, 2000ms / 4 beats, centered 24576..40960). Added the `load_sample_effect_bundle` Tauri command for embedded multi-effect samples, wired `circle` / `front-circle` / `front_circle`, and added Circle to the Effects sample selector. The Target button is disabled for Circle because it is a Pan/Tilt pair rather than a single-attribute preset. Verification: `cargo fmt` (only existing `C:\Users\kouty` canonicalize warning), `cargo test -p rayard sample_effect` (4 tests passed), and `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 ball preset slice): Added `samples/front-dimmer-ball.effect` as a radial PositionWave Ball preset for the `Front` group `Dimmer` attribute (`Sine`, zero direction vector for radial fallback, `wavelength: 3.0`, 4-beat sync). Wired embedded aliases `ball`, `front-dimmer-ball`, and `front_dimmer_ball`, added Ball to the Effects sample selector, and extended sample preset validation/re-target tests. Verification: `cargo fmt` (only existing `C:\Users\kouty` canonicalize warning), `cargo test -p rayard sample_effect` (4 tests passed), `pnpm --dir app build`, and `git diff --check -- app/src-tauri/src/main.rs app/src/App.tsx samples/front-dimmer-ball.effect` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 random/perlin preset slice): Confirmed Engine already evaluates all LFO shapes, including deterministic stepped `Random` and smooth periodic `Perlin`, and UI shape selection already exposed them. Added `samples/front-dimmer-random.effect` and `samples/front-dimmer-perlin.effect` for direct sample loading (`Random` = 1-beat sparkle, `Perlin` = 8-beat smooth drift), wired embedded aliases, added both to the Effects sample selector, and extended sample preset validation/re-target tests. Verification: `cargo fmt` (only existing `C:\Users\kouty` canonicalize warning), `cargo test -p rayard sample_effect` (4 tests passed), `pnpm --dir app build`, and `git diff --check -- app/src-tauri/src/main.rs app/src/App.tsx samples/front-dimmer-random.effect samples/front-dimmer-perlin.effect` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 sACN output UI slice): Confirmed the Rust sACN path already resolves blank/`auto`/`multicast` targets through the standard `239.255.H.L` multicast address for the selected universe. Added a compact sACN multicast preview/action row to `DmxOutputConfigPanel`, including the derived address and a one-click `multicast` target shortcut. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 effect preset sharing slice): Consolidated file-loaded `.effect` imports onto the same `add_effect_preset_to_engine` path used by embedded sample presets, including current-target retargeting and disabled-state preservation. Added a JSON round-trip test for a PositionWave preset that targets both lighting and video (`VideoParam::ColorHueDeg`) and preserves target position, clock sync, blend mode, low/high, and enabled state. Verification: `cargo fmt` (only existing `C:\Users\kouty` canonicalize warning), `cargo test -p rayard effect_preset`, and `cargo test -p rayard effect_summary_serializes_position_wave_preset_with_shared_video_target` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 fixture preset group-load UI slice): Connected the existing Tauri `.preset` bulk-load commands to the Control/Faders preset row. `Load Group Preset` applies a selected fixture preset to compatible fixtures in the active group, while `Load Matching Preset` applies it to all patched compatible fixtures and reports applied/skipped counts through `FixturePresetGroupLoadResult`. Verification: `pnpm --dir app build`, `cargo test -p rayard fixture_preset`, and `cargo test -p rayard preset_validation` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 fixture preset bulk-load polish): Moved the Fader preset action row outside the selected-fixture-only block, so `Load Group Preset` remains available when a group is selected even if no individual fixture editor is active. Single-fixture Save/Load stays disabled without a selected fixture; `Load Matching Preset` stays available when there are patched/filtered fixtures. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 fixture preset matching-load condition): Split `Load Matching Preset` enablement from the current group-filtered fixture count. The button now uses the total patched fixture count, matching the Tauri command's all-patched-compatible scope, while `Load Group Preset` remains gated by the selected group's fixture count. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 Serial DMX output hint slice): Added protocol-specific serial output hints in `DmxOutputConfigPanel`. Enttec USB PRO and DMXKing ultraDMX are labeled as firmware/device-timed paths, while Enttec Open DMX is flagged as host-timed with OS-dependent break timing so live-critical rigs can prefer PRO/DMXKing. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping hotkey slice): Added Setup -> Mapping layer hotkeys through `hotkeyHelpers` and the existing `handleControlKeyDown` path. `L/B/G/V/O` toggle labels, beams, GDTF geometry, projector surfaces, and stage objects; `Shift+5` toggles fixture level readouts. The shortcuts respect the existing editable-target guard and the toolbar titles now advertise the keys. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping viewport hotkey slice): Added Setup -> Mapping viewport hotkeys through `hotkeyHelpers` and the existing guarded keydown path. `F` fits all visible stage items, `Shift+F` fits the current selection, `+`/`-` zoom the 2D stage, and `0` resets the viewport; the viewport button titles now advertise the same keys. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping selection hotkey slice): Added Setup -> Mapping selection-transform hotkeys without moving ownership out of `App.tsx`. `1/2/3` trigger line/grid/circle layouts, `X/Z` align on the matching axis, `Shift+X`/`Shift+Z` distribute on the matching axis, and `[`/`]` rotate the selected fixtures by -15/+15 degrees. The action buttons now expose matching tooltip titles. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping advanced selection hotkey slice): Extended Setup -> Mapping selection-transform hotkeys for the remaining visual placement actions. `Shift+1` / `Shift+2` arrange selected fixtures on the selected stage object as line/grid, `Shift+[` / `Shift+]` mirror the selection across X/Z, and `4` flips the selection 180 degrees. The corresponding action buttons now expose matching tooltip titles. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping selection management hotkey slice): Added Setup -> Mapping selection-management hotkeys through `hotkeyHelpers` while keeping selection ownership in `App.tsx`. `Ctrl+A` picks all fixtures currently visible under the mapping filters, and `Ctrl+Shift+A` clears the current mapping pick. Pick/Clear now report concise status messages, and the corresponding sidebar buttons expose matching tooltip titles. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping stage-object pick hotkey slice): Added Setup -> Mapping stage-object pick hotkeys for selecting fixtures inside the selected reference object. `I` replaces the current pick with fixtures inside the selected object, and `Shift+I` adds those fixtures to the current pick; both reuse the existing `pickFixturesInsideSelectedStageObject` path and now have matching Stage Objects button tooltips. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping selection flag hotkey slice): Added Setup -> Mapping selection flag hotkeys for live-oriented fixture operations from the 2D map. `Q` toggles Highlight, `W` toggles Solo, and `E` toggles Park for the current mapping pick, using the same all-selected-clear behavior as the sidebar buttons. The Selection Flags buttons now expose matching tooltip titles. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 2D mapping stage-object count slice): Added fixture-inside counts to the Setup -> Mapping Stage Objects sidebar. `App.tsx` now derives a `stageObjectFixtureCounts` memo from the existing rotated-object containment logic and passes it through `MappingSelectionSidebarPanel` to `MappingStageObjectPanel`, so each object row and the selected-object header show how many patched fixtures are inside the reference object. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 projector correction readout slice): Added shared projector correction readouts for aspect/ratio, lens distortion, keystone, and corner-pin warp. `videoOutputMapping.ts` now exposes `cornerWarpAmount` and `mappingCorrectionReadout`; `VideoOutputMappingPanel` shows native output ratio plus correction/stage readouts, and Setup -> Mapping projector controls use the same correction summary. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 projector correction control slice): Added partial reset controls and full aspect preset coverage to projector mapping UI. Both `VideoOutputMappingPanel` and Setup -> Mapping projector controls now expose `Clear Lens/Key`, `Clear Corners`, and all `videoOutputAspectPresets` (`16:9`, `16:10`, `4:3`, `1:1`, `9:16`, `21:9`, `2.39:1`) in addition to full reset, clear warp, and output-native ratio. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 projector corner reset slice): Added per-corner reset controls to `ProjectorMapEditor`, so TL/TR/BR/BL corner pins can be returned to zero individually from both Video Output mapping and Setup -> Mapping projector controls. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 viewport containment slice): Locked the main Rayard shell to the viewport with a fixed `.app` root, disabled body overscroll, and compacted the header/layout/panel spacing so the app itself does not become a vertical scroll surface. Long fixture/profile/control content should continue to scroll inside its owning panel. Verification: `pnpm --dir app build` passed. A temporary Vite server was started and stopped for local inspection; Chrome headless screenshot output was not reliable in this desktop session.
- 2026-06-23 (Codex Phase 2 output diagnostics extraction slice): Extracted the Output panel's DMX test frame, DMX route list, and engine telemetry wiring into `OutputDiagnosticsPanel`, preserving `App.tsx` ownership of state/commands while reducing JSX in the main app body. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 sample effect preset panel slice): Moved sample effect preset metadata into `SampleEffectPresetPanel` and replaced the cramped Effects header select/buttons with a dense preset grid showing LFO/Wave/Bundle family, target attribute, descriptions, and per-preset Load/Target actions. Circle remains non-retargetable while other samples still use the current target override path. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 effect clock preset slice): Added clock-sync preset controls to `EffectSourceControlsPanel` for LFO and PositionWave sources, wiring `clock_sync: { beats }` into both add-effect request paths while keeping manual ms/free-speed mode available. PositionWave also gained quick speed and wavelength presets, and `EffectListPanel` now shows synced beat counts. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 2 waveform picker slice): Added visual waveform preset buttons to `EffectSourceControlsPanel` for Sine/Cos/Triangle/Saw/Square/Random/Perlin, keeping the existing select in sync while making LFO and PositionWave source selection faster and more DasLight-like. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 Node Graph clock-sync slice): Node Graph creation now carries the current effect source clock-sync setting into LFO/PositionWave source nodes instead of dropping it to `null`; the editor preview and saved graph list also show the sync beat count. This keeps Node Graph sources aligned with regular LFO/PositionWave effect creation and the shared BPM clock. UI guardrail: Rayard should behave like a fixed one-screen lighting desk; app/window-level scrolling is a regression, and only dense local panes such as libraries, profile details, and long lists should scroll internally. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 viewport regression harness): Added `app/scripts/check-viewport-containment.mjs` plus the `check:viewport` package script. The script starts Vite when needed, launches local Chrome/Edge/Chromium through CDP without adding dependencies, and checks Setup Library/Profiles/Patch/Mapping/Output plus Control and Touch at 1366x768 and 2048x1129. It fails if the document/app shell can scroll horizontally or vertically; local dense panes may still scroll internally. Verification: `node scripts/check-viewport-containment.mjs`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, and focused `git diff --check` passed. Use `CHROME_PATH=...` if Chrome/Edge is not auto-detected. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 effect stack readability slice): Reworked `EffectListPanel` from a long slash-delimited summary line into compact status chips for on/off, LFO/Wave, attribute, shape, timing/sync, range, target, and blend mode, plus a small empty state. The grid uses `minmax(0, 1fr)` and ellipsis-constrained chips so the effect stack stays readable without widening the one-screen desk layout. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 effect duplicate slice): Added `duplicate_effect` as a Tauri command backed by the existing `effect_summary_to_preset` -> `add_effect_preset_to_engine` path, relabeling the new effect as `<label> Copy` while preserving target, timing/clock sync, range, blend mode, and enabled state. `EffectListPanel` now exposes a `Duplicate` action, with action buttons wrapped in `.effectItemActions` so the extra command does not widen the one-screen layout. Verification: `cargo fmt`, `cargo test -p rayard duplicate_effect_copies_lfo_preset_details_into_new_stack_item`, `cargo test -p rayard effect_preset`, `cargo test -p rayard sample_effect`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 effect draft reuse slice): Added `Use Draft` to `EffectListPanel`, letting an existing EffectSummary repopulate the Effects form for fast variation creation. It restores LFO/Wave type, waveform, clock sync, timing, range, phase, blend, wave origin/direction/speed/wavelength, and the primary fixture/group/video target. Current form limitation: mixed lighting+video effects cannot be fully represented at once, so the draft loader restores the primary target and reports that limitation in the status message. Also fixed the Add Effect disabled condition so group targets no longer require an actively selected fixture. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 effect update-from-draft slice): Added Engine/Tauri/UI support for updating an existing effect from the Effects form instead of only creating a variation. `EngineCommand` now supports `UpdateLfoEffect` and `UpdatePositionWaveEffect`, replacing the runtime effect body while preserving effect id, stack order, and enabled state. The Effects form enters edit mode from `Use Draft`; submit becomes `Update Effect <id>` with a compact `Cancel Edit` action so the one-screen desk layout is not widened or made taller than the viewport. The same request builder now handles Add and Update, including the previous group-target guard fix. Verification: `cargo fmt`, `cargo test -p engine update_lfo_effect_preserves_id_order_and_enabled_state`, `cargo test -p rayard duplicate_effect_copies_lfo_preset_details_into_new_stack_item`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 effect update hardening slice): Added direct PositionWave update coverage with `update_position_wave_effect_preserves_id_order_and_enabled_state`, matching the LFO guarantees for id/order/enabled preservation while replacing origin/direction/speed/wavelength/clock/range/blend details. The Effects form now shows a compact `Editing <label>` chip in the submit row so users can see which stack item is being updated without adding a new panel row. `vite.config.ts` now splits Effects UI modules into an `effect-panels` chunk (~27.90 kB), reducing the main `index` chunk from ~495.61 kB to ~467.44 kB while keeping the one-screen viewport check green. Verification: `cargo fmt`, `cargo test -p engine update_position_wave_effect_preserves_id_order_and_enabled_state`, `cargo test -p engine update_lfo_effect_preserves_id_order_and_enabled_state`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 mixed lighting/video effect target slice): Added a compact `Link video layer` toggle to the Effects form for fixture/group targets. When enabled, the same LFO/PositionWave request now carries both the lighting target and a video layer param target, so one shared effect source can drive fixture attributes and VJ layer params together. `Use Draft` now restores mixed fixture/group + video targets instead of dropping the video side, and sample-preset target overrides also inherit the linked video target through the existing request builder. Added `lfo_effect_can_target_fixture_and_video_layer_from_same_source` to prove the engine drives DMX Dimmer and video Opacity from one LFO instance. Verification: `cargo fmt`, `cargo test -p engine lfo_effect_can_target_fixture_and_video_layer_from_same_source`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 mixed position-wave coverage slice): Added `position_wave_effect_can_span_fixture_and_video_layer_positions`, proving one PositionWave instance can evaluate fixture coordinates and video layer projection coordinates together. The test places a fixture at the wave origin and a video layer target one unit along X with a 2m wavelength/Square phase, then verifies DMX Dimmer hits high while video Opacity hits low from the same wave source. This covers the requirement that stage-coordinate waves can pass through both lighting fixtures and projection surfaces. Verification: `cargo fmt`, `cargo test -p engine position_wave_effect_can_span_fixture_and_video_layer_positions`, `cargo test -p engine lfo_effect_can_target_fixture_and_video_layer_from_same_source`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 shared effect sample preset slice): Added `samples/front-dimmer-shared.effect` and registered it as the `shared` sample preset. It is a safe two-beat Front/Dimmer LFO when loaded directly, and when users enable `Link video layer` then press `Target Selected`, the existing target override path turns it into a mixed lighting + VJ layer effect without hard-coding a video layer id into the sample. `SampleEffectPresetPanel` now shows a compact `Shared` card (`Dimmer+Video`). Verification: `cargo fmt`, `cargo test -p rayard sample_effect_presets_are_valid_for_phase1_mini_show`, `cargo test -p rayard embedded_sample_effect_presets_can_retarget_current_effect_target`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 sample project VJ layer slice): Extended `samples/phase1-mini-show.ry` with a `Stage NDI` video layer routed into the existing Main composition and Front Projector output. The `White Hit` cue now targets both the Mini Spot values and the video layer state (`playing: true`, full opacity, 2-bar BPM sync), so the built-in sample demonstrates one cue triggering lighting plus VJ and gives the `Shared` effect preset an immediate layer to link against. Verification: `node -e "JSON.parse(...phase1-mini-show.ry...)"`, `cargo fmt`, `cargo test -p rayard phase1_smoke_project_sample`, `cargo test -p rayard sample_effect`, and focused `git diff --check` passed. UI/CSS was not touched, so the viewport harness was intentionally not rerun in this slice. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 shared preset one-click target slice): Tightened the `Shared` sample preset UX so its `Target` / `Target Selected` action now forces a video-layer target in addition to the current fixture/group lighting target, without requiring users to manually enable `Link video layer` first. `SampleEffectPresetPanel` now receives per-preset target errors, so only `Shared` requires both a lighting target and an available video layer; normal single-target presets, generic `Load Target`, and Node Graph target building keep their previous behavior. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 sample timeline integration slice): Added two `Lighting` timeline cue events to `samples/phase1-mini-show.ry` for the `White Hit` cue at 0ms and 4000ms. Because that cue already carries both fixture targets and the `Stage NDI` video-layer target, the built-in sample now demonstrates a shared timeline triggering lighting plus VJ from one cue event. The second event intentionally establishes a 4s runtime timeline duration, matching the Engine's duration rule of deriving duration from the latest event/automation/media timestamp rather than trusting the saved `duration_ms` field alone. Added `phase1_smoke_project_sample_timeline_event_triggers_light_and_video` to verify timecode sync at 0ms activates the cue, drives DMX, and starts the video layer. Verification: `node -e "JSON.parse(...phase1-mini-show.ry...)"`, `cargo fmt`, `cargo test -p rayard phase1_smoke_project_sample`, `cargo test -p rayard sample_effect`, and focused `git diff --check` passed. UI/CSS was not touched, so the viewport harness was intentionally not rerun in this slice. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 sample shared automation slice): Added synchronized timeline automations to `samples/phase1-mini-show.ry`: fixture 1 `Dimmer` fades linearly from 65535 at 0ms to 0 at 4000ms, and `Stage NDI` `Opacity` fades from 1.0 to 0.0 over the same range. This gives the built-in sample an explicit lighting track plus video track driven by the same timeline position. Added `phase1_smoke_project_sample_timeline_automations_share_position`, which seeks to 2000ms and verifies DMX dimmer byte 128 plus video opacity around 0.5. Verification: `node -e "JSON.parse(...phase1-mini-show.ry...)"`, `cargo fmt`, `cargo test -p rayard phase1_smoke_project_sample`, `cargo test -p rayard sample_effect`, and focused `git diff --check` passed. UI/CSS was not touched, so the viewport harness was intentionally not rerun in this slice. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 smoke report integration slice): Extended `Phase1SmokeReport` and `Phase1SmokeReportPanel` so topbar `Run Smoke` now reports the mini-show's integrated state, not only U0 A1-A8 DMX bytes. The report now includes timeline event/automation/video-automation counts, timeline duration, cue-triggered video layer label/playing/opacity, and a 2000ms timeline automation probe (`Dimmer` byte 128 plus video opacity around 50%). The panel renders these as compact three-column chips to preserve the fixed one-screen desk layout. Verification: `cargo fmt`, `cargo test -p rayard phase1_smoke_project_sample`, `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 timeline overview automation lanes slice): `TimelineOverview` now accepts automation ranges and draws them as subtle non-interactive bars inside the existing Lighting/Video lanes, so cue markers, playhead, and the fixed 44-viewBox overview stay intact while users can see lighting/video automation spans at a glance. `App.tsx` derives those ranges from lighting and video timeline automations, includes automation keyframes in the overview duration fallback, and labels ranges with fixture/layer plus attribute/param. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 timeline summary readability slice): `TimelineCueEventsPanel` now shows a compact header summary for cue events, lighting automations, video automations, and current timeline position/duration. The summary replaces the old single time label instead of adding another row, keeping the DasLight-style one-screen constraint intact while making shared lighting/video timeline state easier to scan. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 automation playhead range slice): Added matching `Playhead` actions to the lighting and video timeline automation forms. Both buttons set the draft start/end range from the current timeline position plus the active snap/nudge interval, so Beat/Bar/Grid editing can quickly create aligned lighting and VJ automation ranges without manual time entry. The action sits inside the existing form/button row and keeps the one-screen viewport check green. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 automation align-to-playhead slice): Added row-level `Align` actions to existing lighting and video timeline automation drafts. Align moves the draft start to the current playhead while preserving its current duration, using the same snap rules as the rest of the timeline, and still requires an explicit Save before changing the engine state. The three-button row wraps locally inside automation items, preserving fixed app-level viewport containment. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 timeline overview automation drag slice): `TimelineOverview` automation bars are now draggable like cue markers. Overview ranges carry explicit lighting/video kind plus automation id; dragging a bar shifts all of that automation's keyframes by the same delta, snaps the new start to the active timeline grid, and persists through the existing `set_timeline_automation` / `set_timeline_video_automation` commands. Clicking a bar still seeks to its start, while drag click suppression prevents accidental seeks after moving. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-30 (Codex Phase 2 timeline overview automation resize slice): Added left/right resize handles to `TimelineOverview` automation bars. Dragging an edge snaps the resized start/end to the active timeline grid and linearly rescales all keyframe times inside the automation range, so two-keyframe fades and future multi-keyframe curves keep their relative shape while users adjust duration visually. The handles are drawn inside the existing SVG lanes and do not add panel height; app-level viewport containment remains green. Verification: `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, `npm run check:viewport --silent`, and focused `git diff --check` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex Phase 1 smoke report UI slice): Added `Phase1SmokeReportPanel` and wired `runPhase1Smoke` to retain the latest report in App state. Output Diagnostics now shows mini-show pass/fail, cue, active cue id, non-zero count, and U0 A1-A8 byte comparisons, so the topbar `Run Smoke` command leaves visible evidence beyond the transient message text. Verification: `pnpm --dir app build` passed. README/docs intentionally not updated yet.
- 2026-06-23 (Codex viewport containment follow-up): Fixed the remaining app-level height overflow by making `.app` a three-row grid (`WorkspaceChrome`, work layout, footer) instead of letting the status footer become an implicit row, constraining the footer to a compact single line, and making Setup -> Mapping's 2D stage shell fill the available row without a fixed `68vh`/`390px` minimum in mapping mode. Touch panels also no longer impose 420-620px panel minimums, and `layoutTouch` stretches its panels so Touch content uses internal panel scrolling instead of parent overflow clipping. Verification: Chrome CDP checks at 2048px and 1366x768 showed `canWindowScrollY: false`; the 1366x768 pass also showed `canWindowScrollX: false` across Patch, Mapping, Control, and Touch. `node node_modules/typescript/bin/tsc --noEmit`, `node node_modules/vite/bin/vite.js build`, and focused `git diff --check` passed; `pnpm --dir app build` is currently blocked by the Codex runtime pnpm wanting to purge `node_modules` non-interactively, so do not answer that prompt. README/docs intentionally not updated yet.
- `App.tsx` is ~12,119 lines in the current working tree (was ~19,200). Still very large; do not grow it casually. Prefer extracting cohesive UI/helper code to `app/src/components/` or focused helper modules. NOTE: the clean module-level pure-helper/data extractions are essentially done; the remaining high-value reduction is componentizing sections inside the `App()` body.
- No Vite chunk warning currently. The main `index` chunk is ~467.98 kB (under 500 kB) and `effect-panels` is ~28.06 kB because `control-panels`, `effect-panels`, `mapping-editors`, and `workspace-chrome` are split via `manualChunks`. Further extraction is now a maintainability goal and a way to keep the main bundle comfortably below the warning threshold. To actually shrink the main chunk, route newly extracted modules into a named chunk in `vite.config.ts` or lazy-load a heavy panel; otherwise small helper modules still bundle into `index`. Good next refactor targets are the remaining Setup 2D Mapping viewport/config shell and Output/Telemetry diagnostics; Timeline, Touch, Fader attributes, Profile loading, Custom Profile, Patch form, fixture list/editor, DMX patch grid/list, Mapping layers/sidebar, Video Setup wiring, and Effects panel pieces are now mostly componentized.
- Browser visual verification is possible by starting Vite from `app/` without pnpm's install check: `node node_modules/vite/bin/vite.js --host 127.0.0.1 --port 5173`, then use Chrome CDP on a temporary remote-debugging port. If `pnpm --dir app build` prompts to purge `node_modules`, avoid it and run the equivalent direct commands: `node node_modules/typescript/bin/tsc --noEmit` and `node node_modules/vite/bin/vite.js build`.
- README has been adjusted to match the latest product decision: 3D visualization is deferred/external, and the 2D mapping visualizer is the immediate focus.
- The repo contains many broad changes. Before large refactors, inspect the current code paths and avoid undoing unrelated edits.
- 2026-07-10 (Codex M0 DMX/recovery checkpoint slice): Completed the in-progress DMX addressing and project recovery checkpoint work from `COMPLETION_PLAN.md` M0. `dmxAddressing.ts` now owns DMX footprint/range/free-address helpers used by Setup -> Patch and duplicate-fixture flows, and `projectRecoveryStorage.ts` backs the local recovery checkpoint surfaced from the Project menu. The viewport harness now covers the tighter Patch/Recovery affordances. Verification: `npm run check:dmx-addressing --silent`, `npm run check:project-storage --silent`, `cargo test -p rayard project_`, `cargo fmt --check`, `npm run check:viewport --silent`, and `npm run build` passed. `pnpm --dir app build` was intentionally not used because this checkout has a known pnpm node_modules-purge prompt risk; `npm run build` exercised the same `tsc --noEmit && vite build` script. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 stage geometry consolidation slice): Closed the M1 `StageWorldBounds` cleanup item by confirming the bounds type already lives in `stageGeometry.ts` and moving the remaining `StageMap2D` view-box constant onto that shared module. `COMPLETION_PLAN.md` now marks the Stage 2D bounds/constants item complete. Verification: `npm run build`, `npm run check:viewport --silent`, and `git diff --check` passed. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 video setup extraction slice): Extracted the Setup -> Video Setup section into `SetupVideoPanel`, keeping App.tsx as the state/command owner while moving the composition/output setup JSX behind a component boundary. `vite.config.ts` now places the setup video wrapper and its output setup child panels in `video-setup-panels`, dropping the main index chunk from ~493.14 kB to ~472.66 kB. Verification: `npm run build`, `npm run check:viewport --silent`, and `git diff --check` passed. App.tsx is ~13,495 lines after this slice; more reduction still needs larger prop-surface consolidation or another panel extraction. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 video control extraction slice): Extracted the Control/Mixer video diagnostics, output controls, source/layer deck, and video automation composition into `VideoControlPanel`, while leaving the existing App-owned signals and commands unchanged. `vite.config.ts` now places that shell and its non-timeline child panels in `video-control-panels`; the main index chunk dropped from ~472.66 kB to ~441.22 kB, satisfying the M1 <450 kB target. Verification: `npm run build`, `npm run check:viewport --silent` (all checked workspaces at 1280x720, 1366x768, and 2048x1129 reported `moved=0,0`), and `git diff --check` passed. App.tsx is ~13,481 lines after this slice. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 mapping hotkey help slice): Added a compact Setup -> Mapping shortcut overlay with 27 existing tool/view/selection/arrange/live-flag bindings. It opens from the `?` tool-rail button or `Shift+/`, closes with the button or `Esc`, and scrolls only inside the overlay without adding layout height. The viewport harness now opens the help from the keyboard and verifies the panel/key count at every checked resolution. Verification: `npm run build` (main index ~443.84 kB), `npm run check:viewport --silent` (mapping help `1/27`, all checked workspaces `moved=0,0` at 1280x720, 1366x768, and 2048x1129), `git diff --check`, and a 1366x768 browser visual check passed; the help was 738x530 inside the 1013x547 mapping shell while document/app remained exactly 1366x768. `COMPLETION_PLAN.md` now marks the Mapping hotkey help item complete. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 effect draft restoration audit): Extracted target resolution for `Use Draft` into the pure `effectDraftTargetPlan` helper and added `check:effect-draft` coverage for mixed fixture/group/map-selection + video targets and video-only targets. Standard Rayard-authored mixed drafts restore both sides. The editor still represents one video binding; imported effects with more bindings keep the additional bindings active in the engine and now state that limitation in the status message instead of silently hiding it. Verification: `npm run check:effect-draft --silent`, `npm run build` (main index ~444.47 kB), and `git diff --check` passed. App.tsx dropped from ~13,498 to ~13,466 lines. `COMPLETION_PLAN.md` now marks the effect draft item complete. Viewport was not rerun because this slice changed pure target planning and status text only; the immediately preceding UI slice passed the full three-resolution harness. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 viewport fixture data extraction): Moved the browser-only timeline/viewport sample profile, patched fixtures, video layer/composition/output, projector mapping, and stage object out of `App.tsx` into `viewportFixtureData.ts`. The App now only detects the requested browser fixture and injects the shared data. Verification: `npm run build` (main index ~444.65 kB), `npm run check:viewport --silent` (all sample-backed Setup/Control/Touch checks and mapping help passed at 1280x720, 1366x768, and 2048x1129 with `moved=0,0`), and `git diff --check` passed. Physical App.tsx length dropped from 14,008 to 13,804 lines. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 mapping runtime type extraction): Moved the 2D mapping fixture/surface/stage-object view models, drag/marquee/pan/snap/bounds state unions, mapping axis modes, projector surface sizing, and GDTF geometry class calculation into `mappingRuntime.ts`. This keeps the large mapping controller's state contracts together without changing signal ownership or runtime behavior. Verification: `npm run build` (main index ~444.65 kB) and `git diff --check` passed. Physical App.tsx length dropped from 13,804 to 13,600 lines. The viewport harness was not rerun because this slice moved type declarations and two unchanged pure calculations only; the immediately preceding data extraction passed the full harness. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 fixture control runtime extraction): Consolidated fader/color/position/Touch dimmer runtime contracts and pure fixture attribute lookup/read helpers into `fixtureControlRuntime.ts`. Removed App-local copies of `DmxPatchSegment`, `DmxUniverseMap`, and `DmxAddressCell` and now imports the canonical exported contracts from `DmxPatchMapPanel`. Verification: `npm run build` (main index ~444.65 kB) and `git diff --check` passed. Physical App.tsx length dropped from 13,600 to 13,483 lines. The viewport harness was not rerun because this slice moved unchanged type declarations and pure helpers only. README/docs remain deferred to M6.
- 2026-07-10 (Codex M1 mapping viewport model extraction): Moved 2D mapping zoom/center/viewBox, cursor/place preview, snap normalization/lines, stage snapping, and marquee-box derivation into `createMappingViewportModel` while keeping the underlying signals and drag/preset actions App-owned. Added a `mapping-panels` manual chunk for the viewport model, mapping runtime, and `Mapping*` components; the main index dropped from ~444.65 kB to ~377.96 kB (`mapping-panels` ~68.44 kB). Verification: `npm run build`, `npm run check:viewport --silent` (all checks passed at 1280x720, 1366x768, and 2048x1129 with `moved=0,0`), and `git diff --check` passed. Physical App.tsx length dropped from 13,483 to 13,382 lines. README/docs remain deferred to M6.

## Suggested Next Work

1. Stabilize Phase 1 handoff:
   - Run the targeted tests listed above after touching related code.
   - Run `pnpm --dir app build` after frontend changes.
   - Run a full workspace/Tauri build only at a milestone. Last milestone passed on 2026-06-14.

2. Frontend cleanup:
   - Extract the Setup 2D Mapping view from `App.tsx`.
   - Extract Output/Telemetry diagnostics from `App.tsx`.
   - Keep DasLight-like dense controls and dark grid mapping style.

3. Phase 1 operational smoke:
   - Create or document a small local smoke workflow: custom fixture profile, patch to U0 A1, set Dimmer, verify DMX Raw, send Art-Net loopback/test frame.
   - Keep `samples/phase1-mini-spot.fixture` and `samples/phase1-mini-show.ry` valid as the quick regression assets when project-file fields change.

4. Phase 2 direction:
   - Expand effect UI around LFO and position-wave presets.
   - Keep shared effect sources capable of targeting both lighting and video.
   - Keep 2D mapping as the stage coordinate source for position-wave fixtures and projector surfaces.

5. Later VJ direction:
   - Replace CPU preview output windows with native wgpu output surfaces.
   - Add real in-process FFmpeg/HAP decode scheduling.
   - Add NDI, Spout, and Syphon transport bindings behind platform-specific modules.

## Handoff Rule Of Thumb

Move in small verified slices. For this repo, a useful slice usually touches one boundary and one test:

- Engine behavior -> `crates/engine/src/lib.rs` plus a focused `cargo test -p engine ...`.
- Tauri command/validation/project files -> `app/src-tauri/src/main.rs` plus `cargo test -p rayard ...`.
- Frontend UI -> extracted component/helper plus `pnpm --dir app build`.
- GDTF parsing -> `crates/gdtf/src/lib.rs` plus `cargo test -p gdtf`.

- 2026-07-10 (Codex M1 mapping render model extraction): Moved fixture/projector/stage-object drag previews and 2D visualizer render-model derivation out of `App.tsx` into `app/src/createMappingRenderModel.ts`. `App.tsx` dropped from 13,382 to 13,047 lines, the main Vite index chunk dropped to about 371.85 kB, and Mapping remains in the existing `mapping-panels` chunk. Verification: `npm run build`, the full `npm run check:viewport --silent` matrix at 1280x720, 1366x768, and 2048x1129, and `git diff --check` passed.
- 2026-07-10 (Codex M1 Setup Mapping workspace extraction): Added `SetupMappingWorkspace` as the typed composition boundary for the fixture list/editor, DMX patch map, 2D stage viewport, stage configuration, layer renderer, and selection sidebar. `App.tsx` keeps state and command ownership while the DasLight-style Mapping desk structure now lives in its own Mapping chunk module. Verification: `npm run build`, the full `npm run check:viewport --silent` matrix at 1280x720, 1366x768, and 2048x1129, and `git diff --check` passed.
