# オペレーター実機監査とT24系トランシェ（2026-07-29）

- 目的: 「Daslightを超える照明ソフトとしての完成」をユーザー指示（2026-07-29）として受け、内部ゲートではなく**人が操作する・見る**軸で現HEAD `9cab090` を実機監査し、修正トランシェ系列T24を定義する。
- 手法: native debug buildへ`Shinkan2026.dvc`（灯体41・キュー30・シーンバンク13・シーンブロック42）を実インポートし、実ウィンドウ操作＋キャプチャ＋3倍拡大＋コード裏取り。証跡は`target/qa/ui-comparison/audit-2026-07-29/`（gitignore領域、要旨は本書へ固定）。
- 安全: インポート時にDMX経路は全無効化されており、監査中の実出力なし。

## 所見（深刻度順）と対応トランシェ

| # | 事象 | 深刻度 | 裏取り | 対応 |
|---|---|---|---|---|
| F1 | 41灯密度で2Dステージのラベルが完全判読不能（フルプロファイル名を全灯体へ常時描画、省略・衝突回避・密度連動なし） | H | 3倍拡大キャプチャ | **T24-A（完了）** |
| F2 | Setup>Mapping上半分の約85%が死空間（設定行以外は空白、配置キャンバスは下段の小ステージのみ） | H | 全景キャプチャ | T24-B |
| F3 | ファイルダイアログが親ウィンドウ未設定（`main.rs`の`rfd::FileDialog::new()`全箇所）。最大化ウィンドウの背後に隠れてオペレーターが見失う | H | EnumWindowsで隠れダイアログ検出＋コード確認 | T24-C |
| F4 | .dvcインポートの進行表示ゼロ。**訂正（T24-C検証時）**: 監査時の「数十秒無反応」の実体はF3の隠れダイアログ（送信未達）であり、パース自体はShinkan規模で1秒未満。ビジー表示は低速環境・大型ファイルへの保険として妥当 | M→L（実測後） | T24-C実機検証で親付きダイアログ→送信→900ms以内に完了メッセージ表示 | T24-C |
| F5 | インポートされた42シーンブロックはSuper Scene Cueの`child_timeline`に入る（`dvc_import.rs:2885`）が、ショータイムラインが空に見え導線ゼロ。MatrixセルにSuper Sceneバッジなし（タイムラインブロックには`is_super_scene`あり） | M | コード＋実機 | T24-D |
| F6 | タイムライン全幅展開でもライブミキサー行が上部を占有し実レーン領域は下端~120px | M | キャプチャ | T24-B/E |
| F7 | Control文脈タブ「ミキサー」が映像フルウィンドウ（VJデスク）へ遷移し照明デスクが消える。照明のLive Mixer（グループ）と用語衝突 | M | キャプチャ | T24-E |
| F8 | Scene Matrix 13バンクが7列＋横スクロール（Daslight比で列密度低） | L-M | キャプチャ | Phase C計測後判断 |
| F9 | ライブ編集フェーダーペインの右半分~500pxが空 | L-M | キャプチャ | T24-E |
| F10 | BPMタップボタンがマスター行下端にクリップ | L | キャプチャ | T24-E |
| F11 | インポートレポートに「シーンブロック→どのSuper Sceneへ」のリンクなし | L | キャプチャ | T24-D |
| F12 | 復旧復元の状態メッセージが日本語ロケールでも英語（"Recovered ... Save to keep it."） | L | 実機 | T24-E |

維持すべき強み: identity色相の貫通、インポートレポートの分類・警告、Touchサーフェス（シーン即ボタン化＋Safety Deck＋T17モディファイア）、空状態ガイダンス、VJ初回導線の安全設計。

Daslight並置根拠: Daslightはステージにラベルを一切描かない（小グリフのみ、識別は選択リスト/グループリボン）。T24-Aの「密度超過時は選択/ホバー/ハイライトのみラベル」はこの思想へ情報量で上回りつつ収束する。

## T24-A ステージ可読性（✅完了 2026-07-29、Codex gpt-5.6-sol実装 / Fable監督検証）

実装: 新設`app/src/stageLabelLayout.ts`（純関数: 12文字＋…省略、可視25灯超＋ズーム<150%で選択/ホバー/ハイライトのみ、優先度=選択>ホバー>ハイライト>DMXアドレス順、8px空間グリッドで衝突除外、O(N + C log C + C·K·B)）。`StageGlyphs.tsx`が全文をtitle属性で保持、Mapping/StagePreview2D両消費者は`createMemo`単位で計算。`check:stage-labels`ゲート新設（実ソースをTS APIでトランスパイルして断言）。既存ハーネス断言の変更なし。vite chunk再配置で main index 476.41kB、circular chunk警告はベースライン2件→1件へ減少（stash A/Bで確認）。

監督検証（全て独立再実行・実exit code確認）: tsc 0 / vite build 0 / localization 100% / identity-color / large-show-ui / stage-labels / git diff --check、フルviewportマトリクス**232 pass / 0 fail**。ネイティブ受入: 再ビルドexeへ復旧チェックポイントからShinkan状態を復元し、同一クロップ座標のビフォーアフターでラベル塊消滅＋選択1灯のみ「mega par pro…」表示、拡大156%で衝突回避付き全ラベル復帰を目視確認。

## 未確定事項（要追加調査）

- F13（確定・M）: Shinkanインポート後、FXワークスペースは「0 有効 · 0 スタック」（j5-fx-workspace.png）。コード裏取り: 変換エフェクトは`CueEffectTarget`（Cue所有）として格納され、グローバルスタックはインポーターのテスト自身が`effects.is_empty()`を断言する設計（dvc_import.rs:1121・3596）。機能欠落ではないが、FXワークスペース側に「Cue所有エフェクトがN件存在する」ことへの導線・表示がゼロで、オペレーターはインポートしたエフェクトを見失う。→ T24-Dを「インポート内容の発見導線」（Super Scene子タイムライン＋Cue所有エフェクト）として拡張して対応。

## T24系完走記録（2026-07-29、同日中に全5トランシェ）

| トランシェ | コミット | 対応所見 | ネイティブ受入の要点 |
|---|---|---|---|
| T24-A ステージ可読性 | 23b50a5 | F1 | 同一座標ビフォーアフターでラベル塊消滅、選択1灯のみ「mega par pro…」、拡大156%で衝突回避付き全復帰 |
| T24-C ダイアログ親＋再入ガード | 6b00c9f | F3+F4 | メインクリック後も前面維持（still present=True foregroundIsDialog=True）。F4真因はF3と実測訂正（パースは1秒未満） |
| T24-D インポート発見導線 | aba8d89 | F5+F11+F13 | 「Cue所有 6」表示・ライブラック到達経路・SSバッジ。バッジ切り詰め欠陥を目視検出→非クリップ断言へ強化 |
| T24-B Mapping死空間解消 | 4f7bb22 | F2 | ステージ373→941px（2.52倍）で41灯が作業サイズ表示、離脱0.5px復元。帯述語の再交渉漏れ→過修正を監督フルマトリクスで2段検出 |
| T24-E 小型バッチ | e334c15 | F7+F9+F10+F12 | VJデスク改名・読取ストレッチ（重なり検出→修正）・タップ完全表示・復旧メッセージ日本語化 |

監督手順の実績: 各トランシェで全ゲート独立再実行（実exit code）＋フルviewportマトリクス232pass＋再ビルドexeへの復旧1クリック復元→実機目視。ネイティブ目視はハーネスが構造的に見えない視覚欠陥2件（ellipsisクリップ・行重なり）を捕捉した。

## Phase C 初回試行の記録（2026-07-29）

Daslight 5を起動し（ユーザー許可済み）、Shinkan2026.dvcの読込を自動化（Ctrl+O→ダイアログへフルパス→Open）で2回試行したが、**2回とも読込直後にDaslight 5がクラッシュ**（クラッシュレポートは送信せず閉、.dvcファイルは読込のみで未変更・無保存）。自動試行は本番ツール保護のため中止。**その後ユーザーが手動でShinkanを開いた状態を提供**し（手動読込は正常＝クラッシュは自動化経路起因の環境要因と裏付け）、安全操作（タブ/ビュー切替のみ、シーンセル非接触）で並置キャプチャを取得した: dl-11（CONTROL全景）/dl-12（実ショーTIMELINE）/dl-13（MIXER）/dl-14（TOUCH）/dl-15（SETUP）。

### 並置観測（2026-07-29、双方Shinkan2026ロード状態）

- **F8 Matrix密度**: Daslightは約12グループ列を同時表示（列幅~145px、カード~66px高で列あたり最大6枚可視）。Syndocalは同幅で7列（列幅~240px、セル46px）。同時可視グループ数はDaslightが約1.7倍。SyndocalのセルはLive modifierストリップ内蔵で1セルの情報量は多い——トレードオフとして記録し、列幅コンパクト化の要否はT25候補として判断する。
- **タイムライン**: Daslight実ショーは13トラック（eye/lock/展開ガター）に名前＋デュレーション刻印付き色ブロック（例: Par-OrangeStrobe [2.78x] 02m00s20）、音声トラック、レーン領域がペイン高の大半。Syndocal側はT24-Fで展開時レーン472px（3.2倍）へ改善中。DaslightのSS SUPER SCENEカードとSyndocalのSSバッジ（T24-D）は型表示でパリティ。
- **ステージ**: Daslightは41灯を無ラベル小グリフで表示（T24-Aのデクラッタ方向の妥当性を再確認）。
- **操作手数（構造観測、ジェスチャ実測ではない）**: ワークスペース切替 SETUP/CONTROL/TOUCH=1クリック（Syndocal同数）/ 文脈面切替 LIVE EDIT/TIMELINE/MIXER=1クリック（Syndocal ライブ編集/タイムライン/VJデスク同数）/ EDIT⇄LIVEトグル=1クリック（同数）/ ブロック選択→固定BLOCK PROPERTIES（Syndocal T3固定インスペクタと同型）。シーントリガー等の実行を伴う計測は未実施のまま（実行操作はユーザー依頼で行う）。

## 未走破（次回監査で継続）

- J4: 静的プログラミング→Cue保存の手数計測 / J5: FXワークスペース実操作（近似44エフェクトのRack密度）/ J8後半: 保存→再起動→再開
- Daslight側の新規並置キャプチャと未計測10タスクの実測（Phase C、実行トリガー操作はユーザー依頼）
