HISTORICAL TASK PROMPT - NOT ACTIVE INSTRUCTIONS
Retained for traceability only. This file does not establish current ownership,
completion, permission to edit, or a resume order. Use the current user task and
AGENTS.md; reusing an old assignment requires a fresh scope/ownership check.
The original prompt follows unchanged. This label does not claim it completed.

You are the implementation owner for a narrow Syndocal VJ Desk UX tranche in C:\Users\kouty\Documents\KDMX.

Model role: Luna Max implementation. Work autonomously and do not ask for clarification unless truly blocked.

Exclusive file ownership for this task:
- app/src/components/VideoClipGridPanel.tsx
- app/src/components/VideoControlPanel.tsx
- app/src/styles.css
- focused UI/static test files only if necessary; report the exact extra file before editing it.

Do not edit app/src-tauri/src/main.rs, protocol, engine, release docs, RELEASE_STATUS.md, qa/CODEX_*, or any other files. Other agents are editing main.rs concurrently. Never commit, stage, push, reset, checkout, launch native Syndocal, kill processes, or run a full native build.

Objective: fix the operator-critical VJ Desk defect where Import Media / source creation becomes unreachable after the clip grid has content. There must be exactly one reachable media-import entry for the whole VJ Desk in each state: empty, populated normal, populated mixer/compact, and full bank/page. Existing `VideoSourceCreatePanel` already contains `Import Multiple` in normal mode; do not add a second grid-level import button while it remains visible. In mixer mode the source panel is hidden, so expose the same source-create surface through one compact disclosure or replace/reposition the existing entry, but do not render a second same-callback action when that disclosure is open. Empty first-run `Choose Media & Set Up` and ordinary `Import Media` must not appear together; preserve its safe bootstrap semantics. Keep the existing VideoSourceCreatePanel workflow and backend state; do not invent a parallel importer or modal. Preserve current typography, controls, icon sizes, spacing, and hit targets; solve space pressure with layout/reflow/disclosure, never global shrinkage. Maintain keyboard/a11y semantics and at least the current accessible name. Do not change behavior outside this route.

Important callback truth: the production `importMediaFiles` callback catches failures internally and reports them through the existing application message path. A local wrapper `catch` cannot observe those errors. Do not claim or implement a fake local import-error state. You may show local busy only if the Promise lifetime is real, while leaving truthful failure feedback on the existing application path; document this remaining localization boundary honestly.

First inspect the actual rendering predicates and current viewport/static harness contracts. Implement the smallest coherent production change. Add or strengthen a focused structural/component test that proves all of: empty grid has exactly one reachable import entry, populated normal grid has one, populated mixer/compact grid has one, and the source create surface remains reachable without losing clips. A mere global string-presence grep is insufficient. If the current test stack cannot mount the component, add a narrow deterministic source-contract harness with branch-specific slices and non-vacuous marker checks, and state that limitation honestly.

Run proportionate gates: pnpm --dir app typecheck or the repository equivalent, the focused test/harness you add or update, cargo fmt only if Rust was not touched (normally no need), and git diff --check on owned files. Do not run the full viewport or native release gate. At the end report changed files, old behavior, new behavior, proof/gates, and remaining visual/native boundary. Then stop editing and leave the worktree uncommitted.
